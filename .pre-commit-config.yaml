ci:
  autoupdate_branch: flowMC-dev
  skip: [uv-lock, pyright]

repos:
  - repo: https://github.com/astral-sh/uv-pre-commit
    rev: 0.11.26
    hooks:
      - id: uv-lock
        args: ["--upgrade"]

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.20
    hooks:
      - id: ruff-check
        args: ["--fix"]
        files: src/
      - id: ruff-format
        files: src/
  
  - repo: https://github.com/RobertCraigie/pyright-python
    rev: v1.1.411
    hooks:
    - id: pyright
      files: src/
      additional_dependencies: [beartype, "diffrax>=0.7.0", einops, "equinox>=0.11.12", "jax>=0.8.2", "jaxtyping>=0.3.3", "optax>=0.2.7", pytest, "tqdm>=4.67.1", typing_extensions]
