{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"flowMC","text":"","path":["flowMC"],"tags":[]},{"location":"#a-jax-based-normalizing-flow-enhanced-mcmc-sampler-for-probabilistic-inference","level":2,"title":"A JAX-based normalizing-flow-enhanced MCMC sampler for probabilistic inference","text":"<p>flowMC is a JAX-based package for normalizing-flow-enhanced Markov chain Monte Carlo (MCMC) sampling. By using normalizing flows as a global proposal, flowMC accelerates convergence for multi-modal and high-dimensional posteriors while running natively on GPU with minimal hyperparameter tuning.</p> <p>Warning</p> <p>flowMC has not yet reached v1.0.0 and the API may change. Use at your own risk. Consider pinning to a specific version if you need API stability.</p>","path":["flowMC"],"tags":[]},{"location":"#documentation","level":2,"title":"Documentation","text":"<ul> <li>Installation — How to install flowMC</li> <li>Quick Start — A basic example to get started</li> <li>Tutorials — Hands-on worked examples</li> <li>Guides — In-depth coverage of each module</li> <li>FAQ — Answers to common questions</li> <li>Citing flowMC — How to cite flowMC</li> <li>Contributing to flowMC — How to contribute to flowMC</li> </ul>","path":["flowMC"],"tags":[]},{"location":"FAQ/","level":1,"title":"FAQ","text":"<p>My local sampler is not accepting</p> <p>This usually means you are setting the step size of the local sampler to be too big. Try reducing the step size in your local sampler.</p> <p>Alternative, this could also mean your sampler is proposing in a region where the target log-PDF is ill-defined (i.e. NaN either in <code>logpdf</code> or its derivative if you are using a gradient-based local sampler). It is worth making sure your <code>logpdf</code> is well-defined across the entire region you are sampling.</p> <p>In order for my local sampler to accept, I have to choose a very small step size, which makes my chain very correlated.</p> <p>This usually indicate some of your parameters are much better measured than others. Since taking a small step in those directions will already change your <code>logpdf</code> value by a lot, the exploration power of the local sampler in other parameters are limited by those which are well measured. Currently, we support different step size for different parameters, which you can tune to see whether that improves the situation or not. If you know the scale of each parameter ahead of time, reparameterizing them to maintain roughly equal scale across parameters also helps.</p> <p>My global sample's loss is exploding/not decreasing</p> <p>This usually means your learning rate used for training the normalizing flow is too large. Try reducing the learning rate by a factor of ten.</p> <p>Another reason for a flat loss is your local sampler is not accepting at all. This is a bit rarer since this means your data used to train the normalizing flow is just your prior, which the normalizing flow should still be able to learn.</p> <p>The sampler is stuck a bit until it starts sampling</p> <p>If you use the option <code>jit</code> in constructing the local sampler, the code will compile your code to speed up the execution. The sampler is not really stuck, but it is compiling the code. Depending on how you code up your <code>logpdf</code> function, the compilation can take a while. If you don't want to wait, you can set <code>jit=False</code>, which would increase the sampling time.</p> <p>The compilation is slow</p> <p>If you have a <code>logpdf</code> with many lines, JAX will take a long time to compile the code. JAX is known to be slow in compilation, especially if your computational graph uses some sort of loop that call a function many times. While we cannot fundamentally get rid of the problem, using a jax.lax.scan is usually how we deal with it.</p> <p>I am running out of GPU memory</p> <p>flowMC has two independent memory bottlenecks, so there are two knobs to turn.</p> <p>The first — and usually the bottleneck — is the NF proposal step. During each global step the target log-PDF (<code>logpdf</code>) is evaluated at all <code>n_chains * n_global_steps</code> flow proposals at once. Reduce <code>n_NFproposal_batch_size</code> (default <code>10000</code>): when <code>n_global_steps</code> exceeds it, the proposals are evaluated in smaller <code>jax.lax.map</code> chunks rather than a single <code>vmap</code>, lowering peak memory. Try this first.</p> <p>The second matters when your <code>logpdf</code> is expensive to evaluate. Sometimes the problem is not the <code>n_chains * n_global_steps</code> proposals, but that even a single <code>logpdf</code> evaluation per chain across all <code>n_chains</code> chains will not fit. In that case also set <code>chain_batch_size</code> to a small positive integer (default <code>0</code> means all chains are vmapped at once; smaller values use less memory) so the local sampler processes chains in sequential sub-batches.</p> <p>Both are bundle arguments (e.g. to <code>RQSpline_MALA_Bundle</code>); see the hyperparameters guide for details.</p>","path":["FAQ"],"tags":[]},{"location":"citing/","level":1,"title":"Citation &amp; Origins","text":"","path":["Citation &amp; Origins"],"tags":[]},{"location":"citing/#origins","level":2,"title":"Origins","text":"<p>flowMC was originally developed as kazewong/flowMC by Kaze W. K. Wong and others. The original repository is no longer actively maintained; this fork is the active continuation of the project.</p>","path":["Citation &amp; Origins"],"tags":[]},{"location":"citing/#citation","level":2,"title":"Citation","text":"<p>If you use flowMC in your research, please cite the following papers:</p> <pre><code>@article{Wong:2022xvh,\n    author = \"Wong, Kaze W. k. and Gabri\\'e, Marylou and Foreman-Mackey, Daniel\",\n    title = \"{flowMC: Normalizing flow enhanced sampling package for probabilistic inference in JAX}\",\n    eprint = \"2211.06397\",\n    archivePrefix = \"arXiv\",\n    primaryClass = \"astro-ph.IM\",\n    doi = \"10.21105/joss.05021\",\n    journal = \"J. Open Source Softw.\",\n    volume = \"8\",\n    number = \"83\",\n    pages = \"5021\",\n    year = \"2023\"\n}\n\n@article{Gabrie:2021tlu,\n    author = \"Gabri\\'e, Marylou and Rotskoff, Grant M. and Vanden-Eijnden, Eric\",\n    title = \"{Adaptive Monte Carlo augmented with normalizing flows}\",\n    eprint = \"2105.12603\",\n    archivePrefix = \"arXiv\",\n    primaryClass = \"physics.data-an\",\n    doi = \"10.1073/pnas.2109420119\",\n    journal = \"Proc. Nat. Acad. Sci.\",\n    volume = \"119\",\n    number = \"10\",\n    pages = \"e2109420119\",\n    year = \"2022\"\n}\n</code></pre>","path":["Citation &amp; Origins"],"tags":[]},{"location":"contributing/","level":1,"title":"Contributing","text":"<p>Contributions of any kind are welcome and appreciated. See the guidelines below.</p>","path":["Contributing"],"tags":[]},{"location":"contributing/#expectations","level":2,"title":"Expectations","text":"<p>flowMC is developed and maintained by the GW JAX Team and community contributors. While we try to be responsive, we don’t always get to every issue immediately. If it has been more than a week or two, feel free to ping the maintainers on the issue.</p>","path":["Contributing"],"tags":[]},{"location":"contributing/#did-you-find-a-bug","level":2,"title":"Did you find a bug?","text":"<p>Ensure the bug was not already reported by searching on GitHub under Issues. If you’re unable to find an open issue addressing the problem, open a new one. Be sure to include a title and clear description, as much relevant information as possible, and the simplest possible code sample demonstrating the expected behaviour that is not occurring.</p>","path":["Contributing"],"tags":[]},{"location":"contributing/#did-you-write-a-patch-that-fixes-a-bug","level":2,"title":"Did you write a patch that fixes a bug?","text":"<p>Open a new GitHub pull request with the patch. Ensure the PR description clearly describes the problem and solution. Include the relevant issue number if applicable.</p>","path":["Contributing"],"tags":[]},{"location":"contributing/#do-you-intend-to-add-a-new-feature-or-change-an-existing-one","level":2,"title":"Do you intend to add a new feature or change an existing one?","text":"<p>Open a new GitHub pull request with the feature or change. Please follow these principles:</p> <ol> <li>New features should be able to take advantage of <code>jax.jit</code> wherever possible.</li> <li>Lightweight and modular implementation is preferred.</li> <li>The core package only does sampling. If you have a concrete example involving a complete analysis (plotting, models, etc.), see the tutorial contribution guide below.</li> </ol> <p>If you are unsure whether a feature fits, open an issue first to discuss it with the maintainers.</p>","path":["Contributing"],"tags":[]},{"location":"contributing/#do-you-intend-to-add-an-example-or-tutorial","level":2,"title":"Do you intend to add an example or tutorial?","text":"<p>Open a new GitHub pull request with the example or tutorial. The example should be self-contained and keep imports from other packages to a minimum. Leave case-specific analysis details out. For more extensive tutorials, we encourage the community to link minimal examples hosted on the flowMC documentation to documentation from other packages.</p>","path":["Contributing"],"tags":[]},{"location":"installation/","level":1,"title":"Installation","text":"<p>The simplest way to install flowMC is through pip:</p> <pre><code>pip install flowMC\n</code></pre> <p>This will install the latest stable release and its dependencies. flowMC is built on JAX. By default, this installs the CPU version of JAX. If you have an NVIDIA GPU, install the CUDA-enabled version:</p> <pre><code>pip install \"flowMC[cuda]\"\n</code></pre> <p>If you want to install the latest version of flowMC, you can clone this repo and install it locally:</p> <pre><code>git clone https://github.com/GW-JAX-Team/flowMC.git\ncd flowMC\npip install -e .\n</code></pre> <p>We recommend using uv to manage your Python environment. After cloning the repository, run <code>uv sync</code> to create a virtual environment with all dependencies installed.</p>","path":["Installation"],"tags":[]},{"location":"quickstart/","level":1,"title":"Quick Start","text":"","path":["Quick Start"],"tags":[]},{"location":"quickstart/#basic-usage","level":2,"title":"Basic Usage","text":"<p>To sample an N dimensional Gaussian, you would do something like:</p> <pre><code>import jax\nimport jax.numpy as jnp\nfrom flowMC.Sampler import Sampler\nfrom flowMC.resource_strategy_bundle.RQSpline_MALA import RQSpline_MALA_Bundle\n\n# Defining the log posterior\n\ndef log_posterior(x, data: dict):\n    return -0.5 * jnp.sum((x - data[\"data\"]) ** 2)\n\n# Initializing the strategy bundle\nn_dims = 2\nn_chains = 10\nkey_0, key_1, key_2 = jax.random.split(jax.random.key(42), 3)\n\nbundle = RQSpline_MALA_Bundle(\n    rng_key=key_0,\n    n_chains=n_chains,\n    n_dims=n_dims,\n    logpdf=log_posterior,\n    n_local_steps=10,\n    n_global_steps=10,\n    n_training_loops=3,\n    n_production_loops=3,\n    n_epochs=10,\n    rq_spline_hidden_units=[64, 64],\n    rq_spline_n_bins=8,\n    rq_spline_n_layers=3,\n)\n\n# Run the sampler\n\ninitial_position = jax.random.normal(key_1, shape=(n_chains, n_dims)) * 1\nsampler = Sampler(\n    n_dim=n_dims,\n    n_chains=n_chains,\n    rng_key=key_2,\n    resource_strategy_bundles=bundle,\n)\n\nsampler.sample(initial_position, {\"data\": jnp.arange(n_dims).astype(jnp.float32)})\n</code></pre> <p>In the ideal case, the only three things you will have to do are:</p> <ol> <li>Write down the log-probability density function you want to sample in the form of <code>log_p(x)</code>, where <code>x</code> is the vector of variables of interest,</li> <li>Choose your sampling strategy and hyperparameters,</li> <li>Give the sampler the initial position of your chains and start sampling.</li> </ol>","path":["Quick Start"],"tags":[]},{"location":"api/flowMC/Sampler/","level":1,"title":"Sampler","text":"","path":["Reference","flowMC","Sampler"],"tags":[]},{"location":"api/flowMC/Sampler/#flowMC.Sampler.Sampler","level":3,"title":"<code>Sampler</code>","text":"<p>Top level API that the users primarily interact with.</p> <p>Parameters:</p> Name Type Description Default <code>n_dim</code> <code>int</code> <p>Dimension of the parameter space.</p> required <code>n_chains</code> <code>int</code> <p>Number of chains to sample.</p> required <code>rng_key</code> <code>Key</code> <p>JAX PRNGKey.</p> required <code>resources</code> <code>dict[str, Resource]</code> <p>Resources to be used by the sampler.</p> <code>None</code> <code>strategies</code> <code>dict[str, Strategy]</code> <p>Strategies to be used by the sampler.</p> <code>None</code> <code>strategy_order</code> <code>list[str]</code> <p>Order of strategies to execute.</p> <code>None</code> <code>resource_strategy_bundles</code> <code>ResourceStrategyBundle</code> <p>Pre-configured bundle containing resources and strategies.</p> <code>None</code> <code>checkpoint_interval</code> <code>float</code> <p>Minimum wall-clock seconds that must elapse since the previous write before a new checkpoint is written. Default <code>600</code> (10 minutes).  Set to <code>0</code> to disable checkpointing entirely.</p> <code>600.0</code> <p>Methods:</p> Name Description <code>__init__</code> <p>Initialize the sampler.</p> <code>deserialize</code> <p>Deserialize the sampler object.</p> <code>sample</code> <p>Execute the strategy loop and populate resource buffers with samples.</p> <code>serialize</code> <p>Serialize the sampler object.</p>","path":["Reference","flowMC","Sampler"],"tags":[]},{"location":"api/flowMC/Sampler/#flowMC.Sampler.Sampler.__init__","level":4,"title":"<code>__init__(*, n_dim: int, n_chains: int, rng_key: Key, resources: Optional[dict[str, Resource]] = None, strategies: Optional[dict[str, Strategy]] = None, strategy_order: Optional[list[str]] = None, resource_strategy_bundles: Optional[ResourceStrategyBundle] = None, outdir: str = './outdir/', checkpoint_interval: float = 600.0) -&gt; None</code>","text":"<p>Initialize the sampler.</p> <p>Provide either <code>resources</code> + <code>strategies</code> + <code>strategy_order</code> or a <code>resource_strategy_bundles</code> pre-configured bundle.</p> <p>Parameters:</p> Name Type Description Default <code>n_dim</code> <code>int</code> <p>Dimension of the parameter space.</p> required <code>n_chains</code> <code>int</code> <p>Number of parallel chains.</p> required <code>rng_key</code> <code>Key</code> <p>JAX PRNG key.</p> required <code>resources</code> <code>Optional[dict[str, Resource]]</code> <p>Dictionary of named resources (kernels, buffers, etc.). Must be paired with <code>strategies</code>.</p> <code>None</code> <code>strategies</code> <code>Optional[dict[str, Strategy]]</code> <p>Dictionary of named strategies. Must be paired with <code>resources</code>.</p> <code>None</code> <code>strategy_order</code> <code>Optional[list[str]]</code> <p>Ordered list of strategy names to execute each call to <code>sample</code>.</p> <code>None</code> <code>resource_strategy_bundles</code> <code>Optional[ResourceStrategyBundle]</code> <p>Pre-configured bundle that provides resources, strategies, and ordering.</p> <code>None</code> <code>outdir</code> <code>str</code> <p>Directory where <code>checkpoint.pkl</code> is written. Created automatically if it does not exist.  Default <code>\"./outdir/\"</code>.</p> <code>'./outdir/'</code> <code>checkpoint_interval</code> <code>float</code> <p>Minimum wall-clock seconds that must elapse since the previous write before a new checkpoint is written. Default <code>600</code> (10 minutes).  Set to <code>0</code> to disable checkpointing. When checkpointing is enabled the JAX XLA compilation cache is also activated at <code>{outdir}/jax_cache/</code> so that compiled step functions are reused across processes (no recompilation on resume).</p> <code>600.0</code> <p>Raises:     ValueError: If neither <code>resources</code>/<code>strategies</code> nor         <code>resource_strategy_bundles</code> is provided.</p>","path":["Reference","flowMC","Sampler"],"tags":[]},{"location":"api/flowMC/Sampler/#flowMC.Sampler.Sampler.deserialize","level":4,"title":"<code>deserialize()</code>","text":"<p>Deserialize the sampler object.</p>","path":["Reference","flowMC","Sampler"],"tags":[]},{"location":"api/flowMC/Sampler/#flowMC.Sampler.Sampler.sample","level":4,"title":"<code>sample(initial_position: Float[Array, 'n_chains n_dim'], data: dict)</code>","text":"<p>Execute the strategy loop and populate resource buffers with samples.</p> <p>If <code>outdir</code> is set and <code>checkpoint_interval &gt; 0</code>, the sampler writes a checkpoint to <code>{outdir}/checkpoint.pkl</code> atomically after each complete training loop (when at least <code>checkpoint_interval</code> seconds have elapsed since the previous write).  On the next call with the same <code>outdir</code> the sampler detects the existing file, validates it against the current configuration, and resumes from the strategy immediately after the last checkpointed one.</p> <p>Checkpoint validation raises <code>ValueError</code> if:</p> <ul> <li><code>n_dim</code> or <code>n_chains</code> differ from the checkpoint.</li> <li><code>strategy_order</code> differs from the checkpoint.</li> <li>The logpdf fingerprint (evaluated at the first chain's position) differs   by more than <code>1e-6</code>, indicating a change in the likelihood or data.</li> </ul> <p>Parameters:</p> Name Type Description Default <code>initial_position</code> <code>Float[Array, 'n_chains n_dim']</code> <p>Starting chain positions, shape <code>(n_chains, n_dim)</code> or broadcastable.  Ignored when resuming from a checkpoint (the checkpointed positions are used instead).</p> required <code>data</code> <code>dict</code> <p>Arbitrary data dict forwarded to every strategy call.  Must be consistent with any checkpoint on disk; changes are detected via the logpdf fingerprint when available.</p> required","path":["Reference","flowMC","Sampler"],"tags":[]},{"location":"api/flowMC/Sampler/#flowMC.Sampler.Sampler.serialize","level":4,"title":"<code>serialize()</code>","text":"<p>Serialize the sampler object.</p>","path":["Reference","flowMC","Sampler"],"tags":[]},{"location":"api/flowMC/resource/base/","level":1,"title":"Base","text":"","path":["Reference","flowMC","resource","Base"],"tags":[]},{"location":"api/flowMC/resource/base/#flowMC.resource.base.Resource","level":3,"title":"<code>Resource</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Base class for resources. Resources are objects such as local sampler and neural networks.</p> <p>This is an abstract template that should not be directly used.</p> <p>Methods:</p> Name Description <code>load_resource</code> <p>Load a resource from disk.</p> <code>print_parameters</code> <p>Print the tunable parameters of the resource to the debug log.</p> <code>save_resource</code> <p>Save the resource to disk.</p>","path":["Reference","flowMC","resource","Base"],"tags":[]},{"location":"api/flowMC/resource/base/#flowMC.resource.base.Resource.load_resource","level":4,"title":"<code>load_resource(path: str) -&gt; Self</code>  <small><code>abstractmethod</code></small>","text":"<p>Load a resource from disk.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>File path to load the resource from.</p> required <p>Returns:</p> Name Type Description <code>Self</code> <code>Self</code> <p>A new instance populated with the loaded data.</p>","path":["Reference","flowMC","resource","Base"],"tags":[]},{"location":"api/flowMC/resource/base/#flowMC.resource.base.Resource.print_parameters","level":4,"title":"<code>print_parameters() -&gt; None</code>  <small><code>abstractmethod</code></small>","text":"<p>Print the tunable parameters of the resource to the debug log.</p>","path":["Reference","flowMC","resource","Base"],"tags":[]},{"location":"api/flowMC/resource/base/#flowMC.resource.base.Resource.save_resource","level":4,"title":"<code>save_resource(path: str) -&gt; None</code>  <small><code>abstractmethod</code></small>","text":"<p>Save the resource to disk.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>File path (without extension) to save the resource.</p> required","path":["Reference","flowMC","resource","Base"],"tags":[]},{"location":"api/flowMC/resource/buffers/","level":1,"title":"Buffers","text":"","path":["Reference","flowMC","resource","Buffers"],"tags":[]},{"location":"api/flowMC/resource/buffers/#flowMC.resource.buffers.Buffer","level":3,"title":"<code>Buffer</code>","text":"<p>               Bases: <code>Resource</code></p> <p>A fixed-size ring-style buffer for storing MCMC chain positions, log-probs, or accept rates.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Name of the buffer.</p> <code>data</code> <code>Float[Array, ...]</code> <p>Underlying data array, initialised to <code>-inf</code>.</p> <code>cursor</code> <code>int</code> <p>Current write offset along <code>cursor_dim</code>.</p> <code>cursor_dim</code> <code>int</code> <p>Dimension along which <code>update_buffer</code> writes.</p> <p>Methods:</p> Name Description <code>__call__</code> <p>Return the underlying data array.</p> <code>__init__</code> <p>Args:</p> <code>get_distribution</code> <p>Compute a histogram of the flattened buffer data.</p> <code>update_buffer</code> <p>Write new data into the buffer at the specified offset.</p>","path":["Reference","flowMC","resource","Buffers"],"tags":[]},{"location":"api/flowMC/resource/buffers/#flowMC.resource.buffers.Buffer.shape","level":4,"title":"<code>shape: tuple[int, ...]</code>  <small><code>property</code></small>","text":"<p>Shape of the underlying data array.</p>","path":["Reference","flowMC","resource","Buffers"],"tags":[]},{"location":"api/flowMC/resource/buffers/#flowMC.resource.buffers.Buffer.__call__","level":4,"title":"<code>__call__() -&gt; Float[Array, ...]</code>","text":"<p>Return the underlying data array.</p>","path":["Reference","flowMC","resource","Buffers"],"tags":[]},{"location":"api/flowMC/resource/buffers/#flowMC.resource.buffers.Buffer.__init__","level":4,"title":"<code>__init__(name: str, shape: tuple[int, ...], cursor_dim: int = 0) -&gt; None</code>","text":"<p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the buffer.</p> required <code>shape</code> <code>tuple[int, ...]</code> <p>Shape of the underlying array.</p> required <code>cursor_dim</code> <code>int</code> <p>The dimension along which writes are appended. Defaults to 0.</p> <code>0</code>","path":["Reference","flowMC","resource","Buffers"],"tags":[]},{"location":"api/flowMC/resource/buffers/#flowMC.resource.buffers.Buffer.get_distribution","level":4,"title":"<code>get_distribution(n_bins: int = 100) -&gt; tuple</code>","text":"<p>Compute a histogram of the flattened buffer data.</p> <p>Parameters:</p> Name Type Description Default <code>n_bins</code> <code>int</code> <p>Number of histogram bins. Defaults to 100.</p> <code>100</code> <p>Returns:</p> Name Type Description <code>tuple</code> <code>tuple</code> <p><code>(counts, bin_edges)</code> as returned by <code>numpy.histogram</code>.</p>","path":["Reference","flowMC","resource","Buffers"],"tags":[]},{"location":"api/flowMC/resource/buffers/#flowMC.resource.buffers.Buffer.update_buffer","level":4,"title":"<code>update_buffer(updates: Array, start: int = 0) -&gt; None</code>","text":"<p>Write new data into the buffer at the specified offset.</p> <p>Parameters:</p> Name Type Description Default <code>updates</code> <code>Array</code> <p>Data to write. The size of the first dimension of <code>updates</code> determines how many entries are written along <code>cursor_dim</code>.</p> required <code>start</code> <code>int</code> <p>Starting index along <code>cursor_dim</code>. Defaults to 0.</p> <code>0</code>","path":["Reference","flowMC","resource","Buffers"],"tags":[]},{"location":"api/flowMC/resource/logPDF/","level":1,"title":"logPDF","text":"","path":["Reference","flowMC","resource","logPDF"],"tags":[]},{"location":"api/flowMC/resource/logPDF/#flowMC.resource.logPDF.LogPDF","level":3,"title":"<code>LogPDF</code>","text":"<p>               Bases: <code>Resource</code></p> <p>A resource class that holds the log-pdf function. The main purpose of this class is to wrap the log-pdf function into the unified Resource interface.</p> <p>Parameters:</p> Name Type Description Default <code>log_pdf</code> <code>Callable[[Float[Array, \"n_dim\"], PyTree], Float[Array, \"1\"]</code> <p>The log-pdf function</p> required <code>variables</code> <code>list[Variable]</code> <p>The list of variables in the log-pdf function</p> <code>None</code> <p>Methods:</p> Name Description <code>__init__</code> <p>Args:</p>","path":["Reference","flowMC","resource","logPDF"],"tags":[]},{"location":"api/flowMC/resource/logPDF/#flowMC.resource.logPDF.LogPDF.__init__","level":4,"title":"<code>__init__(log_pdf: Callable[[Float[Array, ' n_dim'], PyTree], Float[Array, 1]], variables: Optional[list[Variable]] = None, n_dims: Optional[int] = None)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>log_pdf</code> <code>Callable[[Float[Array, \"n_dim\"], PyTree], Float[Array, \"1\"]</code> <p>The log-pdf function</p> required <code>variables</code> <code>list[Variable]</code> <p>The list of variables in the log-pdf function. Defaults to None. n_dims must be provided if this argument is None.</p> <code>None</code> <code>n_dims</code> <code>int</code> <p>The number of dimensions of the log-pdf function. Defaults to None. If variables is provided, this argument is ignored.</p> <code>None</code>","path":["Reference","flowMC","resource","logPDF"],"tags":[]},{"location":"api/flowMC/resource/logPDF/#flowMC.resource.logPDF.TemperedPDF","level":3,"title":"<code>TemperedPDF</code>","text":"<p>               Bases: <code>LogPDF</code></p> <p>A tempered version of <code>LogPDF</code> used for parallel tempering.</p> <p>The tempered log-pdf at inverse temperature <code>beta = 1/T</code> is:</p> <p>$$</p> <p>\\log p_\\beta(x) = \\beta \\cdot \\log \\mathcal{L}(x) + \\log \\pi(x)</p> <p>$$</p> <p>where \\(\\mathcal{L}\\) is the likelihood and \\(\\pi\\) is the prior.</p> <p>Attributes:</p> Name Type Description <code>log_prior</code> <code>Callable</code> <p>The log-prior function.</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Args:</p>","path":["Reference","flowMC","resource","logPDF"],"tags":[]},{"location":"api/flowMC/resource/logPDF/#flowMC.resource.logPDF.TemperedPDF.__init__","level":4,"title":"<code>__init__(log_likelihood: Callable[[Float[Array, ' n_dim'], PyTree], Float[Array, 1]], log_prior: Callable[[Float[Array, ' n_dim'], PyTree], Float[Array, 1]], variables=None, n_dims=None, n_temps=5, max_temp=100) -&gt; None</code>","text":"<p>Parameters:</p> Name Type Description Default <code>log_likelihood</code> <code>Callable</code> <p>Log-likelihood function <code>f(x, data) -&gt; Float</code>.</p> required <code>log_prior</code> <code>Callable</code> <p>Log-prior function <code>f(x, data) -&gt; Float</code>.</p> required <code>variables</code> <code>list[Variable]</code> <p>Named variable list. Defaults to None.</p> <code>None</code> <code>n_dims</code> <code>int</code> <p>Dimension count if <code>variables</code> is None.</p> <code>None</code> <code>n_temps</code> <code>int</code> <p>Number of temperature levels (unused, reserved).</p> <code>5</code> <code>max_temp</code> <code>float</code> <p>Maximum temperature (unused, reserved).</p> <code>100</code>","path":["Reference","flowMC","resource","logPDF"],"tags":[]},{"location":"api/flowMC/resource/logPDF/#flowMC.resource.logPDF.Variable","level":3,"title":"<code>Variable</code>  <small><code>dataclass</code></small>","text":"<p>A dataclass that holds the information of a variable in the log-pdf function.</p> <p>This main purpose of this class is to let the users name their variables, and specify whether they are continuous or not.</p>","path":["Reference","flowMC","resource","logPDF"],"tags":[]},{"location":"api/flowMC/resource/optimizer/","level":1,"title":"Optimizer","text":"","path":["Reference","flowMC","resource","Optimizer"],"tags":[]},{"location":"api/flowMC/resource/optimizer/#flowMC.resource.optimizer.Optimizer","level":3,"title":"<code>Optimizer</code>","text":"<p>               Bases: <code>Resource</code></p> <p>Adam-W optimiser with gradient clipping for training normalising flow models.</p> <p>Attributes:</p> Name Type Description <code>optim</code> <code>GradientTransformation</code> <p>The composed optax update rule.</p> <code>optim_state</code> <code>OptState</code> <p>Optimiser state initialised from the model.</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Args:</p>","path":["Reference","flowMC","resource","Optimizer"],"tags":[]},{"location":"api/flowMC/resource/optimizer/#flowMC.resource.optimizer.Optimizer.__init__","level":4,"title":"<code>__init__(model: eqx.Module, learning_rate: float = 0.001, momentum: float = 0.9) -&gt; None</code>","text":"<p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The model whose trainable parameters will be optimised. Used only to initialise the optimiser state.</p> required <code>learning_rate</code> <code>float</code> <p>Adam learning rate. Defaults to 1e-3.</p> <code>0.001</code> <code>momentum</code> <code>float</code> <p>Adam first-moment decay (<code>b1</code>). Defaults to 0.9.</p> <code>0.9</code>","path":["Reference","flowMC","resource","Optimizer"],"tags":[]},{"location":"api/flowMC/resource/states/","level":1,"title":"States","text":"","path":["Reference","flowMC","resource","States"],"tags":[]},{"location":"api/flowMC/resource/states/#flowMC.resource.states.State","level":3,"title":"<code>State</code>","text":"<p>               Bases: <code>Resource</code></p> <p>A Resource class that holds the state of the system. This is essentially a wrapper around a dictionary such that it can be handled by flowMC.</p> <p>We restrict the type of the state to be simple types including integers, booleans and strings. The main reason for this is State is expected to be used to indiciate stage of individual strategies instead of storing parameters to resources. I.e. State should hold whehter the sampler is in training phase or production phase. But not mass matrix of a kernel per se.</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Initialize the state.</p> <code>update</code> <p>Update the state with new data.</p>","path":["Reference","flowMC","resource","States"],"tags":[]},{"location":"api/flowMC/resource/states/#flowMC.resource.states.State.__init__","level":4,"title":"<code>__init__(data: dict[str, int | bool | str], name: str = 'State')</code>","text":"<p>Initialize the state.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict</code> <p>The data to initialize the state with.</p> required <code>name</code> <code>str</code> <p>The name of the state.</p> <code>'State'</code>","path":["Reference","flowMC","resource","States"],"tags":[]},{"location":"api/flowMC/resource/states/#flowMC.resource.states.State.update","level":4,"title":"<code>update(key: list[str], value: list[int | bool | str])</code>","text":"<p>Update the state with new data.</p> <p>This will modify the state in place.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>The key to update.</p> required <code>value</code> <code>int | bool | str</code> <p>The value to update.</p> required","path":["Reference","flowMC","resource","States"],"tags":[]},{"location":"api/flowMC/resource/kernel/Gaussian_random_walk/","level":1,"title":"Gaussian random walk","text":"","path":["Reference","flowMC","resource","kernel","Gaussian random walk"],"tags":[]},{"location":"api/flowMC/resource/kernel/Gaussian_random_walk/#flowMC.resource.kernel.Gaussian_random_walk.GaussianRandomWalk","level":3,"title":"<code>GaussianRandomWalk</code>","text":"<p>               Bases: <code>ProposalBase</code></p> <p>Gaussian random walk sampler class.</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Initialize Gaussian Random Walk sampler.</p> <code>adapt_step_size</code> <p>Adapt step size based on acceptance rate.</p> <code>apply_per_dim_scaling</code> <p>Multiply step_size element-wise by ratios (returns a new GaussianRandomWalk).</p> <code>get_effective_dim_profile</code> <p>Return the current per-dim step size profile normalised to geometric mean 1.</p> <code>kernel</code> <p>Random walk gaussian kernel. This is a kernel that only evolve a single</p>","path":["Reference","flowMC","resource","kernel","Gaussian random walk"],"tags":[]},{"location":"api/flowMC/resource/kernel/Gaussian_random_walk/#flowMC.resource.kernel.Gaussian_random_walk.GaussianRandomWalk.__init__","level":4,"title":"<code>__init__(step_size: Float[Array, ' n_dim'], periodic_mask: Optional[Bool[Array, ' n_dim']] = None, periodic_bounds: Optional[Float[Array, 'n_dim 2']] = None)</code>","text":"<p>Initialize Gaussian Random Walk sampler.</p> <p>Parameters:</p> Name Type Description Default <code>step_size</code> <code>Float[Array, ' n_dim']</code> <p>Step size for the random walk as a 1D array representing       diagonal elements of the proposal covariance matrix.</p> required <code>periodic_mask</code> <code>Optional[Bool[Array, ' n_dim']]</code> <p>Boolean mask indicating which dimensions are periodic. If None, no periodic boundaries are applied.</p> <code>None</code> <code>periodic_bounds</code> <code>Optional[Float[Array, 'n_dim 2']]</code> <p>Array of shape (n_dim, 2) with [lower, upper] bounds for each periodic dimension. Only used where periodic_mask is True. If None, no periodic boundaries are applied.</p> <code>None</code>","path":["Reference","flowMC","resource","kernel","Gaussian random walk"],"tags":[]},{"location":"api/flowMC/resource/kernel/Gaussian_random_walk/#flowMC.resource.kernel.Gaussian_random_walk.GaussianRandomWalk.adapt_step_size","level":4,"title":"<code>adapt_step_size(acceptance_rate: float, target_rate: float = 0.234) -&gt; Self</code>","text":"<p>Adapt step size based on acceptance rate.</p> <p>Parameters:</p> Name Type Description Default <code>acceptance_rate</code> <code>float</code> <p>The current acceptance rate.</p> required <code>target_rate</code> <code>float</code> <p>The target acceptance rate (default: 0.234 for RWM).</p> <code>0.234</code> <p>Returns:</p> Name Type Description <code>GaussianRandomWalk</code> <code>Self</code> <p>A new instance with updated step_size.</p>","path":["Reference","flowMC","resource","kernel","Gaussian random walk"],"tags":[]},{"location":"api/flowMC/resource/kernel/Gaussian_random_walk/#flowMC.resource.kernel.Gaussian_random_walk.GaussianRandomWalk.apply_per_dim_scaling","level":4,"title":"<code>apply_per_dim_scaling(ratios: Float[Array, ' n_dim']) -&gt; Self</code>","text":"<p>Multiply step_size element-wise by ratios (returns a new GaussianRandomWalk).</p> <p>Parameters:</p> Name Type Description Default <code>ratios</code> <code>Float[Array, ' n_dim']</code> <p>Per-dimension multiplicative scaling factors, shape (n_dim,).</p> required <p>Returns:</p> Name Type Description <code>GaussianRandomWalk</code> <code>Self</code> <p>A new instance with updated step_size.</p>","path":["Reference","flowMC","resource","kernel","Gaussian random walk"],"tags":[]},{"location":"api/flowMC/resource/kernel/Gaussian_random_walk/#flowMC.resource.kernel.Gaussian_random_walk.GaussianRandomWalk.get_effective_dim_profile","level":4,"title":"<code>get_effective_dim_profile() -&gt; Float[Array, ' n_dim']</code>","text":"<p>Return the current per-dim step size profile normalised to geometric mean 1.</p>","path":["Reference","flowMC","resource","kernel","Gaussian random walk"],"tags":[]},{"location":"api/flowMC/resource/kernel/Gaussian_random_walk/#flowMC.resource.kernel.Gaussian_random_walk.GaussianRandomWalk.kernel","level":4,"title":"<code>kernel(rng_key: Key, position: Float[Array, ' n_dim'], log_prob: Float[Array, 1], logpdf: LogPDF | Callable[[Float[Array, ' n_dim'], PyTree], Float[Array, 1]], data: PyTree) -&gt; tuple[Float[Array, ' n_dim'], Float[Array, 1], Int[Array, 1]]</code>","text":"<p>Random walk gaussian kernel. This is a kernel that only evolve a single chain.</p> <p>Parameters:</p> Name Type Description Default <code>rng_key</code> <code>Key</code> <p>JAX PRNGKey</p> required <code>position</code> <code>Float[Array, n_dim]</code> <p>current position of the chain</p> required <code>log_prob</code> <code>Float[Array, 1]</code> <p>current log-probability of the chain</p> required <code>data</code> <code>PyTree</code> <p>data to be passed to the logpdf function</p> required <p>Returns:</p> Name Type Description <code>position</code> <code>Float[Array, n_dim]</code> <p>new position of the chain</p> <code>log_prob</code> <code>Float[Array, 1]</code> <p>new log-probability of the chain</p> <code>do_accept</code> <code>Int[Array, 1]</code> <p>whether the new position is accepted</p>","path":["Reference","flowMC","resource","kernel","Gaussian random walk"],"tags":[]},{"location":"api/flowMC/resource/kernel/HMC/","level":1,"title":"HMC","text":"","path":["Reference","flowMC","resource","kernel","HMC"],"tags":[]},{"location":"api/flowMC/resource/kernel/HMC/#flowMC.resource.kernel.HMC.HMC","level":3,"title":"<code>HMC</code>","text":"<p>               Bases: <code>ProposalBase</code></p> <p>Hamiltonian Monte Carlo sampler class builiding the hmc_sampler method from target logpdf.</p> <p>Parameters:</p> Name Type Description Default <code>condition_matrix</code> <code>Array</code> <p>Diagonal elements of the mass matrix.</p> required <code>step_size</code> <code>Float</code> <p>Step size for leapfrog integration.</p> <code>0.1</code> <code>n_leapfrog</code> <code>Int</code> <p>Number of leapfrog steps.</p> <code>10</code> <code>periodic_mask</code> <code>Array</code> <p>Boolean mask for periodic dimensions.</p> <code>None</code> <code>periodic_bounds</code> <code>Array</code> <p>Bounds for periodic dimensions.</p> <code>None</code> <p>Methods:</p> Name Description <code>__init__</code> <p>Initialize HMC sampler.</p> <code>adapt_step_size</code> <p>Adapt step size based on acceptance rate.</p> <code>apply_per_dim_scaling</code> <p>Scale the effective per-dimension step size by ratios via the mass matrix.</p> <code>get_effective_dim_profile</code> <p>Return the current per-dim effective step profile normalised to geometric mean 1.</p> <code>get_initial_hamiltonian</code> <p>Compute the value of the Hamiltonian from positions with initial momentum</p> <code>kernel</code> <p>Note that since the potential function is the negative log likelihood,</p>","path":["Reference","flowMC","resource","kernel","HMC"],"tags":[]},{"location":"api/flowMC/resource/kernel/HMC/#flowMC.resource.kernel.HMC.HMC.__init__","level":4,"title":"<code>__init__(condition_matrix: Float[Array, ' n_dim'], step_size: Float = 0.1, n_leapfrog: Int = 10, periodic_mask: Optional[Bool[Array, ' n_dim']] = None, periodic_bounds: Optional[Float[Array, 'n_dim 2']] = None)</code>","text":"<p>Initialize HMC sampler.</p> <p>Parameters:</p> Name Type Description Default <code>condition_matrix</code> <code>Float[Array, ' n_dim']</code> <p>Diagonal elements of the mass matrix as a 1D array.</p> required <code>step_size</code> <code>Float</code> <p>Step size for leapfrog integration.</p> <code>0.1</code> <code>n_leapfrog</code> <code>Int</code> <p>Number of leapfrog steps.</p> <code>10</code> <code>periodic_mask</code> <code>Optional[Bool[Array, ' n_dim']]</code> <p>Boolean mask indicating which dimensions are periodic. If None, no periodic boundaries are applied.</p> <code>None</code> <code>periodic_bounds</code> <code>Optional[Float[Array, 'n_dim 2']]</code> <p>Array of shape (n_dim, 2) with [lower, upper] bounds for each periodic dimension. Only used where periodic_mask is True. If None, no periodic boundaries are applied.</p> <code>None</code>","path":["Reference","flowMC","resource","kernel","HMC"],"tags":[]},{"location":"api/flowMC/resource/kernel/HMC/#flowMC.resource.kernel.HMC.HMC.adapt_step_size","level":4,"title":"<code>adapt_step_size(acceptance_rate: float, target_rate: float = 0.65) -&gt; Self</code>","text":"<p>Adapt step size based on acceptance rate.</p> <p>Parameters:</p> Name Type Description Default <code>acceptance_rate</code> <code>float</code> <p>The current acceptance rate.</p> required <code>target_rate</code> <code>float</code> <p>The target acceptance rate (default: 0.65 for HMC).</p> <code>0.65</code> <p>Returns:</p> Name Type Description <code>HMC</code> <code>Self</code> <p>A new instance with updated step_size.</p>","path":["Reference","flowMC","resource","kernel","HMC"],"tags":[]},{"location":"api/flowMC/resource/kernel/HMC/#flowMC.resource.kernel.HMC.HMC.apply_per_dim_scaling","level":4,"title":"<code>apply_per_dim_scaling(ratios: Float[Array, ' n_dim']) -&gt; Self</code>","text":"<p>Scale the effective per-dimension step size by ratios via the mass matrix.</p> <p>The effective step size in dimension i is step_size / sqrt(condition_matrix[i]). Multiplying that by ratios[i] is equivalent to dividing condition_matrix[i] by ratios[i]^2. The geometric mean of condition_matrix is preserved, so adapt_step_size retains control of the global scale.</p> <p>Parameters:</p> Name Type Description Default <code>ratios</code> <code>Float[Array, ' n_dim']</code> <p>Per-dimension scaling factors for the effective step size, shape (n_dim,). Values &gt; 1 increase the effective step in that dimension.</p> required <p>Returns:</p> Name Type Description <code>HMC</code> <code>Self</code> <p>A new instance with updated condition_matrix.</p>","path":["Reference","flowMC","resource","kernel","HMC"],"tags":[]},{"location":"api/flowMC/resource/kernel/HMC/#flowMC.resource.kernel.HMC.HMC.get_effective_dim_profile","level":4,"title":"<code>get_effective_dim_profile() -&gt; Float[Array, ' n_dim']</code>","text":"<p>Return the current per-dim effective step profile normalised to geometric mean 1.</p> <p>The effective step size in dimension i is step_size / sqrt(condition_matrix[i]), so the per-dim profile is 1/sqrt(condition_matrix[i]) normalised to geomean 1.</p>","path":["Reference","flowMC","resource","kernel","HMC"],"tags":[]},{"location":"api/flowMC/resource/kernel/HMC/#flowMC.resource.kernel.HMC.HMC.get_initial_hamiltonian","level":4,"title":"<code>get_initial_hamiltonian(potential: Callable[[Float[Array, ' n_dim'], PyTree], Float[Array, 1]], kinetic: Callable[[Float[Array, ' n_dim'], Float[Array, ' n_dim']], Float[Array, 1]], rng_key: Key, position: Float[Array, ' n_dim'], data: PyTree)</code>","text":"<p>Compute the value of the Hamiltonian from positions with initial momentum draw at random from the standard normal distribution.</p>","path":["Reference","flowMC","resource","kernel","HMC"],"tags":[]},{"location":"api/flowMC/resource/kernel/HMC/#flowMC.resource.kernel.HMC.HMC.kernel","level":4,"title":"<code>kernel(rng_key: Key, position: Float[Array, ' n_dim'], log_prob: Float[Array, 1], logpdf: LogPDF | Callable[[Float[Array, ' n_dim'], PyTree], Float[Array, 1]], data: PyTree) -&gt; tuple[Float[Array, ' n_dim'], Float[Array, 1], Int[Array, 1]]</code>","text":"<p>Note that since the potential function is the negative log likelihood, hamiltonian is going down, but the likelihood value should go up.</p> <p>Parameters:</p> Name Type Description Default <code>rng_key</code> <code>Key</code> <p>Random key.</p> required <code>position</code> <code>Array</code> <p>Current position.</p> required <code>log_prob</code> <code>Array</code> <p>Log probability of the current position.</p> required <code>logpdf</code> <code>LogPDF | Callable</code> <p>Log probability density function.</p> required <code>data</code> <code>PyTree</code> <p>Additional data for the logpdf.</p> required <p>Returns:</p> Name Type Description <code>tuple</code> <code>tuple[Float[Array, ' n_dim'], Float[Array, 1], Int[Array, 1]]</code> <p>(position, log_prob, do_accept).</p>","path":["Reference","flowMC","resource","kernel","HMC"],"tags":[]},{"location":"api/flowMC/resource/kernel/MALA/","level":1,"title":"MALA","text":"","path":["Reference","flowMC","resource","kernel","MALA"],"tags":[]},{"location":"api/flowMC/resource/kernel/MALA/#flowMC.resource.kernel.MALA.MALA","level":3,"title":"<code>MALA</code>","text":"<p>               Bases: <code>ProposalBase</code></p> <p>Metropolis-adjusted Langevin algorithm sampler class.</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Initialize MALA sampler.</p> <code>adapt_step_size</code> <p>Adapt step size based on acceptance rate.</p> <code>apply_per_dim_scaling</code> <p>Multiply step_size element-wise by ratios (returns a new MALA).</p> <code>get_effective_dim_profile</code> <p>Return the current per-dim step size profile normalised to geometric mean 1.</p> <code>kernel</code> <p>Metropolis-adjusted Langevin algorithm kernel for a single chain.</p>","path":["Reference","flowMC","resource","kernel","MALA"],"tags":[]},{"location":"api/flowMC/resource/kernel/MALA/#flowMC.resource.kernel.MALA.MALA.__init__","level":4,"title":"<code>__init__(step_size: Float[Array, ' n_dim'], periodic_mask: Optional[Bool[Array, ' n_dim']] = None, periodic_bounds: Optional[Float[Array, 'n_dim 2']] = None)</code>","text":"<p>Initialize MALA sampler.</p> <p>Parameters:</p> Name Type Description Default <code>step_size</code> <code>Float[Array, ' n_dim']</code> <p>Step size for the MALA sampler as a 1D array representing       diagonal elements of the step size matrix.</p> required <code>periodic_mask</code> <code>Optional[Bool[Array, ' n_dim']]</code> <p>Boolean mask indicating which dimensions are periodic. If None, no periodic boundaries are applied.</p> <code>None</code> <code>periodic_bounds</code> <code>Optional[Float[Array, 'n_dim 2']]</code> <p>Array of shape (n_dim, 2) with [lower, upper] bounds for each periodic dimension. Only used where periodic_mask is True. If None, no periodic boundaries are applied.</p> <code>None</code>","path":["Reference","flowMC","resource","kernel","MALA"],"tags":[]},{"location":"api/flowMC/resource/kernel/MALA/#flowMC.resource.kernel.MALA.MALA.adapt_step_size","level":4,"title":"<code>adapt_step_size(acceptance_rate: float, target_rate: float = 0.574) -&gt; Self</code>","text":"<p>Adapt step size based on acceptance rate.</p> <p>Parameters:</p> Name Type Description Default <code>acceptance_rate</code> <code>float</code> <p>The current acceptance rate.</p> required <code>target_rate</code> <code>float</code> <p>The target acceptance rate (default: 0.574 for MALA).</p> <code>0.574</code> <p>Returns:</p> Name Type Description <code>MALA</code> <code>Self</code> <p>A new instance with updated step_size.</p>","path":["Reference","flowMC","resource","kernel","MALA"],"tags":[]},{"location":"api/flowMC/resource/kernel/MALA/#flowMC.resource.kernel.MALA.MALA.apply_per_dim_scaling","level":4,"title":"<code>apply_per_dim_scaling(ratios: Float[Array, ' n_dim']) -&gt; Self</code>","text":"<p>Multiply step_size element-wise by ratios (returns a new MALA).</p> <p>Parameters:</p> Name Type Description Default <code>ratios</code> <code>Float[Array, ' n_dim']</code> <p>Per-dimension multiplicative scaling factors, shape (n_dim,).</p> required <p>Returns:</p> Name Type Description <code>MALA</code> <code>Self</code> <p>A new instance with updated step_size.</p>","path":["Reference","flowMC","resource","kernel","MALA"],"tags":[]},{"location":"api/flowMC/resource/kernel/MALA/#flowMC.resource.kernel.MALA.MALA.get_effective_dim_profile","level":4,"title":"<code>get_effective_dim_profile() -&gt; Float[Array, ' n_dim']</code>","text":"<p>Return the current per-dim step size profile normalised to geometric mean 1.</p>","path":["Reference","flowMC","resource","kernel","MALA"],"tags":[]},{"location":"api/flowMC/resource/kernel/MALA/#flowMC.resource.kernel.MALA.MALA.kernel","level":4,"title":"<code>kernel(rng_key: Key, position: Float[Array, ' n_dim'], log_prob: Float[Array, 1], logpdf: LogPDF | Callable[[Float[Array, ' n_dim'], PyTree], Float[Array, 1]], data: PyTree) -&gt; tuple[Float[Array, ' n_dim'], Float[Array, 1], Int[Array, 1]]</code>","text":"<p>Metropolis-adjusted Langevin algorithm kernel for a single chain.</p> <p>Parameters:</p> Name Type Description Default <code>rng_key</code> <code>Key</code> <p>JAX PRNGKey for stochastic operations.</p> required <code>position</code> <code>Float[Array, n_dim]</code> <p>Current position of the chain.</p> required <code>log_prob</code> <code>Float[Array, 1]</code> <p>Current log-probability of the chain.</p> required <code>logpdf</code> <code>LogPDF | Callable[[Float[Array, ' n_dim'], PyTree], Float[Array, 1]]</code> <p>Log probability density function to evaluate.</p> required <code>data</code> <code>PyTree</code> <p>Additional data to pass to the logpdf function.</p> required <p>Returns:     Tuple of (new_position, new_log_prob, acceptance_flag):     - new_position: New position of the chain.     - new_log_prob: New log-probability of the chain.     - acceptance_flag: Whether the new position is accepted.</p>","path":["Reference","flowMC","resource","kernel","MALA"],"tags":[]},{"location":"api/flowMC/resource/kernel/NF_proposal/","level":1,"title":"NF proposal","text":"","path":["Reference","flowMC","resource","kernel","NF proposal"],"tags":[]},{"location":"api/flowMC/resource/kernel/NF_proposal/#flowMC.resource.kernel.NF_proposal.NFProposal","level":3,"title":"<code>NFProposal</code>","text":"<p>               Bases: <code>ProposalBase</code></p> <p>Normalizing-flow global proposal kernel.</p> <p>Proposes new positions by sampling from a trained normalizing flow and accepts or rejects via a corrected Metropolis-Hastings ratio that accounts for the flow density.  All <code>n_steps</code> proposals are drawn in one batched call, so this kernel is suited for use with <code>flowMC.strategy.take_steps.TakeGroupSteps</code>.</p> <p>Attributes:</p> Name Type Description <code>model</code> <code>NFModel</code> <p>Trained normalizing flow model used to propose samples.</p> <code>n_batch_size</code> <code>int</code> <p>Maximum number of proposals evaluated in a single <code>jax.lax.map</code> batch (trades memory for throughput).</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Args:</p> <code>kernel</code> <p>Generate and accept/reject <code>n_steps</code> flow proposals for a single chain.</p> <code>sample_flow</code> <p>Sample positions and their flow log-densities.</p>","path":["Reference","flowMC","resource","kernel","NF proposal"],"tags":[]},{"location":"api/flowMC/resource/kernel/NF_proposal/#flowMC.resource.kernel.NF_proposal.NFProposal.__init__","level":4,"title":"<code>__init__(model: NFModel, n_NFproposal_batch_size: int = 100) -&gt; None</code>","text":"<p>Parameters:</p> Name Type Description Default <code>model</code> <code>NFModel</code> <p>Normalizing flow model used to generate proposals.</p> required <code>n_NFproposal_batch_size</code> <code>int</code> <p>Number of log-prob evaluations per <code>jax.lax.map</code> batch. Defaults to 100.</p> <code>100</code>","path":["Reference","flowMC","resource","kernel","NF proposal"],"tags":[]},{"location":"api/flowMC/resource/kernel/NF_proposal/#flowMC.resource.kernel.NF_proposal.NFProposal.kernel","level":4,"title":"<code>kernel(rng_key: Key, position: Float[Array, ' n_dim'], log_prob: Float[Array, 1], logpdf: LogPDF | Callable[[Float[Array, ' n_dim'], PyTree], Float[Array, 1]], data: PyTree) -&gt; tuple[Float[Array, 'n_step n_dim'], Float[Array, 'n_step 1'], Int[Array, 'n_step 1']]</code>","text":"<p>Generate and accept/reject <code>n_steps</code> flow proposals for a single chain.</p> <p>The number of steps is read from <code>data[\"n_steps\"]</code>.  All proposals are sampled from the flow at once and then evaluated serially via <code>jax.lax.scan</code> for the MH accept/reject step.</p> <p>Parameters:</p> Name Type Description Default <code>rng_key</code> <code>Key</code> <p>JAX PRNGKey.</p> required <code>position</code> <code>Float[Array, n_dim]</code> <p>Current chain position.</p> required <code>log_prob</code> <code>Float[Array, 1]</code> <p>Current log-probability.</p> required <code>logpdf</code> <code>LogPDF | Callable</code> <p>Target log-PDF.</p> required <code>data</code> <code>PyTree</code> <p>Auxiliary data; must contain <code>\"n_steps\"</code> (int).</p> required <p>Returns:</p> Name Type Description <code>tuple</code> <code>tuple[Float[Array, 'n_step n_dim'], Float[Array, 'n_step 1'], Int[Array, 'n_step 1']]</code> <ul> <li>positions (Float[Array, \"n_step n_dim\"]): Chain positions after each step.</li> <li>log_probs (Float[Array, \"n_step 1\"]): Log-probs after each step.</li> <li>do_accepts (Int[Array, \"n_step 1\"]): Acceptance flags.</li> </ul>","path":["Reference","flowMC","resource","kernel","NF proposal"],"tags":[]},{"location":"api/flowMC/resource/kernel/NF_proposal/#flowMC.resource.kernel.NF_proposal.NFProposal.sample_flow","level":4,"title":"<code>sample_flow(rng_key: Key, n_steps: int) -&gt; tuple[Float[Array, 'n_steps n_dim'], Float[Array, ' n_steps']]</code>","text":"<p>Sample positions and their flow log-densities.</p> <p>Parameters:</p> Name Type Description Default <code>rng_key</code> <code>Key</code> <p>JAX PRNGKey.</p> required <code>n_steps</code> <code>int</code> <p>Number of samples to draw from the flow.</p> required <p>Returns:</p> Name Type Description <code>tuple</code> <code>tuple[Float[Array, 'n_steps n_dim'], Float[Array, ' n_steps']]</code> <ul> <li>proposal_position (Float[Array, \"n_steps n_dim\"]): Proposed positions.</li> <li>proposed_log_prob (Float[Array, \"n_steps\"]): Flow log-density at each proposal.</li> </ul>","path":["Reference","flowMC","resource","kernel","NF proposal"],"tags":[]},{"location":"api/flowMC/resource/kernel/base/","level":1,"title":"Base","text":"","path":["Reference","flowMC","resource","kernel","Base"],"tags":[]},{"location":"api/flowMC/resource/kernel/base/#flowMC.resource.kernel.base.ProposalBase","level":3,"title":"<code>ProposalBase</code>","text":"<p>               Bases: <code>Module</code>, <code>Resource</code></p> <p>Methods:</p> Name Description <code>__init__</code> <p>Initialize the sampler class.</p> <code>adapt_step_size</code> <p>Adapt the step size based on acceptance rate.</p> <code>kernel</code> <p>Kernel for one step in the proposal cycle.</p>","path":["Reference","flowMC","resource","kernel","Base"],"tags":[]},{"location":"api/flowMC/resource/kernel/base/#flowMC.resource.kernel.base.ProposalBase.__init__","level":4,"title":"<code>__init__()</code>  <small><code>abstractmethod</code></small>","text":"<p>Initialize the sampler class.</p>","path":["Reference","flowMC","resource","kernel","Base"],"tags":[]},{"location":"api/flowMC/resource/kernel/base/#flowMC.resource.kernel.base.ProposalBase.adapt_step_size","level":4,"title":"<code>adapt_step_size(acceptance_rate: float, target_rate: float) -&gt; Self</code>","text":"<p>Adapt the step size based on acceptance rate.</p> <p>This method should be implemented by kernels that support step size adaptation. If not implemented, the default behavior is to return self unchanged.</p> <p>Parameters:</p> Name Type Description Default <code>acceptance_rate</code> <code>float</code> <p>The observed acceptance rate.</p> required <code>target_rate</code> <code>float</code> <p>The target acceptance rate.</p> required <p>Returns:</p> Type Description <code>Self</code> <p>A new instance of the kernel with adapted step size.</p>","path":["Reference","flowMC","resource","kernel","Base"],"tags":[]},{"location":"api/flowMC/resource/kernel/base/#flowMC.resource.kernel.base.ProposalBase.kernel","level":4,"title":"<code>kernel(rng_key: Key, position: Float[Array, 'nstep n_dim'], log_prob: Float[Array, 'nstep 1'], logpdf: LogPDF | Callable[[Float[Array, ' n_dim'], PyTree], Float[Array, 1]], data: PyTree) -&gt; tuple[Float[Array, 'nstep n_dim'], Float[Array, 'nstep 1'], Int[Array, 'n_step 1']]</code>  <small><code>abstractmethod</code></small>","text":"<p>Kernel for one step in the proposal cycle.</p>","path":["Reference","flowMC","resource","kernel","Base"],"tags":[]},{"location":"api/flowMC/resource/model/common/","level":1,"title":"Common","text":"","path":["Reference","flowMC","resource","model","Common"],"tags":[]},{"location":"api/flowMC/resource/model/common/#flowMC.resource.model.common.Bijection","level":3,"title":"<code>Bijection</code>","text":"<p>               Bases: <code>Module</code></p> <p>Base class for bijective transformations.</p> <p>Subclasses must implement <code>forward</code> and <code>inverse</code>. The default <code>__call__</code> delegates to <code>forward</code>.</p> <p>This is an abstract template that should not be directly used.</p> <p>Methods:</p> Name Description <code>__call__</code> <p>Apply the forward transformation.</p> <code>forward</code> <p>Transform from input space to output space.</p> <code>inverse</code> <p>Transform from output space back to input space.</p>","path":["Reference","flowMC","resource","model","Common"],"tags":[]},{"location":"api/flowMC/resource/model/common/#flowMC.resource.model.common.Bijection.__call__","level":4,"title":"<code>__call__(x: Float[Array, ' n_dim'], condition: Float[Array, ' n_condition']) -&gt; tuple[Float[Array, ' n_dim'], Float]</code>","text":"<p>Apply the forward transformation.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Float[Array, n_dim]</code> <p>Input array.</p> required <code>condition</code> <code>Float[Array, n_condition]</code> <p>Conditioning variables.</p> required <p>Returns:</p> Type Description <code>tuple[Float[Array, ' n_dim'], Float]</code> <p>tuple[Float[Array, \"n_dim\"], Float]: Transformed output and log-det Jacobian.</p>","path":["Reference","flowMC","resource","model","Common"],"tags":[]},{"location":"api/flowMC/resource/model/common/#flowMC.resource.model.common.Bijection.forward","level":4,"title":"<code>forward(x: Float[Array, ' n_dim'], condition: Float[Array, ' n_condition']) -&gt; tuple[Float[Array, ' n_dim'], Float]</code>  <small><code>abstractmethod</code></small>","text":"<p>Transform from input space to output space.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Float[Array, n_dim]</code> <p>Input array.</p> required <code>condition</code> <code>Float[Array, n_condition]</code> <p>Conditioning variables.</p> required <p>Returns:</p> Type Description <code>tuple[Float[Array, ' n_dim'], Float]</code> <p>tuple[Float[Array, \"n_dim\"], Float]: Transformed output and log-det Jacobian.</p>","path":["Reference","flowMC","resource","model","Common"],"tags":[]},{"location":"api/flowMC/resource/model/common/#flowMC.resource.model.common.Bijection.inverse","level":4,"title":"<code>inverse(x: Float[Array, ' n_dim'], condition: Float[Array, ' n_condition']) -&gt; tuple[Float[Array, ' n_dim'], Float]</code>  <small><code>abstractmethod</code></small>","text":"<p>Transform from output space back to input space.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Float[Array, n_dim]</code> <p>Array in the output (transformed) space.</p> required <code>condition</code> <code>Float[Array, n_condition]</code> <p>Conditioning variables.</p> required <p>Returns:</p> Type Description <code>tuple[Float[Array, ' n_dim'], Float]</code> <p>tuple[Float[Array, \"n_dim\"], Float]: Inverse output and log-det Jacobian.</p>","path":["Reference","flowMC","resource","model","Common"],"tags":[]},{"location":"api/flowMC/resource/model/common/#flowMC.resource.model.common.Distribution","level":3,"title":"<code>Distribution</code>","text":"<p>               Bases: <code>Module</code></p> <p>Base class for probability distributions.</p> <p>Subclasses must implement <code>log_prob</code> and <code>sample</code>. The default <code>__call__</code> delegates to <code>log_prob</code>.</p> <p>This is an abstract template that should not be directly used.</p> <p>Methods:</p> Name Description <code>__call__</code> <p>Evaluate the log-probability of <code>x</code>.</p>","path":["Reference","flowMC","resource","model","Common"],"tags":[]},{"location":"api/flowMC/resource/model/common/#flowMC.resource.model.common.Distribution.__call__","level":4,"title":"<code>__call__(x: Array, key: Optional[Key] = None) -&gt; Array</code>","text":"<p>Evaluate the log-probability of <code>x</code>.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Array</code> <p>Input sample.</p> required <code>key</code> <code>Key</code> <p>Unused; reserved for subclass compatibility.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>Array</code> <code>Array</code> <p>Log-probability of <code>x</code>.</p>","path":["Reference","flowMC","resource","model","Common"],"tags":[]},{"location":"api/flowMC/resource/model/common/#flowMC.resource.model.common.Gaussian","level":3,"title":"<code>Gaussian</code>","text":"<p>               Bases: <code>Distribution</code></p> <p>Multivariate Gaussian distribution.</p> <p>Parameters:</p> Name Type Description Default <code>mean</code> <code>Array</code> <p>Mean.</p> required <code>cov</code> <code>Array</code> <p>Covariance matrix.</p> required <code>learnable</code> <code>bool</code> <p>Whether the mean and covariance matrix are learnable parameters.</p> <code>False</code> <p>Attributes:</p> Name Type Description <code>mean</code> <code>Array</code> <p>Mean.</p> <code>cov</code> <code>Array</code> <p>Covariance matrix.</p>","path":["Reference","flowMC","resource","model","Common"],"tags":[]},{"location":"api/flowMC/resource/model/common/#flowMC.resource.model.common.MLP","level":3,"title":"<code>MLP</code>","text":"<p>               Bases: <code>Module</code></p> <p>Multilayer perceptron.</p> <p>Parameters:</p> Name Type Description Default <code>shape</code> <code>List[int]</code> <p>Shape of the MLP. The first element is the input dimension, the last element is the output dimension.</p> required <code>key</code> <code>Key</code> <p>Random key.</p> required <p>Attributes:</p> Name Type Description <code>layers</code> <code>List</code> <p>List of layers.</p> <code>activation</code> <code>Callable</code> <p>Activation function.</p> <code>use_bias</code> <code>bool</code> <p>Whether to use bias.</p>","path":["Reference","flowMC","resource","model","Common"],"tags":[]},{"location":"api/flowMC/resource/model/common/#flowMC.resource.model.common.MaskedCouplingLayer","level":3,"title":"<code>MaskedCouplingLayer</code>","text":"<p>               Bases: <code>Bijection</code></p> <p>Masked coupling layer.</p> <p>f(x) = (1-m)b(x;c(mx;z)) + m*x where b is the inner bijector, m is the mask, and c is the conditioner.</p> <p>Parameters:</p> Name Type Description Default <code>bijector</code> <code>Bijection</code> <p>inner bijector in the masked coupling layer.</p> required <code>mask</code> <code>Array</code> <p>Mask. 0 for the input variables that are transformed, 1 for the input variables that are not transformed.</p> required","path":["Reference","flowMC","resource","model","Common"],"tags":[]},{"location":"api/flowMC/resource/model/flowmatching/base/","level":1,"title":"Base","text":"","path":["Reference","flowMC","resource","model","flowmatching","Base"],"tags":[]},{"location":"api/flowMC/resource/model/flowmatching/base/#flowMC.resource.model.flowmatching.base.CondOTScheduler","level":3,"title":"<code>CondOTScheduler</code>","text":"<p>               Bases: <code>Scheduler</code></p> <p>Conditional Optimal Transport Scheduler.</p> <p>Methods:</p> Name Description <code>__call__</code> <p>Return the parameters of the scheduler at time t.</p>","path":["Reference","flowMC","resource","model","flowmatching","Base"],"tags":[]},{"location":"api/flowMC/resource/model/flowmatching/base/#flowMC.resource.model.flowmatching.base.CondOTScheduler.__call__","level":4,"title":"<code>__call__(t: Float) -&gt; tuple[Float, Float, Float, Float]</code>","text":"<p>Return the parameters of the scheduler at time t.</p>","path":["Reference","flowMC","resource","model","flowmatching","Base"],"tags":[]},{"location":"api/flowMC/resource/model/flowmatching/base/#flowMC.resource.model.flowmatching.base.FlowMatchingModel","level":3,"title":"<code>FlowMatchingModel</code>","text":"<p>               Bases: <code>Module</code>, <code>Resource</code></p> <p>Methods:</p> Name Description <code>train</code> <p>Train a flow matching model.</p> <code>train_epoch</code> <p>Train for a single epoch.</p>","path":["Reference","flowMC","resource","model","flowmatching","Base"],"tags":[]},{"location":"api/flowMC/resource/model/flowmatching/base/#flowMC.resource.model.flowmatching.base.FlowMatchingModel.train","level":4,"title":"<code>train(rng: Key, data: tuple[Float[Array, 'n_example n_dim'], Float[Array, 'n_example n_dim'], Float[Array, 'n_example 1']], optim: optax.GradientTransformation, state: optax.OptState, num_epochs: int, batch_size: int, verbose: bool = True) -&gt; tuple[Key, Self, optax.OptState, Array]</code>","text":"<p>Train a flow matching model.</p> <p>Parameters:</p> Name Type Description Default <code>rng</code> <code>Key</code> <p>JAX PRNGKey.</p> required <code>data</code> <code>tuple</code> <p>Training data as (x0, x1, t) batches.</p> required <code>optim</code> <code>GradientTransformation</code> <p>Optimizer.</p> required <code>state</code> <code>OptState</code> <p>Optimizer state.</p> required <code>num_epochs</code> <code>int</code> <p>Number of epochs to train for.</p> required <code>batch_size</code> <code>int</code> <p>Batch size.</p> required <code>verbose</code> <code>bool</code> <p>Whether to print progress.</p> <code>True</code> <p>Returns:</p> Name Type Description <code>rng</code> <code>Key</code> <p>Updated JAX PRNGKey.</p> <code>model</code> <code>Self</code> <p>Best model found during training.</p> <code>state</code> <code>OptState</code> <p>Optimizer state corresponding to the best model.</p> <code>loss_values</code> <code>Array</code> <p>Loss values for each epoch.</p>","path":["Reference","flowMC","resource","model","flowmatching","Base"],"tags":[]},{"location":"api/flowMC/resource/model/flowmatching/base/#flowMC.resource.model.flowmatching.base.FlowMatchingModel.train_epoch","level":4,"title":"<code>train_epoch(rng: Key, optim: optax.GradientTransformation, state: optax.OptState, data: tuple[Float[Array, 'n_example n_dim'], Float[Array, 'n_example n_dim'], Float[Array, 'n_example 1']], batch_size: Float) -&gt; tuple[Float, Self, optax.OptState]</code>","text":"<p>Train for a single epoch.</p>","path":["Reference","flowMC","resource","model","flowmatching","Base"],"tags":[]},{"location":"api/flowMC/resource/model/flowmatching/base/#flowMC.resource.model.flowmatching.base.Path","level":3,"title":"<code>Path</code>","text":"<p>Methods:</p> Name Description <code>sample</code> <p>Sample a point along the path between x0 and x1 at time t.</p>","path":["Reference","flowMC","resource","model","flowmatching","Base"],"tags":[]},{"location":"api/flowMC/resource/model/flowmatching/base/#flowMC.resource.model.flowmatching.base.Path.sample","level":4,"title":"<code>sample(x0: Float, x1: Float, t: Float) -&gt; Float</code>","text":"<p>Sample a point along the path between x0 and x1 at time t.</p>","path":["Reference","flowMC","resource","model","flowmatching","Base"],"tags":[]},{"location":"api/flowMC/resource/model/flowmatching/base/#flowMC.resource.model.flowmatching.base.Scheduler","level":3,"title":"<code>Scheduler</code>","text":"<p>Methods:</p> Name Description <code>__call__</code> <p>Return the parameters of the scheduler at time t.</p>","path":["Reference","flowMC","resource","model","flowmatching","Base"],"tags":[]},{"location":"api/flowMC/resource/model/flowmatching/base/#flowMC.resource.model.flowmatching.base.Scheduler.__call__","level":4,"title":"<code>__call__(t: Float) -&gt; tuple[Float, Float, Float, Float]</code>","text":"<p>Return the parameters of the scheduler at time t.</p>","path":["Reference","flowMC","resource","model","flowmatching","Base"],"tags":[]},{"location":"api/flowMC/resource/model/flowmatching/base/#flowMC.resource.model.flowmatching.base.Solver","level":3,"title":"<code>Solver</code>","text":"<p>               Bases: <code>Module</code></p> <p>Methods:</p> Name Description <code>log_prob</code> <p>Compute the log probability of the initial condition x1.</p> <code>sample</code> <p>Sample points from the solver.</p>","path":["Reference","flowMC","resource","model","flowmatching","Base"],"tags":[]},{"location":"api/flowMC/resource/model/flowmatching/base/#flowMC.resource.model.flowmatching.base.Solver.log_prob","level":4,"title":"<code>log_prob(x1: Float[Array, ' n_dims'], dt: Float = 0.1) -&gt; Float</code>","text":"<p>Compute the log probability of the initial condition x1. This solves the ODE backward, i.e. from the posterior to the prior.</p>","path":["Reference","flowMC","resource","model","flowmatching","Base"],"tags":[]},{"location":"api/flowMC/resource/model/flowmatching/base/#flowMC.resource.model.flowmatching.base.Solver.sample","level":4,"title":"<code>sample(rng_key: Key, n_samples: int, dt: Float = 0.1) -&gt; Float[Array, 'n_samples n_dims']</code>","text":"<p>Sample points from the solver. This sovles the ODE forward, i.e. from the prior to the posterior.</p>","path":["Reference","flowMC","resource","model","flowmatching","Base"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/base/","level":1,"title":"Base","text":"","path":["Reference","flowMC","resource","model","nf_model","Base"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/base/#flowMC.resource.model.nf_model.base.NFModel","level":3,"title":"<code>NFModel</code>","text":"<p>               Bases: <code>Module</code>, <code>Resource</code></p> <p>Base class for normalizing flow models.</p> <p>This is an abstract template that should not be directly used.</p> <p>Methods:</p> Name Description <code>__call__</code> <p>Forward pass of the model.</p> <code>forward</code> <p>Forward pass of the model.</p> <code>inverse</code> <p>Inverse pass of the model.</p> <code>to_precision</code> <p>Convert all parameters to a given precision.</p> <code>train</code> <p>Train a normalizing flow model.</p> <code>train_epoch</code> <p>Train for a single epoch.</p> <code>train_step</code> <p>Train for a single step.</p>","path":["Reference","flowMC","resource","model","nf_model","Base"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/base/#flowMC.resource.model.nf_model.base.NFModel.__call__","level":4,"title":"<code>__call__(x: Float[Array, ' n_dim']) -&gt; tuple[Float[Array, ' n_dim'], Float]</code>","text":"<p>Forward pass of the model.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Float[Array, n_dim]</code> <p>Input data.</p> required <p>Returns:</p> Type Description <code>tuple[Float[Array, ' n_dim'], Float]</code> <p>tuple[Float[Array, \"n_dim\"], Float]: Output data and log determinant of the Jacobian.</p>","path":["Reference","flowMC","resource","model","nf_model","Base"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/base/#flowMC.resource.model.nf_model.base.NFModel.forward","level":4,"title":"<code>forward(x: Float[Array, ' n_dim'], key: Optional[Key] = None) -&gt; tuple[Float[Array, ' n_dim'], Float]</code>  <small><code>abstractmethod</code></small>","text":"<p>Forward pass of the model.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Float[Array, n_dim]</code> <p>Input data.</p> required <p>Returns:</p> Type Description <code>tuple[Float[Array, ' n_dim'], Float]</code> <p>tuple[Float[Array, \"n_dim\"], Float]: Output data and log determinant of the Jacobian.</p>","path":["Reference","flowMC","resource","model","nf_model","Base"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/base/#flowMC.resource.model.nf_model.base.NFModel.inverse","level":4,"title":"<code>inverse(x: Float[Array, ' n_dim']) -&gt; tuple[Float[Array, ' n_dim'], Float]</code>  <small><code>abstractmethod</code></small>","text":"<p>Inverse pass of the model.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Float[Array, n_dim]</code> <p>Input data.</p> required <p>Returns:</p> Type Description <code>tuple[Float[Array, ' n_dim'], Float]</code> <p>tuple[Float[Array, \"n_dim\"], Float]: Output data and log determinant of the Jacobian.</p>","path":["Reference","flowMC","resource","model","nf_model","Base"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/base/#flowMC.resource.model.nf_model.base.NFModel.to_precision","level":4,"title":"<code>to_precision(precision: str = 'float32') -&gt; Self</code>","text":"<p>Convert all parameters to a given precision.</p> <p>Warning</p> <p>This function is experimental and may change in the future.</p> <p>Parameters:</p> Name Type Description Default <code>precision</code> <code>str</code> <p>Precision to convert to.</p> <code>'float32'</code> <p>Returns:</p> Name Type Description <code>Self</code> <code>Self</code> <p>Model with parameters converted to the given precision.</p>","path":["Reference","flowMC","resource","model","nf_model","Base"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/base/#flowMC.resource.model.nf_model.base.NFModel.train","level":4,"title":"<code>train(rng: Key, data: Array, optim: optax.GradientTransformation, state: optax.OptState, num_epochs: int, batch_size: int, verbose: bool = True) -&gt; tuple[Key, Self, optax.OptState, Array]</code>","text":"<p>Train a normalizing flow model.</p> <p>Parameters:</p> Name Type Description Default <code>rng</code> <code>Key</code> <p>JAX PRNGKey.</p> required <code>data</code> <code>Array</code> <p>Training data.</p> required <code>optim</code> <code>GradientTransformation</code> <p>Optimizer.</p> required <code>state</code> <code>OptState</code> <p>Optimizer state.</p> required <code>num_epochs</code> <code>int</code> <p>Number of epochs to train for.</p> required <code>batch_size</code> <code>int</code> <p>Batch size.</p> required <code>verbose</code> <code>bool</code> <p>Whether to print progress.</p> <code>True</code> <p>Returns:</p> Name Type Description <code>rng</code> <code>Key</code> <p>Updated JAX PRNGKey.</p> <code>model</code> <code>Self</code> <p>Best model found during training.</p> <code>state</code> <code>OptState</code> <p>Optimizer state corresponding to the best model.</p> <code>loss_values</code> <code>Array</code> <p>Loss values for each epoch.</p>","path":["Reference","flowMC","resource","model","nf_model","Base"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/base/#flowMC.resource.model.nf_model.base.NFModel.train_epoch","level":4,"title":"<code>train_epoch(rng: Key, optim: optax.GradientTransformation, state: optax.OptState, data: Float[Array, 'n_example n_dim'], batch_size: Float) -&gt; tuple[Float, Self, optax.OptState]</code>","text":"<p>Train for a single epoch.</p>","path":["Reference","flowMC","resource","model","nf_model","Base"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/base/#flowMC.resource.model.nf_model.base.NFModel.train_step","level":4,"title":"<code>train_step(model: Self, x: Float[Array, 'n_batch n_dim'], optim: optax.GradientTransformation, state: optax.OptState) -&gt; tuple[Float[Array, 1], Self, optax.OptState]</code>","text":"<p>Train for a single step.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Array</code> <p>Training data.</p> required <code>optim</code> <code>GradientTransformation</code> <p>Optimizer.</p> required <code>state</code> <code>OptState</code> <p>Optimizer state.</p> required <p>Returns:</p> Name Type Description <code>tuple</code> <code>tuple[Float[Array, 1], Self, OptState]</code> <p>(loss, model, state).</p>","path":["Reference","flowMC","resource","model","nf_model","Base"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/realNVP/","level":1,"title":"realNVP","text":"","path":["Reference","flowMC","resource","model","nf_model","realNVP"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/realNVP/#flowMC.resource.model.nf_model.realNVP.AffineCoupling","level":3,"title":"<code>AffineCoupling</code>","text":"<p>               Bases: <code>Module</code></p> <p>Affine coupling layer. (Defined in the RealNVP paper https://arxiv.org/abs/1605.08803) We use tanh as the default activation function.</p> <p>Parameters:</p> Name Type Description Default <code>n_features</code> <code>int</code> <p>(int) The number of features in the input.</p> required <code>n_hidden</code> <code>int</code> <p>(int) The number of hidden units in the MLP.</p> required <code>mask</code> <code>Array</code> <p>(ndarray) Alternating mask for the affine coupling layer.</p> required <code>dt</code> <code>Float</code> <p>(Float) Scaling factor for the affine coupling layer.</p> <code>1</code> <p>Methods:</p> Name Description <code>forward</code> <p>From latent space to data space.</p> <code>inverse</code> <p>From data space to latent space.</p>","path":["Reference","flowMC","resource","model","nf_model","realNVP"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/realNVP/#flowMC.resource.model.nf_model.realNVP.AffineCoupling.forward","level":4,"title":"<code>forward(x: Array) -&gt; Tuple[Array, Array]</code>","text":"<p>From latent space to data space.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Array</code> <p>(Array) Latent space.</p> required <p>Returns:</p> Name Type Description <code>outputs</code> <code>Array</code> <p>(Array) Data space.</p> <code>log_det</code> <code>Array</code> <p>(Array) Log determinant of the Jacobian.</p>","path":["Reference","flowMC","resource","model","nf_model","realNVP"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/realNVP/#flowMC.resource.model.nf_model.realNVP.AffineCoupling.inverse","level":4,"title":"<code>inverse(x: Array) -&gt; Tuple[Array, Array]</code>","text":"<p>From data space to latent space.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Array</code> <p>(Array) Data space.</p> required <p>Returns:</p> Name Type Description <code>outputs</code> <code>Array</code> <p>(Array) Latent space.</p> <code>log_det</code> <code>Array</code> <p>(Array) Log determinant of the Jacobian.</p>","path":["Reference","flowMC","resource","model","nf_model","realNVP"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/realNVP/#flowMC.resource.model.nf_model.realNVP.RealNVP","level":3,"title":"<code>RealNVP</code>","text":"<p>               Bases: <code>NFModel</code></p> <p>RealNVP mode defined in the paper https://arxiv.org/abs/1605.08803. MLP is needed to make sure the scaling between layers are more or less the same.</p> <p>Parameters:</p> Name Type Description Default <code>n_features</code> <code>int</code> <p>The number of features in the input.</p> required <code>n_layers</code> <code>int</code> <p>The number of affine coupling layers.</p> required <code>n_hidden</code> <code>int</code> <p>The number of hidden units in the MLP.</p> required Properties <p>data_mean: (ndarray) Mean of Gaussian base distribution data_cov: (ndarray) Covariance of Gaussian base distribution</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Args:</p> <code>inverse</code> <p>From latent space to data space.</p>","path":["Reference","flowMC","resource","model","nf_model","realNVP"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/realNVP/#flowMC.resource.model.nf_model.realNVP.RealNVP.__init__","level":4,"title":"<code>__init__(n_features: int, n_layers: int, n_hidden: int, key: Key, **kwargs) -&gt; None</code>","text":"<p>Parameters:</p> Name Type Description Default <code>n_features</code> <code>int</code> <p>Dimensionality of the data.</p> required <code>n_layers</code> <code>int</code> <p>Number of affine coupling layers.</p> required <code>n_hidden</code> <code>int</code> <p>Number of hidden units per coupling-layer MLP.</p> required <code>key</code> <code>Key</code> <p>JAX PRNGKey for parameter initialisation.</p> required <code>**kwargs</code> <p>Optional overrides: - <code>base_dist</code> (Distribution): Custom base distribution.   Defaults to a standard Gaussian. - <code>data_mean</code> (Array): Initial data mean for whitening.   Updated during training. - <code>data_cov</code> (Array): Initial data covariance for whitening.   Updated during training.</p> <code>{}</code>","path":["Reference","flowMC","resource","model","nf_model","realNVP"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/realNVP/#flowMC.resource.model.nf_model.realNVP.RealNVP.inverse","level":4,"title":"<code>inverse(x: Float[Array, ' n_dim'], condition: Optional[Float[Array, ' n_condition']] = None) -&gt; tuple[Float[Array, ' n_dim'], Float]</code>","text":"<p>From latent space to data space.</p>","path":["Reference","flowMC","resource","model","nf_model","realNVP"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/rqSpline/","level":1,"title":"rqSpline","text":"","path":["Reference","flowMC","resource","model","nf_model","rqSpline"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/rqSpline/#flowMC.resource.model.nf_model.rqSpline.MaskedCouplingRQSpline","level":3,"title":"<code>MaskedCouplingRQSpline</code>","text":"<p>               Bases: <code>NFModel</code></p> <p>Rational quadratic spline normalizing flow model using distrax.</p> <p>Parameters:</p> Name Type Description Default <code>n_features</code> <code>int</code> <p>Number of features in the data.</p> required <code>n_layers</code> <code>int</code> <p>Number of layers in the conditioner.</p> required <code>hidden_size</code> <code>list[int]</code> <p>Hidden size of the conditioner.</p> required <code>num_bins</code> <code>int</code> <p>Number of bins in the spline.</p> required <code>key</code> <code>Key</code> <p>Random key for initialization.</p> required <code>spline_range</code> <code>tuple[float, float]</code> <p>Range of the spline. Defaults to (-10.0, 10.0).</p> <code>(-10.0, 10.0)</code> Properties <p>n_features (int) :  Number of features in the data. data_mean (Array) : Mean of the data. data_cov (Array) : Covariance of the data.</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Args:</p> <code>inverse</code> <p>From latent space to data space.</p> <code>log_prob</code> <p>From data space to latent space.</p>","path":["Reference","flowMC","resource","model","nf_model","rqSpline"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/rqSpline/#flowMC.resource.model.nf_model.rqSpline.MaskedCouplingRQSpline.__init__","level":4,"title":"<code>__init__(n_features: int, n_layers: int, hidden_size: list[int], num_bins: int, key: Key, spline_range: tuple[float, float] = (-10.0, 10.0), **kwargs) -&gt; None</code>","text":"<p>Parameters:</p> Name Type Description Default <code>n_features</code> <code>int</code> <p>Dimensionality of the data.</p> required <code>n_layers</code> <code>int</code> <p>Number of masked coupling layers.</p> required <code>hidden_size</code> <code>list[int]</code> <p>Hidden layer widths for the conditioner MLP.</p> required <code>num_bins</code> <code>int</code> <p>Number of rational-quadratic spline bins.</p> required <code>key</code> <code>Key</code> <p>JAX PRNGKey for parameter initialisation.</p> required <code>spline_range</code> <code>tuple[float, float]</code> <p><code>(min, max)</code> range of the spline. Inputs outside this range are mapped linearly. Defaults to <code>(-10.0, 10.0)</code>.</p> <code>(-10.0, 10.0)</code> <code>**kwargs</code> <p>Optional overrides: - <code>base_dist</code> (Distribution): Custom base distribution.   Defaults to a standard Gaussian. - <code>data_mean</code> (Array): Initial data mean for whitening.   Updated during training. - <code>data_cov</code> (Array): Initial data covariance for whitening.   Updated during training.</p> <code>{}</code>","path":["Reference","flowMC","resource","model","nf_model","rqSpline"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/rqSpline/#flowMC.resource.model.nf_model.rqSpline.MaskedCouplingRQSpline.inverse","level":4,"title":"<code>inverse(x: Float[Array, ' n_dim'], condition: Optional[Float[Array, ' n_condition']] = None) -&gt; tuple[Float[Array, ' n_dim'], Float]</code>","text":"<p>From latent space to data space.</p>","path":["Reference","flowMC","resource","model","nf_model","rqSpline"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/rqSpline/#flowMC.resource.model.nf_model.rqSpline.MaskedCouplingRQSpline.log_prob","level":4,"title":"<code>log_prob(x: Float[Array, 'n_sample n_dim']) -&gt; Float[Array, ' n_sample']</code>","text":"<p>From data space to latent space.</p>","path":["Reference","flowMC","resource","model","nf_model","rqSpline"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/rqSpline/#flowMC.resource.model.nf_model.rqSpline.RQSpline","level":3,"title":"<code>RQSpline</code>","text":"<p>               Bases: <code>Bijection</code></p> <p>Attributes:</p> Name Type Description <code>conditioner</code> <code>MLP</code> <p>A rational-quadratic spline bijection.</p>","path":["Reference","flowMC","resource","model","nf_model","rqSpline"],"tags":[]},{"location":"api/flowMC/resource/model/nf_model/rqSpline/#flowMC.resource.model.nf_model.rqSpline.RQSpline.conditioner","level":4,"title":"<code>conditioner: MLP = conditioner</code>  <small><code>instance-attribute</code></small>","text":"<p>A rational-quadratic spline bijection.</p> <p>This bijection is a piecewise rational-quadratic spline with <code>num_bins</code> bins. The spline is defined by the bin boundaries on the x and y axes, the slopes at the knot points, and the slopes at the boundaries of the spline range. The spline is linear outside the spline range.</p> <p>Parameters:</p> Name Type Description Default <code>conditioner</code> <code>Module</code> <p>A conditioner that takes the input and returns the parameters of the spline.</p> required <code>range_min</code> <code>float</code> <p>The minimum value of the spline range.</p> required <code>range_max</code> <code>float</code> <p>The maximum value of the spline range.</p> required <code>num_bins</code> <code>int</code> <p>The number of bins in the spline.</p> required <code>min_bin_size</code> <code>float</code> <p>The minimum size of a bin.</p> required <code>min_knot_slope</code> <code>float</code> <p>The minimum slope of the spline at the knot points.</p> required <p>Attributes:</p> Name Type Description <code>range_min</code> <code>float</code> <p>The minimum value of the spline range.</p> <code>range_max</code> <code>float</code> <p>The maximum value of the spline range.</p>","path":["Reference","flowMC","resource","model","nf_model","rqSpline"],"tags":[]},{"location":"api/flowMC/resource_strategy_bundle/RQSpline_GRW/","level":1,"title":"RQSpline GRW","text":"","path":["Reference","flowMC","resource_strategy_bundle","RQSpline GRW"],"tags":[]},{"location":"api/flowMC/resource_strategy_bundle/RQSpline_GRW/#flowMC.resource_strategy_bundle.RQSpline_GRW.RQSpline_GRW_Bundle","level":3,"title":"<code>RQSpline_GRW_Bundle</code>","text":"<p>               Bases: <code>ResourceStrategyBundle</code></p> <p>A bundle that uses a Rational Quadratic Spline as a normalizing flow model and Gaussian Random Walk as a local sampler.</p> <p>This is similar to the RQSpline_MALA_Bundle but uses GRW instead of MALA for local sampling. GRW is a simpler sampler that does not use gradients.</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Build all resources and strategies for an RQSpline + GRW sampling run.</p>","path":["Reference","flowMC","resource_strategy_bundle","RQSpline GRW"],"tags":[]},{"location":"api/flowMC/resource_strategy_bundle/RQSpline_GRW/#flowMC.resource_strategy_bundle.RQSpline_GRW.RQSpline_GRW_Bundle.__init__","level":4,"title":"<code>__init__(rng_key: Key, n_chains: int, n_dims: int, logpdf: Callable[[Float[Array, ' n_dim'], dict], Float], n_local_steps: int, n_global_steps: int, n_training_loops: int, n_production_loops: int, n_epochs: int, grw_step_size: Float | Float[Array, ' n_dim'] = 0.1, adapt_step_size: bool = True, adapt_step_size_per_dim: bool = True, periodic: Optional[dict[int, tuple[float, float]]] = None, rq_spline_hidden_units: list[int] = [32, 32], rq_spline_n_bins: int = 8, rq_spline_n_layers: int = 4, n_NFproposal_batch_size: int = 10000, learning_rate: float = 0.001, batch_size: int = 10000, n_max_examples: int = 10000, history_window: int = 100, chain_batch_size: int = 0, local_thinning: int = 1, global_thinning: int = 1, early_stopping: bool = False, early_stopping_tolerance: float = 0.05, early_stopping_patience: int = 3, early_stopping_min_acceptance: float = 0.1, verbose: bool = False) -&gt; None</code>","text":"<p>Build all resources and strategies for an RQSpline + GRW sampling run.</p> <p>Parameters:</p> Name Type Description Default <code>rng_key</code> <code>Key</code> <p>JAX PRNGKey used to initialise the normalizing flow.</p> required <code>n_chains</code> <code>int</code> <p>Number of parallel MCMC chains.</p> required <code>n_dims</code> <code>int</code> <p>Dimensionality of the target distribution.</p> required <code>logpdf</code> <code>Callable</code> <p>Log-PDF <code>f(x, data) -&gt; Float</code>.</p> required <code>n_local_steps</code> <code>int</code> <p>GRW steps per training/production loop iteration.</p> required <code>n_global_steps</code> <code>int</code> <p>NF-proposal steps per training/production loop iteration.</p> required <code>n_training_loops</code> <code>int</code> <p>Number of train-then-sample iterations (warmup).</p> required <code>n_production_loops</code> <code>int</code> <p>Number of production sampling iterations.</p> required <code>n_epochs</code> <code>int</code> <p>NF training epochs per training loop.</p> required <code>grw_step_size</code> <code>float | Float[Array, n_dim]</code> <p>Initial GRW step size; scalar or per-dimension array. Defaults to 0.1.</p> <code>0.1</code> <code>adapt_step_size</code> <code>bool</code> <p>Adapt the GRW step size during training. Defaults to True.</p> <code>True</code> <code>adapt_step_size_per_dim</code> <code>bool</code> <p>Also tune per-dimension step size ratios using the empirical std of recent chain positions. Runs after adapt_step_size. Defaults to True.</p> <code>True</code> <code>periodic</code> <code>dict[int, tuple[float, float]] | None</code> <p>Periodic boundary conditions as <code>{dim_index: (lower, upper)}</code>. Defaults to None.</p> <code>None</code> <code>rq_spline_hidden_units</code> <code>list[int]</code> <p>Hidden units per conditioner MLP layer. Defaults to <code>[32, 32]</code>.</p> <code>[32, 32]</code> <code>rq_spline_n_bins</code> <code>int</code> <p>Number of RQ-spline bins. Defaults to 8.</p> <code>8</code> <code>rq_spline_n_layers</code> <code>int</code> <p>Number of masked coupling layers. Defaults to 4.</p> <code>4</code> <code>n_NFproposal_batch_size</code> <code>int</code> <p>NF log-prob evaluation batch size. Defaults to 10000.</p> <code>10000</code> <code>learning_rate</code> <code>float</code> <p>Adam learning rate for NF training. Defaults to 1e-3.</p> <code>0.001</code> <code>batch_size</code> <code>int</code> <p>Mini-batch size for NF training. Defaults to 10000.</p> <code>10000</code> <code>n_max_examples</code> <code>int</code> <p>Maximum training examples per call. Defaults to 10000.</p> <code>10000</code> <code>history_window</code> <code>int</code> <p>Use only the last <code>history_window</code> stored steps as training data. Defaults to 100.</p> <code>100</code> <code>chain_batch_size</code> <code>int</code> <p>Process chains in sub-batches of this size to reduce peak memory. 0 disables batching. Defaults to 0.</p> <code>0</code> <code>local_thinning</code> <code>int</code> <p>Store every <code>local_thinning</code>-th local step. Defaults to 1.</p> <code>1</code> <code>global_thinning</code> <code>int</code> <p>Store every <code>global_thinning</code>-th global step. Defaults to 1.</p> <code>1</code> <code>early_stopping</code> <code>bool</code> <p>Enable early stopping based on global acceptance rate stability. Defaults to False.</p> <code>False</code> <code>early_stopping_tolerance</code> <code>float</code> <p>Relative change threshold for early stopping. Defaults to 0.05.</p> <code>0.05</code> <code>early_stopping_patience</code> <code>int</code> <p>Consecutive stable loops required before stopping. Defaults to 3.</p> <code>3</code> <code>early_stopping_min_acceptance</code> <code>float</code> <p>Minimum global acceptance rate that also triggers early stopping. Defaults to 0.1.</p> <code>0.1</code> <code>verbose</code> <code>bool</code> <p>Enable progress bars and debug logging. Defaults to False.</p> <code>False</code>","path":["Reference","flowMC","resource_strategy_bundle","RQSpline GRW"],"tags":[]},{"location":"api/flowMC/resource_strategy_bundle/RQSpline_GRW_PT/","level":1,"title":"RQSpline GRW PT","text":"","path":["Reference","flowMC","resource_strategy_bundle","RQSpline GRW PT"],"tags":[]},{"location":"api/flowMC/resource_strategy_bundle/RQSpline_GRW_PT/#flowMC.resource_strategy_bundle.RQSpline_GRW_PT.RQSpline_GRW_PT_Bundle","level":3,"title":"<code>RQSpline_GRW_PT_Bundle</code>","text":"<p>               Bases: <code>ResourceStrategyBundle</code></p> <p>A bundle that uses a Rational Quadratic Spline as a normalizing flow model and Gaussian Random Walk as a local sampler.</p> <p>The main difference between this and the RQSpline_GRW_Bundle is that this bundle uses an additional parallel tempering step to sample from the target distribution.</p> <p>In this bundle, the sampler requires an additional logpdf function that is the prior distribution. If the log prior is not provided, it will be set to 0.</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Build all resources and strategies for an RQSpline + GRW + PT sampling run.</p>","path":["Reference","flowMC","resource_strategy_bundle","RQSpline GRW PT"],"tags":[]},{"location":"api/flowMC/resource_strategy_bundle/RQSpline_GRW_PT/#flowMC.resource_strategy_bundle.RQSpline_GRW_PT.RQSpline_GRW_PT_Bundle.__init__","level":4,"title":"<code>__init__(rng_key: Key, n_chains: int, n_dims: int, logpdf: Callable[[Float[Array, ' n_dim'], dict], Float], n_local_steps: int, n_global_steps: int, n_training_loops: int, n_production_loops: int, n_epochs: int, grw_step_size: Float | Float[Array, ' n_dim'] = 0.1, adapt_step_size: bool = True, adapt_step_size_per_dim: bool = True, periodic: Optional[dict[int, tuple[float, float]]] = None, rq_spline_hidden_units: list[int] = [32, 32], rq_spline_n_bins: int = 8, rq_spline_n_layers: int = 4, n_NFproposal_batch_size: int = 10000, learning_rate: float = 0.001, batch_size: int = 10000, n_max_examples: int = 10000, history_window: int = 100, chain_batch_size: int = 0, local_thinning: int = 1, global_thinning: int = 1, n_temperatures: int = 5, max_temperature: float = 5.0, n_tempered_steps: int = -1, logprior: Callable[[Float[Array, ' n_dim'], dict], Float] = lambda x, _: 0.0, early_stopping: bool = False, early_stopping_tolerance: float = 0.05, early_stopping_patience: int = 3, early_stopping_min_acceptance: float = 0.1, verbose: bool = False) -&gt; None</code>","text":"<p>Build all resources and strategies for an RQSpline + GRW + PT sampling run.</p> <p>Parameters:</p> Name Type Description Default <code>rng_key</code> <code>Key</code> <p>JAX PRNGKey used to initialise the normalizing flow.</p> required <code>n_chains</code> <code>int</code> <p>Number of parallel MCMC chains.</p> required <code>n_dims</code> <code>int</code> <p>Dimensionality of the target distribution.</p> required <code>logpdf</code> <code>Callable</code> <p>Log-likelihood <code>f(x, data) -&gt; Float</code>.</p> required <code>n_local_steps</code> <code>int</code> <p>GRW steps per training/production loop iteration.</p> required <code>n_global_steps</code> <code>int</code> <p>NF-proposal steps per training/production loop iteration.</p> required <code>n_training_loops</code> <code>int</code> <p>Number of train-then-sample iterations (warmup).</p> required <code>n_production_loops</code> <code>int</code> <p>Number of production sampling iterations.</p> required <code>n_epochs</code> <code>int</code> <p>NF training epochs per training loop.</p> required <code>grw_step_size</code> <code>float | Float[Array, n_dim]</code> <p>Initial GRW step size; scalar or per-dimension array. Defaults to 0.1.</p> <code>0.1</code> <code>adapt_step_size</code> <code>bool</code> <p>Adapt the GRW step size during training. Defaults to True.</p> <code>True</code> <code>adapt_step_size_per_dim</code> <code>bool</code> <p>Also tune per-dimension step size ratios using the empirical std of recent chain positions. Runs after adapt_step_size. Defaults to True.</p> <code>True</code> <code>periodic</code> <code>dict[int, tuple[float, float]] | None</code> <p>Periodic boundary conditions as <code>{dim_index: (lower, upper)}</code>. Defaults to None.</p> <code>None</code> <code>rq_spline_hidden_units</code> <code>list[int]</code> <p>Hidden units per conditioner MLP layer. Defaults to <code>[32, 32]</code>.</p> <code>[32, 32]</code> <code>rq_spline_n_bins</code> <code>int</code> <p>Number of RQ-spline bins. Defaults to 8.</p> <code>8</code> <code>rq_spline_n_layers</code> <code>int</code> <p>Number of masked coupling layers. Defaults to 4.</p> <code>4</code> <code>n_NFproposal_batch_size</code> <code>int</code> <p>NF log-prob evaluation batch size. Defaults to 10000.</p> <code>10000</code> <code>learning_rate</code> <code>float</code> <p>Adam learning rate for NF training. Defaults to 1e-3.</p> <code>0.001</code> <code>batch_size</code> <code>int</code> <p>Mini-batch size for NF training. Defaults to 10000.</p> <code>10000</code> <code>n_max_examples</code> <code>int</code> <p>Maximum training examples per call. Defaults to 10000.</p> <code>10000</code> <code>history_window</code> <code>int</code> <p>Use only the last <code>history_window</code> stored steps as training data. Defaults to 100.</p> <code>100</code> <code>chain_batch_size</code> <code>int</code> <p>Process chains in sub-batches of this size to reduce peak memory. 0 disables batching. Defaults to 0.</p> <code>0</code> <code>local_thinning</code> <code>int</code> <p>Store every <code>local_thinning</code>-th local step. Defaults to 1.</p> <code>1</code> <code>global_thinning</code> <code>int</code> <p>Store every <code>global_thinning</code>-th global step. Defaults to 1.</p> <code>1</code> <code>n_temperatures</code> <code>int</code> <p>Number of parallel tempering temperature levels. Defaults to 5.</p> <code>5</code> <code>max_temperature</code> <code>float</code> <p>Maximum temperature (lowest inverse temperature). Defaults to 5.0.</p> <code>5.0</code> <code>n_tempered_steps</code> <code>int</code> <p>Local kernel steps per temperature per PT exchange. Pass -1 to use <code>n_local_steps</code>. Defaults to -1.</p> <code>-1</code> <code>logprior</code> <code>Callable</code> <p>Log-prior <code>f(x, data) -&gt; Float</code> used to construct the tempered density. Defaults to a constant-zero prior.</p> <code>lambda x, _: 0.0</code> <code>early_stopping</code> <code>bool</code> <p>Enable early stopping based on global acceptance rate stability. Defaults to False.</p> <code>False</code> <code>early_stopping_tolerance</code> <code>float</code> <p>Relative change threshold for early stopping. Defaults to 0.05.</p> <code>0.05</code> <code>early_stopping_patience</code> <code>int</code> <p>Consecutive stable loops required before stopping. Defaults to 3.</p> <code>3</code> <code>early_stopping_min_acceptance</code> <code>float</code> <p>Minimum global acceptance rate that also triggers early stopping. Defaults to 0.1.</p> <code>0.1</code> <code>verbose</code> <code>bool</code> <p>Enable progress bars and debug logging. Defaults to False.</p> <code>False</code>","path":["Reference","flowMC","resource_strategy_bundle","RQSpline GRW PT"],"tags":[]},{"location":"api/flowMC/resource_strategy_bundle/RQSpline_HMC/","level":1,"title":"RQSpline HMC","text":"","path":["Reference","flowMC","resource_strategy_bundle","RQSpline HMC"],"tags":[]},{"location":"api/flowMC/resource_strategy_bundle/RQSpline_HMC/#flowMC.resource_strategy_bundle.RQSpline_HMC.RQSpline_HMC_Bundle","level":3,"title":"<code>RQSpline_HMC_Bundle</code>","text":"<p>               Bases: <code>ResourceStrategyBundle</code></p> <p>A bundle that uses a Rational Quadratic Spline as a normalizing flow model and Hamiltonian Monte Carlo as a local sampler.</p> <p>This is similar to the RQSpline_MALA_Bundle but uses HMC instead of MALA for local sampling.</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Build all resources and strategies for an RQSpline + HMC sampling run.</p>","path":["Reference","flowMC","resource_strategy_bundle","RQSpline HMC"],"tags":[]},{"location":"api/flowMC/resource_strategy_bundle/RQSpline_HMC/#flowMC.resource_strategy_bundle.RQSpline_HMC.RQSpline_HMC_Bundle.__init__","level":4,"title":"<code>__init__(rng_key: Key, n_chains: int, n_dims: int, logpdf: Callable[[Float[Array, ' n_dim'], dict], Float], n_local_steps: int, n_global_steps: int, n_training_loops: int, n_production_loops: int, n_epochs: int, hmc_step_size: float = 0.1, hmc_n_leapfrog: int = 10, condition_matrix: Float | Float[Array, ' n_dim'] = 1, adapt_step_size: bool = True, adapt_step_size_per_dim: bool = True, periodic: Optional[dict[int, tuple[float, float]]] = None, rq_spline_hidden_units: list[int] = [32, 32], rq_spline_n_bins: int = 8, rq_spline_n_layers: int = 4, n_NFproposal_batch_size: int = 10000, learning_rate: float = 0.001, batch_size: int = 10000, n_max_examples: int = 10000, history_window: int = 100, chain_batch_size: int = 0, local_thinning: int = 1, global_thinning: int = 1, early_stopping: bool = False, early_stopping_tolerance: float = 0.05, early_stopping_patience: int = 3, early_stopping_min_acceptance: float = 0.1, verbose: bool = False) -&gt; None</code>","text":"<p>Build all resources and strategies for an RQSpline + HMC sampling run.</p> <p>Parameters:</p> Name Type Description Default <code>rng_key</code> <code>Key</code> <p>JAX PRNGKey used to initialise the normalizing flow.</p> required <code>n_chains</code> <code>int</code> <p>Number of parallel MCMC chains.</p> required <code>n_dims</code> <code>int</code> <p>Dimensionality of the target distribution.</p> required <code>logpdf</code> <code>Callable</code> <p>Log-PDF <code>f(x, data) -&gt; Float</code>.</p> required <code>n_local_steps</code> <code>int</code> <p>HMC steps per training/production loop iteration.</p> required <code>n_global_steps</code> <code>int</code> <p>NF-proposal steps per training/production loop iteration.</p> required <code>n_training_loops</code> <code>int</code> <p>Number of train-then-sample iterations (warmup).</p> required <code>n_production_loops</code> <code>int</code> <p>Number of production sampling iterations.</p> required <code>n_epochs</code> <code>int</code> <p>NF training epochs per training loop.</p> required <code>hmc_step_size</code> <code>float</code> <p>Initial leapfrog step size. Defaults to 0.1.</p> <code>0.1</code> <code>hmc_n_leapfrog</code> <code>int</code> <p>Number of leapfrog steps per HMC proposal. Defaults to 10.</p> <code>10</code> <code>condition_matrix</code> <code>float | Float[Array, n_dim]</code> <p>Diagonal mass-matrix elements; scalar (broadcast) or per-dimension array. Defaults to 1.</p> <code>1</code> <code>adapt_step_size</code> <code>bool</code> <p>Adapt the HMC step size during training. Defaults to True.</p> <code>True</code> <code>adapt_step_size_per_dim</code> <code>bool</code> <p>Also tune per-dimension effective step sizes via the mass matrix using the empirical std of recent chain positions. Runs after adapt_step_size. Defaults to True.</p> <code>True</code> <code>periodic</code> <code>dict[int, tuple[float, float]] | None</code> <p>Periodic boundary conditions as <code>{dim_index: (lower, upper)}</code>. Defaults to None.</p> <code>None</code> <code>rq_spline_hidden_units</code> <code>list[int]</code> <p>Hidden units per conditioner MLP layer. Defaults to <code>[32, 32]</code>.</p> <code>[32, 32]</code> <code>rq_spline_n_bins</code> <code>int</code> <p>Number of RQ-spline bins. Defaults to 8.</p> <code>8</code> <code>rq_spline_n_layers</code> <code>int</code> <p>Number of masked coupling layers. Defaults to 4.</p> <code>4</code> <code>n_NFproposal_batch_size</code> <code>int</code> <p>NF log-prob evaluation batch size. Defaults to 10000.</p> <code>10000</code> <code>learning_rate</code> <code>float</code> <p>Adam learning rate for NF training. Defaults to 1e-3.</p> <code>0.001</code> <code>batch_size</code> <code>int</code> <p>Mini-batch size for NF training. Defaults to 10000.</p> <code>10000</code> <code>n_max_examples</code> <code>int</code> <p>Maximum training examples per call. Defaults to 10000.</p> <code>10000</code> <code>history_window</code> <code>int</code> <p>Use only the last <code>history_window</code> stored steps as training data. Defaults to 100.</p> <code>100</code> <code>chain_batch_size</code> <code>int</code> <p>Process chains in sub-batches of this size to reduce peak memory. 0 disables batching. Defaults to 0.</p> <code>0</code> <code>local_thinning</code> <code>int</code> <p>Store every <code>local_thinning</code>-th local step. Defaults to 1.</p> <code>1</code> <code>global_thinning</code> <code>int</code> <p>Store every <code>global_thinning</code>-th global step. Defaults to 1.</p> <code>1</code> <code>early_stopping</code> <code>bool</code> <p>Enable early stopping based on global acceptance rate stability. Defaults to False.</p> <code>False</code> <code>early_stopping_tolerance</code> <code>float</code> <p>Relative change threshold for early stopping. Defaults to 0.05.</p> <code>0.05</code> <code>early_stopping_patience</code> <code>int</code> <p>Consecutive stable loops required before stopping. Defaults to 3.</p> <code>3</code> <code>early_stopping_min_acceptance</code> <code>float</code> <p>Minimum global acceptance rate that also triggers early stopping. Defaults to 0.1.</p> <code>0.1</code> <code>verbose</code> <code>bool</code> <p>Enable progress bars and debug logging. Defaults to False.</p> <code>False</code>","path":["Reference","flowMC","resource_strategy_bundle","RQSpline HMC"],"tags":[]},{"location":"api/flowMC/resource_strategy_bundle/RQSpline_HMC_PT/","level":1,"title":"RQSpline HMC PT","text":"","path":["Reference","flowMC","resource_strategy_bundle","RQSpline HMC PT"],"tags":[]},{"location":"api/flowMC/resource_strategy_bundle/RQSpline_HMC_PT/#flowMC.resource_strategy_bundle.RQSpline_HMC_PT.RQSpline_HMC_PT_Bundle","level":3,"title":"<code>RQSpline_HMC_PT_Bundle</code>","text":"<p>               Bases: <code>ResourceStrategyBundle</code></p> <p>A bundle that uses a Rational Quadratic Spline as a normalizing flow model and Hamiltonian Monte Carlo as a local sampler.</p> <p>The main difference between this and the RQSpline_HMC_Bundle is that this bundle uses an additional parallel tempering step to sample from the target distribution.</p> <p>In this bundle, the sampler requires an additional logpdf function that is the prior distribution. If the log prior is not provided, it will be set to 0.</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Build all resources and strategies for an RQSpline + HMC + PT sampling run.</p>","path":["Reference","flowMC","resource_strategy_bundle","RQSpline HMC PT"],"tags":[]},{"location":"api/flowMC/resource_strategy_bundle/RQSpline_HMC_PT/#flowMC.resource_strategy_bundle.RQSpline_HMC_PT.RQSpline_HMC_PT_Bundle.__init__","level":4,"title":"<code>__init__(rng_key: Key, n_chains: int, n_dims: int, logpdf: Callable[[Float[Array, ' n_dim'], dict], Float], n_local_steps: int, n_global_steps: int, n_training_loops: int, n_production_loops: int, n_epochs: int, hmc_step_size: float = 0.1, hmc_n_leapfrog: int = 10, condition_matrix: Float | Float[Array, ' n_dim'] = 1, adapt_step_size: bool = True, adapt_step_size_per_dim: bool = True, periodic: Optional[dict[int, tuple[float, float]]] = None, rq_spline_hidden_units: list[int] = [32, 32], rq_spline_n_bins: int = 8, rq_spline_n_layers: int = 4, n_NFproposal_batch_size: int = 10000, learning_rate: float = 0.001, batch_size: int = 10000, n_max_examples: int = 10000, history_window: int = 100, chain_batch_size: int = 0, local_thinning: int = 1, global_thinning: int = 1, n_temperatures: int = 5, max_temperature: float = 5.0, n_tempered_steps: int = -1, logprior: Callable[[Float[Array, ' n_dim'], dict], Float] = lambda x, _: 0.0, early_stopping: bool = False, early_stopping_tolerance: float = 0.05, early_stopping_patience: int = 3, early_stopping_min_acceptance: float = 0.1, verbose: bool = False) -&gt; None</code>","text":"<p>Build all resources and strategies for an RQSpline + HMC + PT sampling run.</p> <p>Parameters:</p> Name Type Description Default <code>rng_key</code> <code>Key</code> <p>JAX PRNGKey used to initialise the normalizing flow.</p> required <code>n_chains</code> <code>int</code> <p>Number of parallel MCMC chains.</p> required <code>n_dims</code> <code>int</code> <p>Dimensionality of the target distribution.</p> required <code>logpdf</code> <code>Callable</code> <p>Log-likelihood <code>f(x, data) -&gt; Float</code>.</p> required <code>n_local_steps</code> <code>int</code> <p>HMC steps per training/production loop iteration.</p> required <code>n_global_steps</code> <code>int</code> <p>NF-proposal steps per training/production loop iteration.</p> required <code>n_training_loops</code> <code>int</code> <p>Number of train-then-sample iterations (warmup).</p> required <code>n_production_loops</code> <code>int</code> <p>Number of production sampling iterations.</p> required <code>n_epochs</code> <code>int</code> <p>NF training epochs per training loop.</p> required <code>hmc_step_size</code> <code>float</code> <p>Initial leapfrog step size. Defaults to 0.1.</p> <code>0.1</code> <code>hmc_n_leapfrog</code> <code>int</code> <p>Number of leapfrog steps per HMC proposal. Defaults to 10.</p> <code>10</code> <code>condition_matrix</code> <code>float | Float[Array, n_dim]</code> <p>Diagonal mass-matrix elements; scalar (broadcast) or per-dimension array. Defaults to 1.</p> <code>1</code> <code>adapt_step_size</code> <code>bool</code> <p>Adapt the HMC step size during training. Defaults to True.</p> <code>True</code> <code>adapt_step_size_per_dim</code> <code>bool</code> <p>Also tune per-dimension effective step sizes via the mass matrix using the empirical std of recent chain positions. Runs after adapt_step_size. Defaults to True.</p> <code>True</code> <code>periodic</code> <code>dict[int, tuple[float, float]] | None</code> <p>Periodic boundary conditions as <code>{dim_index: (lower, upper)}</code>. Defaults to None.</p> <code>None</code> <code>rq_spline_hidden_units</code> <code>list[int]</code> <p>Hidden units per conditioner MLP layer. Defaults to <code>[32, 32]</code>.</p> <code>[32, 32]</code> <code>rq_spline_n_bins</code> <code>int</code> <p>Number of RQ-spline bins. Defaults to 8.</p> <code>8</code> <code>rq_spline_n_layers</code> <code>int</code> <p>Number of masked coupling layers. Defaults to 4.</p> <code>4</code> <code>n_NFproposal_batch_size</code> <code>int</code> <p>NF log-prob evaluation batch size. Defaults to 10000.</p> <code>10000</code> <code>learning_rate</code> <code>float</code> <p>Adam learning rate for NF training. Defaults to 1e-3.</p> <code>0.001</code> <code>batch_size</code> <code>int</code> <p>Mini-batch size for NF training. Defaults to 10000.</p> <code>10000</code> <code>n_max_examples</code> <code>int</code> <p>Maximum training examples per call. Defaults to 10000.</p> <code>10000</code> <code>history_window</code> <code>int</code> <p>Use only the last <code>history_window</code> stored steps as training data. Defaults to 100.</p> <code>100</code> <code>chain_batch_size</code> <code>int</code> <p>Process chains in sub-batches of this size to reduce peak memory. 0 disables batching. Defaults to 0.</p> <code>0</code> <code>local_thinning</code> <code>int</code> <p>Store every <code>local_thinning</code>-th local step. Defaults to 1.</p> <code>1</code> <code>global_thinning</code> <code>int</code> <p>Store every <code>global_thinning</code>-th global step. Defaults to 1.</p> <code>1</code> <code>n_temperatures</code> <code>int</code> <p>Number of parallel tempering temperature levels. Defaults to 5.</p> <code>5</code> <code>max_temperature</code> <code>float</code> <p>Maximum temperature (lowest inverse temperature). Defaults to 5.0.</p> <code>5.0</code> <code>n_tempered_steps</code> <code>int</code> <p>Local kernel steps per temperature per PT exchange. Pass -1 to use <code>n_local_steps</code>. Defaults to -1.</p> <code>-1</code> <code>logprior</code> <code>Callable</code> <p>Log-prior <code>f(x, data) -&gt; Float</code> used to construct the tempered density. Defaults to a constant-zero prior.</p> <code>lambda x, _: 0.0</code> <code>early_stopping</code> <code>bool</code> <p>Enable early stopping based on global acceptance rate stability. Defaults to False.</p> <code>False</code> <code>early_stopping_tolerance</code> <code>float</code> <p>Relative change threshold for early stopping. Defaults to 0.05.</p> <code>0.05</code> <code>early_stopping_patience</code> <code>int</code> <p>Consecutive stable loops required before stopping. Defaults to 3.</p> <code>3</code> <code>early_stopping_min_acceptance</code> <code>float</code> <p>Minimum global acceptance rate that also triggers early stopping. Defaults to 0.1.</p> <code>0.1</code> <code>verbose</code> <code>bool</code> <p>Enable progress bars and debug logging. Defaults to False.</p> <code>False</code>","path":["Reference","flowMC","resource_strategy_bundle","RQSpline HMC PT"],"tags":[]},{"location":"api/flowMC/resource_strategy_bundle/RQSpline_MALA/","level":1,"title":"RQSpline MALA","text":"","path":["Reference","flowMC","resource_strategy_bundle","RQSpline MALA"],"tags":[]},{"location":"api/flowMC/resource_strategy_bundle/RQSpline_MALA/#flowMC.resource_strategy_bundle.RQSpline_MALA.RQSpline_MALA_Bundle","level":3,"title":"<code>RQSpline_MALA_Bundle</code>","text":"<p>               Bases: <code>ResourceStrategyBundle</code></p> <p>A bundle that uses a Rational Quadratic Spline as a normalizing flow model and the Metropolis Adjusted Langevin Algorithm as a local sampler.</p> <p>This is the base algorithm described in https://www.pnas.org/doi/full/10.1073/pnas.2109420119</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Build all resources and strategies for an RQSpline + MALA sampling run.</p>","path":["Reference","flowMC","resource_strategy_bundle","RQSpline MALA"],"tags":[]},{"location":"api/flowMC/resource_strategy_bundle/RQSpline_MALA/#flowMC.resource_strategy_bundle.RQSpline_MALA.RQSpline_MALA_Bundle.__init__","level":4,"title":"<code>__init__(rng_key: Key, n_chains: int, n_dims: int, logpdf: Callable[[Float[Array, ' n_dim'], dict], Float], n_local_steps: int, n_global_steps: int, n_training_loops: int, n_production_loops: int, n_epochs: int, mala_step_size: Float | Float[Array, ' n_dim'] = 0.1, adapt_step_size: bool = True, adapt_step_size_per_dim: bool = True, periodic: Optional[dict[int, tuple[float, float]]] = None, rq_spline_hidden_units: list[int] = [32, 32], rq_spline_n_bins: int = 8, rq_spline_n_layers: int = 4, n_NFproposal_batch_size: int = 10000, learning_rate: float = 0.001, batch_size: int = 10000, n_max_examples: int = 10000, history_window: int = 100, chain_batch_size: int = 0, local_thinning: int = 1, global_thinning: int = 1, early_stopping: bool = False, early_stopping_tolerance: float = 0.05, early_stopping_patience: int = 3, early_stopping_min_acceptance: float = 0.1, verbose: bool = False) -&gt; None</code>","text":"<p>Build all resources and strategies for an RQSpline + MALA sampling run.</p> <p>Parameters:</p> Name Type Description Default <code>rng_key</code> <code>Key</code> <p>JAX PRNGKey used to initialise the normalizing flow.</p> required <code>n_chains</code> <code>int</code> <p>Number of parallel MCMC chains.</p> required <code>n_dims</code> <code>int</code> <p>Dimensionality of the target distribution.</p> required <code>logpdf</code> <code>Callable</code> <p>Log-PDF <code>f(x, data) -&gt; Float</code>.</p> required <code>n_local_steps</code> <code>int</code> <p>MALA steps per training/production loop iteration.</p> required <code>n_global_steps</code> <code>int</code> <p>NF-proposal steps per training/production loop iteration.</p> required <code>n_training_loops</code> <code>int</code> <p>Number of train-then-sample iterations (warmup).</p> required <code>n_production_loops</code> <code>int</code> <p>Number of production sampling iterations.</p> required <code>n_epochs</code> <code>int</code> <p>NF training epochs per training loop.</p> required <code>mala_step_size</code> <code>float | Float[Array, n_dim]</code> <p>Initial MALA step size; scalar or per-dimension array. Defaults to 0.1.</p> <code>0.1</code> <code>adapt_step_size</code> <code>bool</code> <p>Adapt the MALA step size during training. Defaults to True.</p> <code>True</code> <code>adapt_step_size_per_dim</code> <code>bool</code> <p>Also tune per-dimension step size ratios using the empirical std of recent chain positions. Runs after adapt_step_size. Defaults to True.</p> <code>True</code> <code>periodic</code> <code>dict[int, tuple[float, float]] | None</code> <p>Periodic boundary conditions as <code>{dim_index: (lower, upper)}</code>. Defaults to None.</p> <code>None</code> <code>rq_spline_hidden_units</code> <code>list[int]</code> <p>Hidden units per conditioner MLP layer. Defaults to <code>[32, 32]</code>.</p> <code>[32, 32]</code> <code>rq_spline_n_bins</code> <code>int</code> <p>Number of RQ-spline bins. Defaults to 8.</p> <code>8</code> <code>rq_spline_n_layers</code> <code>int</code> <p>Number of masked coupling layers. Defaults to 4.</p> <code>4</code> <code>n_NFproposal_batch_size</code> <code>int</code> <p>NF log-prob evaluation batch size. Defaults to 10000.</p> <code>10000</code> <code>learning_rate</code> <code>float</code> <p>Adam learning rate for NF training. Defaults to 1e-3.</p> <code>0.001</code> <code>batch_size</code> <code>int</code> <p>Mini-batch size for NF training. Defaults to 10000.</p> <code>10000</code> <code>n_max_examples</code> <code>int</code> <p>Maximum training examples per call. Defaults to 10000.</p> <code>10000</code> <code>history_window</code> <code>int</code> <p>Use only the last <code>history_window</code> stored steps as training data. Defaults to 100.</p> <code>100</code> <code>chain_batch_size</code> <code>int</code> <p>Process chains in sub-batches of this size to reduce peak memory. 0 disables batching. Defaults to 0.</p> <code>0</code> <code>local_thinning</code> <code>int</code> <p>Store every <code>local_thinning</code>-th local step. Defaults to 1.</p> <code>1</code> <code>global_thinning</code> <code>int</code> <p>Store every <code>global_thinning</code>-th global step. Defaults to 1.</p> <code>1</code> <code>early_stopping</code> <code>bool</code> <p>Enable early stopping based on global acceptance rate stability. Defaults to False.</p> <code>False</code> <code>early_stopping_tolerance</code> <code>float</code> <p>Relative change threshold for early stopping. Defaults to 0.05.</p> <code>0.05</code> <code>early_stopping_patience</code> <code>int</code> <p>Consecutive stable loops required before stopping. Defaults to 3.</p> <code>3</code> <code>early_stopping_min_acceptance</code> <code>float</code> <p>Minimum global acceptance rate that also triggers early stopping. Defaults to 0.1.</p> <code>0.1</code> <code>verbose</code> <code>bool</code> <p>Enable progress bars and debug logging. Defaults to False.</p> <code>False</code>","path":["Reference","flowMC","resource_strategy_bundle","RQSpline MALA"],"tags":[]},{"location":"api/flowMC/resource_strategy_bundle/RQSpline_MALA_PT/","level":1,"title":"RQSpline MALA PT","text":"","path":["Reference","flowMC","resource_strategy_bundle","RQSpline MALA PT"],"tags":[]},{"location":"api/flowMC/resource_strategy_bundle/RQSpline_MALA_PT/#flowMC.resource_strategy_bundle.RQSpline_MALA_PT.RQSpline_MALA_PT_Bundle","level":3,"title":"<code>RQSpline_MALA_PT_Bundle</code>","text":"<p>               Bases: <code>ResourceStrategyBundle</code></p> <p>A bundle that uses a Rational Quadratic Spline as a normalizing flow model and the Metropolis Adjusted Langevin Algorithm as a local sampler.</p> <p>The main difference between this and the RQSpline_MALA_Bundle is that this bundle uses an additional parallel tempering step to sample from the target distribution.</p> <p>In this bundle, the sampler requires an additional logpdf function that is the prior distribution. If the log prior is not provided, it will be set to 0.</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Build all resources and strategies for an RQSpline + MALA + PT sampling run.</p>","path":["Reference","flowMC","resource_strategy_bundle","RQSpline MALA PT"],"tags":[]},{"location":"api/flowMC/resource_strategy_bundle/RQSpline_MALA_PT/#flowMC.resource_strategy_bundle.RQSpline_MALA_PT.RQSpline_MALA_PT_Bundle.__init__","level":4,"title":"<code>__init__(rng_key: Key, n_chains: int, n_dims: int, logpdf: Callable[[Float[Array, ' n_dim'], dict], Float], n_local_steps: int, n_global_steps: int, n_training_loops: int, n_production_loops: int, n_epochs: int, mala_step_size: Float | Float[Array, ' n_dim'] = 0.1, adapt_step_size: bool = True, adapt_step_size_per_dim: bool = True, periodic: Optional[dict[int, tuple[float, float]]] = None, rq_spline_hidden_units: list[int] = [32, 32], rq_spline_n_bins: int = 8, rq_spline_n_layers: int = 4, n_NFproposal_batch_size: int = 10000, learning_rate: float = 0.001, batch_size: int = 10000, n_max_examples: int = 10000, history_window: int = 100, chain_batch_size: int = 0, local_thinning: int = 1, global_thinning: int = 1, n_temperatures: int = 5, max_temperature: float = 5.0, n_tempered_steps: int = -1, logprior: Callable[[Float[Array, ' n_dim'], dict], Float] = lambda x, _: 0.0, early_stopping: bool = False, early_stopping_tolerance: float = 0.05, early_stopping_patience: int = 3, early_stopping_min_acceptance: float = 0.1, verbose: bool = False) -&gt; None</code>","text":"<p>Build all resources and strategies for an RQSpline + MALA + PT sampling run.</p> <p>Parameters:</p> Name Type Description Default <code>rng_key</code> <code>Key</code> <p>JAX PRNGKey used to initialise the normalizing flow.</p> required <code>n_chains</code> <code>int</code> <p>Number of parallel MCMC chains.</p> required <code>n_dims</code> <code>int</code> <p>Dimensionality of the target distribution.</p> required <code>logpdf</code> <code>Callable</code> <p>Log-likelihood <code>f(x, data) -&gt; Float</code>.</p> required <code>n_local_steps</code> <code>int</code> <p>MALA steps per training/production loop iteration.</p> required <code>n_global_steps</code> <code>int</code> <p>NF-proposal steps per training/production loop iteration.</p> required <code>n_training_loops</code> <code>int</code> <p>Number of train-then-sample iterations (warmup).</p> required <code>n_production_loops</code> <code>int</code> <p>Number of production sampling iterations.</p> required <code>n_epochs</code> <code>int</code> <p>NF training epochs per training loop.</p> required <code>mala_step_size</code> <code>float | Float[Array, n_dim]</code> <p>Initial MALA step size; scalar or per-dimension array. Defaults to 0.1.</p> <code>0.1</code> <code>adapt_step_size</code> <code>bool</code> <p>Adapt the MALA step size during training. Defaults to True.</p> <code>True</code> <code>adapt_step_size_per_dim</code> <code>bool</code> <p>Also tune per-dimension step size ratios using the empirical std of recent chain positions. Runs after adapt_step_size. Defaults to True.</p> <code>True</code> <code>periodic</code> <code>dict[int, tuple[float, float]] | None</code> <p>Periodic boundary conditions as <code>{dim_index: (lower, upper)}</code>. Defaults to None.</p> <code>None</code> <code>rq_spline_hidden_units</code> <code>list[int]</code> <p>Hidden units per conditioner MLP layer. Defaults to <code>[32, 32]</code>.</p> <code>[32, 32]</code> <code>rq_spline_n_bins</code> <code>int</code> <p>Number of RQ-spline bins. Defaults to 8.</p> <code>8</code> <code>rq_spline_n_layers</code> <code>int</code> <p>Number of masked coupling layers. Defaults to 4.</p> <code>4</code> <code>n_NFproposal_batch_size</code> <code>int</code> <p>NF log-prob evaluation batch size. Defaults to 10000.</p> <code>10000</code> <code>learning_rate</code> <code>float</code> <p>Adam learning rate for NF training. Defaults to 1e-3.</p> <code>0.001</code> <code>batch_size</code> <code>int</code> <p>Mini-batch size for NF training. Defaults to 10000.</p> <code>10000</code> <code>n_max_examples</code> <code>int</code> <p>Maximum training examples per call. Defaults to 10000.</p> <code>10000</code> <code>history_window</code> <code>int</code> <p>Use only the last <code>history_window</code> stored steps as training data. Defaults to 100.</p> <code>100</code> <code>chain_batch_size</code> <code>int</code> <p>Process chains in sub-batches of this size to reduce peak memory. 0 disables batching. Defaults to 0.</p> <code>0</code> <code>local_thinning</code> <code>int</code> <p>Store every <code>local_thinning</code>-th local step. Defaults to 1.</p> <code>1</code> <code>global_thinning</code> <code>int</code> <p>Store every <code>global_thinning</code>-th global step. Defaults to 1.</p> <code>1</code> <code>n_temperatures</code> <code>int</code> <p>Number of parallel tempering temperature levels. Defaults to 5.</p> <code>5</code> <code>max_temperature</code> <code>float</code> <p>Maximum temperature (lowest inverse temperature). Defaults to 5.0.</p> <code>5.0</code> <code>n_tempered_steps</code> <code>int</code> <p>Local kernel steps per temperature per PT exchange. Pass -1 to use <code>n_local_steps</code>. Defaults to -1.</p> <code>-1</code> <code>logprior</code> <code>Callable</code> <p>Log-prior <code>f(x, data) -&gt; Float</code> used to construct the tempered density. Defaults to a constant-zero prior.</p> <code>lambda x, _: 0.0</code> <code>early_stopping</code> <code>bool</code> <p>Enable early stopping based on global acceptance rate stability. Defaults to False.</p> <code>False</code> <code>early_stopping_tolerance</code> <code>float</code> <p>Relative change threshold for early stopping. Defaults to 0.05.</p> <code>0.05</code> <code>early_stopping_patience</code> <code>int</code> <p>Consecutive stable loops required before stopping. Defaults to 3.</p> <code>3</code> <code>early_stopping_min_acceptance</code> <code>float</code> <p>Minimum global acceptance rate that also triggers early stopping. Defaults to 0.1.</p> <code>0.1</code> <code>verbose</code> <code>bool</code> <p>Enable progress bars and debug logging. Defaults to False.</p> <code>False</code>","path":["Reference","flowMC","resource_strategy_bundle","RQSpline MALA PT"],"tags":[]},{"location":"api/flowMC/resource_strategy_bundle/base/","level":1,"title":"Base","text":"","path":["Reference","flowMC","resource_strategy_bundle","Base"],"tags":[]},{"location":"api/flowMC/resource_strategy_bundle/base/#flowMC.resource_strategy_bundle.base.ResourceStrategyBundle","level":3,"title":"<code>ResourceStrategyBundle</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Resource-Strategy Bundle is aim to be the highest level of abstraction in the flowMC library.</p> <p>It is a collection of resources and strategies that are used to perform a specific task.</p>","path":["Reference","flowMC","resource_strategy_bundle","Base"],"tags":[]},{"location":"api/flowMC/strategy/adapt_step_size/","level":1,"title":"Adapt step size","text":"","path":["Reference","flowMC","strategy","Adapt step size"],"tags":[]},{"location":"api/flowMC/strategy/adapt_step_size/#flowMC.strategy.adapt_step_size.AdaptStepSize","level":3,"title":"<code>AdaptStepSize</code>","text":"<p>               Bases: <code>Strategy</code></p> <p>Strategy to adapt the step size of a local sampler based on acceptance rates.</p> <p>This strategy computes the acceptance rate from a specified buffer and calls the kernel's adapt_step_size method.</p> <p>The strategy is generic and works with any kernel that implements the adapt_step_size(acceptance_rate, target_rate) method.</p> <p>Methods:</p> Name Description <code>__call__</code> <p>Adapt the kernel's step size based on recent acceptance rates.</p> <code>__init__</code> <p>Initialize the AdaptStepSize strategy.</p>","path":["Reference","flowMC","strategy","Adapt step size"],"tags":[]},{"location":"api/flowMC/strategy/adapt_step_size/#flowMC.strategy.adapt_step_size.AdaptStepSize.__call__","level":4,"title":"<code>__call__(rng_key: Key, resources: dict[str, Resource], initial_position: Float[Array, 'n_chains n_dim'], data: dict) -&gt; tuple[Key, dict[str, Resource], Float[Array, 'n_chains n_dim']]</code>","text":"<p>Adapt the kernel's step size based on recent acceptance rates.</p> <p>Parameters:</p> Name Type Description Default <code>rng_key</code> <code>Key</code> <p>JAX PRNGKey.</p> required <code>resources</code> <code>dict[str, Resource]</code> <p>Dictionary of resources.</p> required <code>initial_position</code> <code>Float[Array, 'n_chains n_dim']</code> <p>Current positions of chains.</p> required <code>data</code> <code>dict</code> <p>Additional data.</p> required <p>Returns:</p> Type Description <code>tuple[Key, dict[str, Resource], Float[Array, 'n_chains n_dim']]</code> <p>Tuple of (rng_key, resources, initial_position).</p>","path":["Reference","flowMC","strategy","Adapt step size"],"tags":[]},{"location":"api/flowMC/strategy/adapt_step_size/#flowMC.strategy.adapt_step_size.AdaptStepSize.__init__","level":4,"title":"<code>__init__(kernel_name: str, state_name: str, acceptance_buffer_key: str, target_acceptance_rate: float, acceptance_window: int = 0, n_loops_skip: int = 0, verbose: bool = False)</code>","text":"<p>Initialize the AdaptStepSize strategy.</p> <p>Parameters:</p> Name Type Description Default <code>kernel_name</code> <code>str</code> <p>Name of the kernel resource to adapt.</p> required <code>state_name</code> <code>str</code> <p>Name of the State resource that tracks sampler state.</p> required <code>acceptance_buffer_key</code> <code>str</code> <p>Key in the State resource that points to the acceptance buffer name (e.g., \"target_local_accs\").</p> required <code>target_acceptance_rate</code> <code>float</code> <p>Target acceptance rate for adaptation. Common values: 0.234 (Random Walk), 0.574 (MALA), 0.65 (HMC).</p> required <code>acceptance_window</code> <code>int</code> <p>Number of recent samples to use for computing acceptance rate. If 0, uses all available samples.</p> <code>0</code> <code>n_loops_skip</code> <code>int</code> <p>Skip adaptation for the first N calls to allow chains to reach a stable state before adapting (warmup period).</p> <code>0</code> <code>verbose</code> <code>bool</code> <p>Whether to log adaptation information.</p> <code>False</code>","path":["Reference","flowMC","strategy","Adapt step size"],"tags":[]},{"location":"api/flowMC/strategy/adapt_step_size/#flowMC.strategy.adapt_step_size.AdaptStepSizePerDim","level":3,"title":"<code>AdaptStepSizePerDim</code>","text":"<p>               Bases: <code>Strategy</code></p> <p>Strategy to adapt the per-dimension step size profile using empirical scale estimates.</p> <p>Computes the sample standard deviation of the most-recent <code>window</code> chain positions per chain and sets the per-dimension step size profile so that each dimension's effective step is proportional to its posterior scale.  The geometric mean of the step sizes is preserved, so this strategy controls the shape of the step size vector while <code>AdaptStepSize</code> continues to control its global scale.</p> <p>The positions buffer is fully overwritten every training loop (written from offset 0), so all stored positions are already from the most recent loop — no burn-in bias. Pass <code>window=n_local_steps // local_thinning</code> so the window covers exactly one training loop's worth of stored positions.  The fixed shape limits the array that enters the JIT-compiled variance kernel, keeping per-call runtime low.</p> <p>Each call drives the kernel's effective per-dim profile exactly to the current scale estimate in one step (no damping, no clipping).  Adaptation starts from the first call.</p> <p>Run this after AdaptStepSize in the training phase. Requires the kernel to implement get_effective_dim_profile() and apply_per_dim_scaling().</p> <p>Methods:</p> Name Description <code>__call__</code> <p>Adapt per-dimension step sizes based on the empirical std of recent chain positions.</p> <code>__init__</code> <p>Initialize AdaptStepSizePerDim.</p>","path":["Reference","flowMC","strategy","Adapt step size"],"tags":[]},{"location":"api/flowMC/strategy/adapt_step_size/#flowMC.strategy.adapt_step_size.AdaptStepSizePerDim.__call__","level":4,"title":"<code>__call__(rng_key: Key, resources: dict[str, Resource], initial_position: Float[Array, 'n_chains n_dim'], data: dict) -&gt; tuple[Key, dict[str, Resource], Float[Array, 'n_chains n_dim']]</code>","text":"<p>Adapt per-dimension step sizes based on the empirical std of recent chain positions.</p> <p>Parameters:</p> Name Type Description Default <code>rng_key</code> <code>Key</code> <p>JAX PRNGKey.</p> required <code>resources</code> <code>dict[str, Resource]</code> <p>Dictionary of resources.</p> required <code>initial_position</code> <code>Float[Array, 'n_chains n_dim']</code> <p>Current positions of chains.</p> required <code>data</code> <code>dict</code> <p>Additional data (unused).</p> required <p>Returns:</p> Type Description <code>tuple[Key, dict[str, Resource], Float[Array, 'n_chains n_dim']]</code> <p>Tuple of (rng_key, resources, initial_position).</p>","path":["Reference","flowMC","strategy","Adapt step size"],"tags":[]},{"location":"api/flowMC/strategy/adapt_step_size/#flowMC.strategy.adapt_step_size.AdaptStepSizePerDim.__init__","level":4,"title":"<code>__init__(kernel_name: str, state_name: str, positions_buffer_key: str, window: int = 50, verbose: bool = False)</code>","text":"<p>Initialize AdaptStepSizePerDim.</p> <p>Parameters:</p> Name Type Description Default <code>kernel_name</code> <code>str</code> <p>Name of the kernel resource to adapt.</p> required <code>state_name</code> <code>str</code> <p>Name of the State resource that tracks sampler state.</p> required <code>positions_buffer_key</code> <code>str</code> <p>Key in State that points to the positions buffer name (e.g. \"target_positions\").</p> required <code>window</code> <code>int</code> <p>Number of most-recent steps per chain to use when estimating per-dimension scales. Pass <code>n_local_steps // local_thinning</code> so the window exactly covers the positions written in one training loop.</p> <code>50</code> <code>verbose</code> <code>bool</code> <p>Log per-dim step sizes after each adaptation.</p> <code>False</code>","path":["Reference","flowMC","strategy","Adapt step size"],"tags":[]},{"location":"api/flowMC/strategy/base/","level":1,"title":"Base","text":"","path":["Reference","flowMC","strategy","Base"],"tags":[]},{"location":"api/flowMC/strategy/base/#flowMC.strategy.base.Strategy","level":3,"title":"<code>Strategy</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Base class for strategies, which are basically wrapper blocks that modify the state of the sampler.</p> <p>This is an abstract template that should not be directly used.</p> <p>Methods:</p> Name Description <code>__call__</code> <p>Execute one step of the strategy.</p> <code>__init__</code> <p>Initialize the strategy.</p>","path":["Reference","flowMC","strategy","Base"],"tags":[]},{"location":"api/flowMC/strategy/base/#flowMC.strategy.base.Strategy.__call__","level":4,"title":"<code>__call__(rng_key: Key, resources: dict[str, Resource], initial_position: Float[Array, 'n_chains n_dim'], data: dict) -&gt; tuple[Key, dict[str, Resource], Float[Array, 'n_chains n_dim']]</code>  <small><code>abstractmethod</code></small>","text":"<p>Execute one step of the strategy.</p> <p>Parameters:</p> Name Type Description Default <code>rng_key</code> <code>Key</code> <p>JAX PRNG key (consumed and split internally).</p> required <code>resources</code> <code>dict[str, Resource]</code> <p>Mutable resource dictionary shared across all strategies.</p> required <code>initial_position</code> <code>Float[Array, 'n_chains n_dim']</code> <p>Current chain positions of shape <code>(n_chains, n_dim)</code>.</p> required <code>data</code> <code>dict</code> <p>Auxiliary data passed to log-pdf calls.</p> required <p>Returns:</p> Name Type Description <code>tuple</code> <code>tuple[Key, dict[str, Resource], Float[Array, 'n_chains n_dim']]</code> <p>A 3-tuple of: - Key: Updated PRNG key. - dict[str, Resource]: Updated resources. - Float[Array, \"n_chains n_dim\"]: Updated chain positions.</p>","path":["Reference","flowMC","strategy","Base"],"tags":[]},{"location":"api/flowMC/strategy/base/#flowMC.strategy.base.Strategy.__init__","level":4,"title":"<code>__init__()</code>  <small><code>abstractmethod</code></small>","text":"<p>Initialize the strategy.</p>","path":["Reference","flowMC","strategy","Base"],"tags":[]},{"location":"api/flowMC/strategy/check_early_stop/","level":1,"title":"Check early stop","text":"","path":["Reference","flowMC","strategy","Check early stop"],"tags":[]},{"location":"api/flowMC/strategy/check_early_stop/#flowMC.strategy.check_early_stop.CheckEarlyStop","level":3,"title":"<code>CheckEarlyStop</code>","text":"<p>               Bases: <code>Strategy</code></p> <p>Strategy that checks if global acceptance rate has stabilized.</p> <p>After each training loop iteration, this strategy reads the global acceptance rate from the buffer and compares it (relative change) against the previous loop. If the relative change falls below the tolerance for a specified number of consecutive checks, it sets <code>\"early_stopped\": True</code> in the sampler state, which causes the Sampler to skip remaining training loops and jump directly to the production phase.</p> <p>The <code>n_training_loops</code> parameter effectively becomes the maximum number of training loops when this strategy is enabled.</p> <p>Methods:</p> Name Description <code>__call__</code> <p>Check if global acceptance rate has stabilized.</p> <code>__init__</code> <p>Initialize the CheckEarlyStop strategy.</p>","path":["Reference","flowMC","strategy","Check early stop"],"tags":[]},{"location":"api/flowMC/strategy/check_early_stop/#flowMC.strategy.check_early_stop.CheckEarlyStop.__call__","level":4,"title":"<code>__call__(rng_key: Key, resources: dict[str, Resource], initial_position: Float[Array, 'n_chains n_dim'], data: dict) -&gt; tuple[Key, dict[str, Resource], Float[Array, 'n_chains n_dim']]</code>","text":"<p>Check if global acceptance rate has stabilized.</p> <p>If stable for <code>patience</code> consecutive loops after the warmup period, sets <code>\"early_stopped\": True</code> in the sampler state.</p> <p>Parameters:</p> Name Type Description Default <code>rng_key</code> <code>Key</code> <p>JAX PRNGKey.</p> required <code>resources</code> <code>dict[str, Resource]</code> <p>Dictionary of resources.</p> required <code>initial_position</code> <code>Float[Array, 'n_chains n_dim']</code> <p>Current positions of chains.</p> required <code>data</code> <code>dict</code> <p>Additional data.</p> required <p>Returns:</p> Type Description <code>tuple[Key, dict[str, Resource], Float[Array, 'n_chains n_dim']]</code> <p>Tuple of (rng_key, resources, initial_position).</p>","path":["Reference","flowMC","strategy","Check early stop"],"tags":[]},{"location":"api/flowMC/strategy/check_early_stop/#flowMC.strategy.check_early_stop.CheckEarlyStop.__init__","level":4,"title":"<code>__init__(state_name: str, acceptance_buffer_key: str = 'target_global_accs', relative_tolerance: float = 0.1, min_acceptance_rate: float = 0.1, min_stable_acceptance: float = 0.01, acceptance_window: int = 0, n_loops_skip: int = 3, patience: int = 3, verbose: bool = False)</code>","text":"<p>Initialize the CheckEarlyStop strategy.</p> <p>Parameters:</p> Name Type Description Default <code>state_name</code> <code>str</code> <p>Name of the State resource (e.g., <code>\"sampler_state\"</code>).</p> required <code>acceptance_buffer_key</code> <code>str</code> <p>Key in the State that points to the global acceptance buffer name (e.g., <code>\"target_global_accs\"</code>).</p> <code>'target_global_accs'</code> <code>relative_tolerance</code> <code>float</code> <p>Relative change threshold. Early stopping triggers when <code>|current - prev| / max(prev, 1e-8) &lt; tol</code>.</p> <code>0.1</code> <code>min_acceptance_rate</code> <code>float</code> <p>Minimum global acceptance rate threshold. Early stopping triggers immediately when the current mean acceptance rate reaches or exceeds this value, regardless of stability. Set to 0 to disable this condition.</p> <code>0.1</code> <code>min_stable_acceptance</code> <code>float</code> <p>Minimum acceptance rate required for the stability condition to trigger early stop. If the acceptance is below this value the sampler is considered stuck near zero and the stable condition is suppressed. Defaults to 0.01.</p> <code>0.01</code> <code>acceptance_window</code> <code>int</code> <p>Number of most-recent thinned steps to use when computing the mean acceptance rate. If 0 (default), all available finite entries are used. Set this to <code>n_global_steps // global_thinning</code> to evaluate only the acceptance from the latest training loop iteration.</p> <code>0</code> <code>n_loops_skip</code> <code>int</code> <p>Minimum number of training loops before early stopping can trigger. Must be &gt;= 1 so that at least one comparison can be made.</p> <code>3</code> <code>patience</code> <code>int</code> <p>Number of consecutive loops where the acceptance rate must be stable before triggering early stop. Defaults to 3.</p> <code>3</code> <code>verbose</code> <code>bool</code> <p>Whether to log detailed information.</p> <code>False</code>","path":["Reference","flowMC","strategy","Check early stop"],"tags":[]},{"location":"api/flowMC/strategy/importance_sampling/","level":1,"title":"Importance sampling","text":"","path":["Reference","flowMC","strategy","Importance sampling"],"tags":[]},{"location":"api/flowMC/strategy/lambda_function/","level":1,"title":"Lambda function","text":"","path":["Reference","flowMC","strategy","Lambda function"],"tags":[]},{"location":"api/flowMC/strategy/lambda_function/#flowMC.strategy.lambda_function.Lambda","level":3,"title":"<code>Lambda</code>","text":"<p>               Bases: <code>Strategy</code></p> <p>A strategy that applies a function to the resources.</p> <p>This should be used for simple functions or calling methods from a class. If you find yourself writing a Lambda strategy that is more than a few lines long, consider writing a custom strategy instead.</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Initialize the lambda strategy.</p>","path":["Reference","flowMC","strategy","Lambda function"],"tags":[]},{"location":"api/flowMC/strategy/lambda_function/#flowMC.strategy.lambda_function.Lambda.__init__","level":4,"title":"<code>__init__(lambda_function: Callable)</code>","text":"<p>Initialize the lambda strategy.</p> <p>Parameters:</p> Name Type Description Default <code>lambda_function</code> <code>Callable</code> <p>A callable that takes a resource and applies the lambda function.</p> required","path":["Reference","flowMC","strategy","Lambda function"],"tags":[]},{"location":"api/flowMC/strategy/optimization/","level":1,"title":"Optimization","text":"","path":["Reference","flowMC","strategy","Optimization"],"tags":[]},{"location":"api/flowMC/strategy/optimization/#flowMC.strategy.optimization.AdamOptimization","level":3,"title":"<code>AdamOptimization</code>","text":"<p>               Bases: <code>Strategy</code></p> <p>Optimize a set of chains using Adam optimization.</p> <p>.. note::     If the posterior is unbounded, the optimization may return NaNs.</p> <p>Attributes:</p> Name Type Description <code>logpdf</code> <code>Callable</code> <p>Log-probability density function <code>f(x, data) -&gt; Float</code>.</p> <code>n_steps</code> <code>int</code> <p>Number of optimization steps.</p> <code>learning_rate</code> <code>float</code> <p>Adam learning rate.</p> <code>noise_level</code> <code>float</code> <p>Noise scale multiplied onto the gradient at each step.</p> <code>bounds</code> <code>Float[Array, 'n_dim 2']</code> <p>Box constraints; shape <code>(n_dim, 2)</code> or <code>(1, 2)</code> (broadcast to all dimensions).</p> <p>Methods:</p> Name Description <code>__call__</code> <p>Optimise all chains toward the mode of the log-PDF.</p> <code>__init__</code> <p>Args:</p> <code>optimize</code> <p>Optimization kernel that can be used independently of <code>__call__</code>.</p>","path":["Reference","flowMC","strategy","Optimization"],"tags":[]},{"location":"api/flowMC/strategy/optimization/#flowMC.strategy.optimization.AdamOptimization.__call__","level":4,"title":"<code>__call__(rng_key: Key, resources: dict[str, Resource], initial_position: Float[Array, 'n_chain n_dim'], data: dict) -&gt; tuple[Key, dict[str, Resource], Float[Array, 'n_chain n_dim']]</code>","text":"<p>Optimise all chains toward the mode of the log-PDF.</p> <p>Parameters:</p> Name Type Description Default <code>rng_key</code> <code>Key</code> <p>JAX PRNGKey (consumed and split internally).</p> required <code>resources</code> <code>dict[str, Resource]</code> <p>Resource dictionary (returned unchanged).</p> required <code>initial_position</code> <code>Float[Array, 'n_chain n_dim']</code> <p>Starting positions.</p> required <code>data</code> <code>dict</code> <p>Auxiliary data passed to the log-PDF.</p> required <p>Returns:</p> Name Type Description <code>tuple</code> <code>tuple[Key, dict[str, Resource], Float[Array, 'n_chain n_dim']]</code> <p><code>(rng_key, resources, optimized_positions)</code>.</p>","path":["Reference","flowMC","strategy","Optimization"],"tags":[]},{"location":"api/flowMC/strategy/optimization/#flowMC.strategy.optimization.AdamOptimization.__init__","level":4,"title":"<code>__init__(logpdf: Callable[[Float[Array, ' n_dim'], dict], Float], n_steps: int = 100, learning_rate: float = 0.01, noise_level: float = 10, bounds: Float[Array, 'n_dim 2'] = jnp.array([[-jnp.inf, jnp.inf]])) -&gt; None</code>","text":"<p>Parameters:</p> Name Type Description Default <code>logpdf</code> <code>Callable</code> <p>Log-PDF <code>f(x, data) -&gt; Float</code> to maximise.</p> required <code>n_steps</code> <code>int</code> <p>Number of Adam steps. Defaults to 100.</p> <code>100</code> <code>learning_rate</code> <code>float</code> <p>Adam learning rate. Defaults to 1e-2.</p> <code>0.01</code> <code>noise_level</code> <code>float</code> <p>Noise scale added to gradients each step. Defaults to 10.</p> <code>10</code> <code>bounds</code> <code>Float[Array, 'n_dim 2']</code> <p>Box constraints of shape <code>(n_dim, 2)</code> or <code>(1, 2)</code> (broadcast). Defaults to <code>[[-inf, inf]]</code> (unconstrained).</p> <code>array([[-inf, inf]])</code>","path":["Reference","flowMC","strategy","Optimization"],"tags":[]},{"location":"api/flowMC/strategy/optimization/#flowMC.strategy.optimization.AdamOptimization.optimize","level":4,"title":"<code>optimize(rng_key: Key, objective: Callable, initial_position: Float[Array, 'n_chain n_dim'], data: dict) -&gt; tuple[Key, Float[Array, 'n_chain n_dim'], Float[Array, ' n_chain']]</code>","text":"<p>Optimization kernel that can be used independently of <code>__call__</code>.</p> <p>Parameters:</p> Name Type Description Default <code>rng_key</code> <code>Key</code> <p>JAX PRNGKey for noise generation.</p> required <code>objective</code> <code>Callable</code> <p>Scalar-valued objective to minimise (pass the negated log-PDF to maximise it).</p> required <code>initial_position</code> <code>Float[Array, 'n_chain n_dim']</code> <p>Starting positions.</p> required <code>data</code> <code>dict</code> <p>Auxiliary data passed to the objective.</p> required <p>Returns:</p> Name Type Description <code>tuple</code> <code>tuple[Key, Float[Array, 'n_chain n_dim'], Float[Array, ' n_chain']]</code> <ul> <li>rng_key (Key): Updated PRNGKey.</li> <li>optimized_positions (Float[Array, \"n_chain n_dim\"]): Final positions.</li> <li>final_log_prob (Float[Array, \"n_chain\"]): Log-PDF at final positions.</li> </ul>","path":["Reference","flowMC","strategy","Optimization"],"tags":[]},{"location":"api/flowMC/strategy/parallel_tempering/","level":1,"title":"Parallel tempering","text":"","path":["Reference","flowMC","strategy","Parallel tempering"],"tags":[]},{"location":"api/flowMC/strategy/parallel_tempering/#flowMC.strategy.parallel_tempering.ParallelTempering","level":3,"title":"<code>ParallelTempering</code>","text":"<p>               Bases: <code>Strategy</code></p> <p>Sample a tempered PDF with one exchange step. This is in essence closer to TakeSteps than global tuning. Considering the tempered version of the PDF is only there to help with convergence, by default the extra information in temperature not equal to 1 is not saved.</p> <p>There should be a version of this class that saves the extra information in the temperature not equal to 1, which could be used for other purposes such as diagnostics or training.</p> <p>Methods:</p> Name Description <code>__call__</code> <p>Resources must contain:</p> <code>__init__</code> <p>Args:</p>","path":["Reference","flowMC","strategy","Parallel tempering"],"tags":[]},{"location":"api/flowMC/strategy/parallel_tempering/#flowMC.strategy.parallel_tempering.ParallelTempering.__call__","level":4,"title":"<code>__call__(rng_key: Key, resources: dict[str, Resource], initial_position: Float[Array, 'n_chains n_dims'], data: dict) -&gt; tuple[Key, dict[str, Resource], Float[Array, 'n_chains n_dim']]</code>","text":"Resources must contain <ul> <li>TemperedPDF</li> <li>Local kernel</li> <li>A buffer holding the tempered positions</li> <li>A buffer holding the temperatures</li> </ul> <p>This strategy has 3 main steps: 1. Sample from the tempered PDF using the local kernel for n_steps 2. Exchange the samples between the temperatures 3. Adapt the temperatures based on the acceptance rate</p> <p>TODO: Add way to turn of temperature adaptation to maintain detail balance.</p>","path":["Reference","flowMC","strategy","Parallel tempering"],"tags":[]},{"location":"api/flowMC/strategy/parallel_tempering/#flowMC.strategy.parallel_tempering.ParallelTempering.__init__","level":4,"title":"<code>__init__(n_steps: int, tempered_logpdf_name: str, kernel_name: str, tempered_buffer_names: list[str], state_name: str, verbose: bool = False) -&gt; None</code>","text":"<p>Parameters:</p> Name Type Description Default <code>n_steps</code> <code>int</code> <p>Number of local kernel steps per temperature per call.</p> required <code>tempered_logpdf_name</code> <code>str</code> <p>Resource key for the <code>flowMC.resource.logPDF.TemperedPDF</code>.</p> required <code>kernel_name</code> <code>str</code> <p>Resource key for the local proposal kernel.</p> required <code>tempered_buffer_names</code> <code>list[str]</code> <p>Resource keys for the tempered-position buffer and the temperature buffer (in that order).</p> required <code>state_name</code> <code>str</code> <p>Resource key for the sampler <code>flowMC.resource.states.State</code>.</p> required <code>verbose</code> <code>bool</code> <p>Enable debug logging. Defaults to False.</p> <code>False</code>","path":["Reference","flowMC","strategy","Parallel tempering"],"tags":[]},{"location":"api/flowMC/strategy/sequential_monte_carlo/","level":1,"title":"Sequential monte carlo","text":"","path":["Reference","flowMC","strategy","Sequential monte carlo"],"tags":[]},{"location":"api/flowMC/strategy/take_steps/","level":1,"title":"Take steps","text":"","path":["Reference","flowMC","strategy","Take steps"],"tags":[]},{"location":"api/flowMC/strategy/take_steps/#flowMC.strategy.take_steps.TakeGroupSteps","level":3,"title":"<code>TakeGroupSteps</code>","text":"<p>               Bases: <code>TakeSteps</code></p> <p>TakeGroupSteps is a strategy that takes a number of steps in a group manner, i.e. all steps are taken at once.</p> <p>This is intended to be used for kernels such as normalizing flow, which proposal steps are independent of each other, and benefit from being computed in parallel.</p> <p>Methods:</p> Name Description <code>sample</code> <p>Advance a single chain by running all steps simultaneously via the kernel.</p>","path":["Reference","flowMC","strategy","Take steps"],"tags":[]},{"location":"api/flowMC/strategy/take_steps/#flowMC.strategy.take_steps.TakeGroupSteps.sample","level":4,"title":"<code>sample(kernel: ProposalBase, rng_key: Key, initial_position: Float[Array, ' n_dim'], logpdf: LogPDF, data: dict) -&gt; tuple[Float[Array, 'n_steps n_dim'], Float[Array, ' n_steps'], Float[Array, ' n_steps']]</code>","text":"<p>Advance a single chain by running all steps simultaneously via the kernel.</p> <p>The kernel is called once with <code>n_steps</code> injected into <code>data</code>, so all proposals are generated in a single batched call — no sequential dependency.</p> <p>Parameters:</p> Name Type Description Default <code>kernel</code> <code>ProposalBase</code> <p>Proposal kernel (e.g., <code>flowMC.resource.kernel.NF_proposal.NFProposal</code>).</p> required <code>rng_key</code> <code>Key</code> <p>JAX PRNGKey for this chain.</p> required <code>initial_position</code> <code>Float[Array, n_dim]</code> <p>Starting position.</p> required <code>logpdf</code> <code>LogPDF</code> <p>Log-PDF to sample from.</p> required <code>data</code> <code>dict</code> <p>Auxiliary data passed to the log-PDF.</p> required <p>Returns:</p> Name Type Description <code>tuple</code> <code>tuple[Float[Array, 'n_steps n_dim'], Float[Array, ' n_steps'], Float[Array, ' n_steps']]</code> <p><code>(positions, log_probs, do_accepts)</code> each of length <code>n_steps</code>.</p>","path":["Reference","flowMC","strategy","Take steps"],"tags":[]},{"location":"api/flowMC/strategy/take_steps/#flowMC.strategy.take_steps.TakeSerialSteps","level":3,"title":"<code>TakeSerialSteps</code>","text":"<p>               Bases: <code>TakeSteps</code></p> <p>TakeSerialSteps is a strategy that takes a number of steps in a serial manner, i.e. one after the other.</p> <p>This uses jax.lax.scan to iterate over the steps and apply the kernel to the current position. This is intended to be used for most local kernels that are dependent on the previous step.</p> <p>Methods:</p> Name Description <code>body</code> <p>Single scan body: advance position by one kernel step.</p> <code>sample</code> <p>Advance a single chain serially using <code>jax.lax.scan</code>.</p>","path":["Reference","flowMC","strategy","Take steps"],"tags":[]},{"location":"api/flowMC/strategy/take_steps/#flowMC.strategy.take_steps.TakeSerialSteps.body","level":4,"title":"<code>body(kernel: ProposalBase, carry: tuple, aux: None) -&gt; tuple[tuple, tuple]</code>","text":"<p>Single scan body: advance position by one kernel step.</p> <p>Parameters:</p> Name Type Description Default <code>kernel</code> <code>ProposalBase</code> <p>Proposal kernel.</p> required <code>carry</code> <code>tuple</code> <p><code>(key, position, log_prob, logpdf, data)</code>.</p> required <code>aux</code> <code>None</code> <p>Unused scan auxiliary input.</p> required <p>Returns:</p> Name Type Description <code>tuple</code> <code>tuple[tuple, tuple]</code> <p>Updated carry and <code>(position, log_prob, do_accept)</code> outputs.</p>","path":["Reference","flowMC","strategy","Take steps"],"tags":[]},{"location":"api/flowMC/strategy/take_steps/#flowMC.strategy.take_steps.TakeSerialSteps.sample","level":4,"title":"<code>sample(kernel: ProposalBase, rng_key: Key, initial_position: Float[Array, ' n_dim'], logpdf: LogPDF, data: dict) -&gt; tuple[Float[Array, 'n_steps n_dim'], Float[Array, ' n_steps'], Float[Array, ' n_steps']]</code>","text":"<p>Advance a single chain serially using <code>jax.lax.scan</code>.</p> <p>Parameters:</p> Name Type Description Default <code>kernel</code> <code>ProposalBase</code> <p>Proposal kernel.</p> required <code>rng_key</code> <code>Key</code> <p>JAX PRNGKey for this chain.</p> required <code>initial_position</code> <code>Float[Array, n_dim]</code> <p>Starting position.</p> required <code>logpdf</code> <code>LogPDF</code> <p>Log-PDF to sample from.</p> required <code>data</code> <code>dict</code> <p>Auxiliary data passed to the log-PDF.</p> required <p>Returns:</p> Name Type Description <code>tuple</code> <code>tuple[Float[Array, 'n_steps n_dim'], Float[Array, ' n_steps'], Float[Array, ' n_steps']]</code> <p><code>(positions, log_probs, do_accepts)</code> each of length <code>n_steps</code>.</p>","path":["Reference","flowMC","strategy","Take steps"],"tags":[]},{"location":"api/flowMC/strategy/take_steps/#flowMC.strategy.take_steps.TakeSteps","level":3,"title":"<code>TakeSteps</code>","text":"<p>               Bases: <code>Strategy</code></p> <p>Base class for strategies that run a kernel for a fixed number of steps.</p> <p>Subclasses implement <code>sample</code> to define how a single chain is advanced, and this base class handles vmapping over chains, thinning, and writing results into the associated <code>flowMC.resource.buffers.Buffer</code> resources.</p> <p>Attributes:</p> Name Type Description <code>logpdf_name</code> <code>str</code> <p>Resource key for the <code>flowMC.resource.logPDF.LogPDF</code>.</p> <code>kernel_name</code> <code>str</code> <p>Resource key for the proposal kernel.</p> <code>state_name</code> <code>str</code> <p>Resource key for the sampler <code>flowMC.resource.states.State</code>.</p> <code>buffer_names</code> <code>list[str]</code> <p>State keys pointing to the position, log-prob, and acceptance-rate buffer names (in that order).</p> <code>n_steps</code> <code>int</code> <p>Number of kernel steps per call.</p> <code>current_position</code> <code>int</code> <p>Write cursor along the buffer's <code>cursor_dim</code>.</p> <code>thinning</code> <code>int</code> <p>Store every <code>thinning</code>-th step.</p> <code>chain_batch_size</code> <code>int</code> <p>Number of chains per vmap batch; 0 means no batching.</p> <p>Methods:</p> Name Description <code>__call__</code> <p>Run the kernel for all chains, apply thinning, and update buffers.</p> <code>__init__</code> <p>Args:</p> <code>sample</code> <p>Advance a single chain for <code>n_steps</code> using the given kernel.</p> <code>set_current_position</code> <p>Set the write cursor position for the output buffers.</p>","path":["Reference","flowMC","strategy","Take steps"],"tags":[]},{"location":"api/flowMC/strategy/take_steps/#flowMC.strategy.take_steps.TakeSteps.__call__","level":4,"title":"<code>__call__(rng_key: Key, resources: dict[str, Resource], initial_position: Float[Array, 'n_chains n_dim'], data: dict) -&gt; tuple[Key, dict[str, Resource], Float[Array, 'n_chains n_dim']]</code>","text":"<p>Run the kernel for all chains, apply thinning, and update buffers.</p> <p>Parameters:</p> Name Type Description Default <code>rng_key</code> <code>Key</code> <p>JAX PRNGKey (consumed and split internally).</p> required <code>resources</code> <code>dict[str, Resource]</code> <p>Mutable resource dictionary.</p> required <code>initial_position</code> <code>Float[Array, 'n_chains n_dim']</code> <p>Current chain positions.</p> required <code>data</code> <code>dict</code> <p>Auxiliary data passed to log-PDF calls.</p> required <p>Returns:</p> Name Type Description <code>tuple</code> <code>Key</code> <p><code>(rng_key, resources, last_positions)</code> where <code>last_positions</code></p> <code>dict[str, Resource]</code> <p>has shape <code>(n_chains, n_dim)</code>.</p>","path":["Reference","flowMC","strategy","Take steps"],"tags":[]},{"location":"api/flowMC/strategy/take_steps/#flowMC.strategy.take_steps.TakeSteps.__init__","level":4,"title":"<code>__init__(logpdf_name: str, kernel_name: str, state_name: str, buffer_names: list[str], n_steps: int, thinning: int = 1, chain_batch_size: int = 0, verbose: bool = False) -&gt; None</code>","text":"<p>Parameters:</p> Name Type Description Default <code>logpdf_name</code> <code>str</code> <p>Resource key for the log-PDF to sample from.</p> required <code>kernel_name</code> <code>str</code> <p>Resource key for the proposal kernel.</p> required <code>state_name</code> <code>str</code> <p>Resource key for the sampler state.</p> required <code>buffer_names</code> <code>list[str]</code> <p>List of three state keys that resolve to the buffer resource names for positions, log-probs, and acceptance rates.</p> required <code>n_steps</code> <code>int</code> <p>Number of kernel steps to take per call.</p> required <code>thinning</code> <code>int</code> <p>Keep every <code>thinning</code>-th step. Defaults to 1.</p> <code>1</code> <code>chain_batch_size</code> <code>int</code> <p>If &gt; 1, process chains in sub-batches of this size to reduce peak memory. 0 disables batching. Defaults to 0.</p> <code>0</code> <code>verbose</code> <code>bool</code> <p>Enable debug logging. Defaults to False.</p> <code>False</code>","path":["Reference","flowMC","strategy","Take steps"],"tags":[]},{"location":"api/flowMC/strategy/take_steps/#flowMC.strategy.take_steps.TakeSteps.sample","level":4,"title":"<code>sample(kernel: ProposalBase, rng_key: Key, initial_position: Float[Array, ' n_dim'], logpdf: LogPDF, data: dict) -&gt; tuple[Float[Array, 'n_steps n_dim'], Float[Array, ' n_steps'], Float[Array, ' n_steps']]</code>  <small><code>abstractmethod</code></small>","text":"<p>Advance a single chain for <code>n_steps</code> using the given kernel.</p> <p>This method is vmapped over chains by <code>__call__</code>, so it operates on a single chain at a time.</p> <p>Parameters:</p> Name Type Description Default <code>kernel</code> <code>ProposalBase</code> <p>Proposal kernel.</p> required <code>rng_key</code> <code>Key</code> <p>JAX PRNGKey for this chain.</p> required <code>initial_position</code> <code>Float[Array, n_dim]</code> <p>Starting position.</p> required <code>logpdf</code> <code>LogPDF</code> <p>Log-PDF to sample from.</p> required <code>data</code> <code>dict</code> <p>Auxiliary data passed to the log-PDF.</p> required <p>Returns:</p> Name Type Description <code>tuple</code> <code>tuple[Float[Array, 'n_steps n_dim'], Float[Array, ' n_steps'], Float[Array, ' n_steps']]</code> <p><code>(positions, log_probs, do_accepts)</code> each of length <code>n_steps</code>.</p>","path":["Reference","flowMC","strategy","Take steps"],"tags":[]},{"location":"api/flowMC/strategy/take_steps/#flowMC.strategy.take_steps.TakeSteps.set_current_position","level":4,"title":"<code>set_current_position(current_position: int) -&gt; None</code>","text":"<p>Set the write cursor position for the output buffers.</p> <p>Parameters:</p> Name Type Description Default <code>current_position</code> <code>int</code> <p>New cursor value along the buffer's step dimension.</p> required","path":["Reference","flowMC","strategy","Take steps"],"tags":[]},{"location":"api/flowMC/strategy/train_model/","level":1,"title":"Train model","text":"","path":["Reference","flowMC","strategy","Train model"],"tags":[]},{"location":"api/flowMC/strategy/train_model/#flowMC.strategy.train_model.TrainModel","level":3,"title":"<code>TrainModel</code>","text":"<p>               Bases: <code>Strategy</code></p> <p>Strategy that trains a normalizing flow model on buffered chain positions.</p> <p>Each call draws the most-recent <code>history_window</code> steps from the position buffer, sub-samples up to <code>n_max_examples</code> points, and runs <code>n_epochs</code> of gradient descent via the associated <code>flowMC.resource.optimizer.Optimizer</code>.</p> <p>Attributes:</p> Name Type Description <code>model_resource</code> <code>str</code> <p>Resource key for the <code>flowMC.resource.model.nf_model.base.NFModel</code>.</p> <code>data_resource</code> <code>str</code> <p>Resource key for the position <code>flowMC.resource.buffers.Buffer</code>.</p> <code>optimizer_resource</code> <code>str</code> <p>Resource key for the <code>flowMC.resource.optimizer.Optimizer</code>.</p> <code>loss_buffer_name</code> <code>str</code> <p>Resource key for an optional <code>flowMC.resource.buffers.Buffer</code> to record loss values. Empty string disables loss recording.</p> <code>n_epochs</code> <code>int</code> <p>Number of training epochs per call.</p> <code>batch_size</code> <code>int</code> <p>Mini-batch size.</p> <code>n_max_examples</code> <code>int</code> <p>Maximum number of training examples sampled per call.</p> <code>history_window</code> <code>int</code> <p>Number of most-recent steps from the buffer used as training data.</p> <code>verbose</code> <code>bool</code> <p>Whether to print a training progress bar.</p> <p>Methods:</p> Name Description <code>__init__</code> <p>Args:</p>","path":["Reference","flowMC","strategy","Train model"],"tags":[]},{"location":"api/flowMC/strategy/train_model/#flowMC.strategy.train_model.TrainModel.__init__","level":4,"title":"<code>__init__(model_resource: str, data_resource: str, optimizer_resource: str, loss_buffer_name: str = '', n_epochs: int = 100, batch_size: int = 64, n_max_examples: int = 10000, history_window: int = 100, verbose: bool = False) -&gt; None</code>","text":"<p>Parameters:</p> Name Type Description Default <code>model_resource</code> <code>str</code> <p>Resource key for the normalizing flow model.</p> required <code>data_resource</code> <code>str</code> <p>Resource key for the position buffer used as training data.</p> required <code>optimizer_resource</code> <code>str</code> <p>Resource key for the optimizer.</p> required <code>loss_buffer_name</code> <code>str</code> <p>Resource key for an optional <code>flowMC.resource.buffers.Buffer</code> to record loss values. Pass <code>\"\"</code> to disable. Defaults to <code>\"\"</code>.</p> <code>''</code> <code>n_epochs</code> <code>int</code> <p>Number of training epochs per call. Defaults to 100.</p> <code>100</code> <code>batch_size</code> <code>int</code> <p>Mini-batch size. Defaults to 64.</p> <code>64</code> <code>n_max_examples</code> <code>int</code> <p>Cap on training examples drawn per call. Defaults to 10000.</p> <code>10000</code> <code>history_window</code> <code>int</code> <p>Use only the last <code>history_window</code> thinned steps from the buffer. Defaults to 100.</p> <code>100</code> <code>verbose</code> <code>bool</code> <p>Print training progress bar. Defaults to False.</p> <code>False</code>","path":["Reference","flowMC","strategy","Train model"],"tags":[]},{"location":"api/flowMC/strategy/update_state/","level":1,"title":"Update state","text":"","path":["Reference","flowMC","strategy","Update state"],"tags":[]},{"location":"api/flowMC/strategy/update_state/#flowMC.strategy.update_state.UpdateState","level":3,"title":"<code>UpdateState</code>","text":"<p>               Bases: <code>Strategy</code></p> <p>Update a state resource in place.</p> <p>This strategy is meant to be used to update the state not too frequently. If you are looking for an option that iterates over some parameters, say the paramters of a neural network, you should write a custom strategy that does that.</p> <p>Methods:</p> Name Description <code>__call__</code> <p>Update the state resource in place.</p> <code>__init__</code> <p>Initialize the update state strategy.</p>","path":["Reference","flowMC","strategy","Update state"],"tags":[]},{"location":"api/flowMC/strategy/update_state/#flowMC.strategy.update_state.UpdateState.__call__","level":4,"title":"<code>__call__(rng_key: Key, resources: dict[str, Resource], initial_position: Float[Array, 'n_chains n_dim'], data: dict) -&gt; tuple[Key, dict[str, Resource], Float[Array, 'n_chains n_dim']]</code>","text":"<p>Update the state resource in place.</p>","path":["Reference","flowMC","strategy","Update state"],"tags":[]},{"location":"api/flowMC/strategy/update_state/#flowMC.strategy.update_state.UpdateState.__init__","level":4,"title":"<code>__init__(state_name: str, keys: list[str], values: list[int | bool | str])</code>","text":"<p>Initialize the update state strategy.</p> <p>Parameters:</p> Name Type Description Default <code>state_name</code> <code>str</code> <p>The name of the state resource to update.</p> required <code>keys</code> <code>list[str]</code> <p>The keys to update in the state resource.</p> required <code>values</code> <code>list[int | bool | str]</code> <p>The values to update in the state resource.</p> required","path":["Reference","flowMC","strategy","Update state"],"tags":[]},{"location":"api/flowMC/utils/logging/","level":1,"title":"Logging","text":"<p>Centralized logging utilities for flowMC.</p> <p>This module provides utilities to configure logging for flowMC components in a consistent and isolated manner.</p> <p>Functions:</p> Name Description <code>disable_verbose_logging</code> <p>Disable verbose logging and restore default behavior.</p> <code>enable_verbose_logging</code> <p>Enable verbose logging for a specific logger.</p>","path":["Reference","flowMC","utils","Logging"],"tags":[]},{"location":"api/flowMC/utils/logging/#flowMC.utils.logging.disable_verbose_logging","level":3,"title":"<code>disable_verbose_logging(logger: logging.Logger) -&gt; None</code>","text":"<p>Disable verbose logging and restore default behavior.</p> <p>This function resets a logger to its default state: - Resets the logger's level to NOTSET (inherits from parent) - Re-enables propagation to parent loggers - Removes any handlers that were added</p> <p>Parameters:</p> Name Type Description Default <code>logger</code> <code>Logger</code> <p>The logger instance to reset.</p> required","path":["Reference","flowMC","utils","Logging"],"tags":[]},{"location":"api/flowMC/utils/logging/#flowMC.utils.logging.enable_verbose_logging","level":3,"title":"<code>enable_verbose_logging(logger: logging.Logger, level: int = logging.DEBUG, format_string: Optional[str] = None) -&gt; None</code>","text":"<p>Enable verbose logging for a specific logger.</p> <p>This function configures a logger to output debug messages in an isolated way, without affecting other loggers in the application. It: - Sets the logger's level to the specified level (default: DEBUG) - Disables propagation to parent loggers to avoid interference - Adds a StreamHandler if one doesn't already exist</p> <p>Parameters:</p> Name Type Description Default <code>logger</code> <code>Logger</code> <p>The logger instance to configure.</p> required <code>level</code> <code>int</code> <p>The logging level to set (default: logging.DEBUG).</p> <code>DEBUG</code> <code>format_string</code> <code>Optional[str]</code> <p>Custom format string for log messages. If None, uses a default format: '%(levelname)s | %(name)s | %(message)s'</p> <code>None</code> Example <p>import logging from flowMC.utils.logging import enable_verbose_logging</p> <p>logger = logging.getLogger(name) enable_verbose_logging(logger) logger.debug(\"This will now be printed\")</p>","path":["Reference","flowMC","utils","Logging"],"tags":[]},{"location":"guides/","level":1,"title":"Guides","text":"<p>These guides cover each flowMC component in depth — what it does and how to use it.</p> <ul> <li>Architecture — How flowMC is designed internally: the resource-strategy API, the <code>Sampler</code> control loop, and the guiding principles behind the design.</li> <li>Bundles — Reference guide for the built-in resource-strategy bundles (<code>RQSpline_MALA</code>, <code>RQSpline_HMC</code>, <code>RQSpline_GRW</code>, and their parallel-tempering variants). Explains what each bundle does, when to choose it, and the structure of the training and production sampling loops.</li> <li>Hyperparameter Reference — Complete reference for every constructor parameter across all bundles, organised into categories (required, local sampler, normalising flow, training, execution, early stopping, parallel tempering).</li> </ul>","path":["Guides"],"tags":[]},{"location":"guides/architecture/","level":1,"title":"Architecture","text":"<p>This guide describes the internal architecture of flowMC — how its components are designed, how they interact, and how to extend them for your own use case.</p>","path":["Guides","Architecture"],"tags":[]},{"location":"guides/architecture/#anatomy-of-flowmc","level":2,"title":"Anatomy of flowMC","text":"<p>Prior to version 0.4.0, <code>flowMC</code> was a package that was designed to execute the algorithm detailed in this paper. Since then the community has tried applying <code>flowMC</code> to different problems. While there were some successes, there are also limiting factors in terms of performance. One of the biggest issues <code>flowMC</code> faced is the fact that the global-local sampling algorithm was baked into the top level <code>Sampler</code> API, which means <code>flowMC</code> can only use the exact algorithm described in the paper. What if the users want to use a different model? Or run some optimisation steps during the sampling stage? Or apply annealing? These are either impossible or not very intuitive in <code>flowMC</code> prior to version 0.4.0.</p> <p>Seeing this limitation, we redesigned the middle level API of <code>flowMC</code> while keeping the top level API as similar as possible.</p>","path":["Guides","Architecture"],"tags":[]},{"location":"guides/architecture/#target-distribution","level":3,"title":"Target distribution","text":"<p>The target distribution should be defined as a log-probability density function, which follows the following function signature:</p> <pre><code>def target_log_prob_fn(x: Array, data: dict) -&gt; Float:\n    ...\n    return log_prob\n</code></pre> <p>The <code>target_log_prob_fn</code> should take in a 1-D array <code>x</code> of length <code>n_dim</code> and a dictionary <code>data</code> that contains any additional data that the target distribution depends on. The function should return a scalar that is the log-probability density of the target distribution at <code>x</code>.</p> <p>To ensure the target distribution is well-defined and performant, you should also check whether the function is behaving as expected when <code>jax.jit</code> and <code>jax.grad</code> are applied to it.</p>","path":["Guides","Architecture"],"tags":[]},{"location":"guides/architecture/#sampler","level":3,"title":"Sampler","text":"<p>On the top level, the <code>Sampler</code> class is a thin wrapper on top of the resource-strategy pair (defined below) that provides a couple of extra functionality. The <code>Sampler</code> class manages the resources and strategies, as well as run-related parameters such as where would the resources be stored if the user decides to serialise the resources.</p> <pre><code>nf_sampler = Sampler(\n    n_dim=n_dim,\n    n_chains=n_chains,\n    rng_key=rng_key,\n    # you can either supply the resources and strategies directly,\n    # which is prioritized over the resource-strategy bundles\n    resources=resources,\n    strategies=strategies,\n    strategy_order=strategy_order,\n    # or you can supply the resource-strategy bundles\n    resource_strategy_bundles=bundle,\n)\n</code></pre> <p>The main loop of <code>Sampler</code> is straightforward after initialisation: given the available resources, it iterates through the list of strategies, each of which takes the resources, performs some actions (such as taking local steps or training a normalising flow), and returns the updated resources. The <code>Sampler</code> supports early stopping: if a <code>State</code> resource sets an <code>early_stopped</code> flag during training, the main loop skips the remaining training strategies and jumps directly to the production phase.</p>","path":["Guides","Architecture"],"tags":[]},{"location":"guides/architecture/#resource-and-strategy","level":3,"title":"Resource and Strategy","text":"<p>At the core of the flowMC API are the resource and strategy interfaces. Broadly speaking, resources are similar to data classes and strategies are similar to functions.</p> <p>Resources store some attribute and can be manipulated, but should not have too many methods associated with them. For example, a buffer that stores the sampling results is a resource, a MALA kernel is a resource, and a normalising flow model is a resource. Strategies are functions that take in resources and return updated resources. For example, taking a local step requires two kinds of resources: a proposal distribution and the buffer where the samples are stored.</p> <p>If you are initialising the resources and strategies directly, you can do something like:</p> <pre><code>resources = {\n    \"buffer\": Buffer(name, n_chains, n_steps, n_dims),\n    \"proposal\": MALA(step_size),\n    \"model\": NormalizingFlow(model_parameters),\n}\n\nstrategies = {\n    \"Strategy 1\": Strategy1(),\n    \"Strategy 2\": Strategy2(),\n}\n\nstrategy_order = [\"Strategy 1\", \"Strategy 2\", \"Strategy 1\", ...]\n</code></pre> <p>This separation allows users to compose different strategies together. For example, a strategy can take in both a MALA kernel and a normalising flow model, and update the MALA step size using information from the flow — without that logic being hard-coded into either component.</p> <p>A good rule of thumb for deciding whether something should be a resource or a strategy: ask whether the new data structure/functionality is something that should be updated by other strategies. If yes, it should be a resource; if no, it should be a strategy.</p> <p>One extra criterion is JAX compatibility. Resources should be compatible with <code>jit</code>; strategies are not required to be. For example, a training loop that iterates over epochs and logs metadata does not need to be jitted — it should be a strategy. A neural network that runs on GPU during both sampling and training should be a resource.</p> <p>You can find the hyperparameters of a resource, a strategy, or a resource-strategy bundle in the API docs.</p>","path":["Guides","Architecture"],"tags":[]},{"location":"guides/architecture/#guiding-principles","level":2,"title":"Guiding principles","text":"","path":["Guides","Architecture"],"tags":[]},{"location":"guides/architecture/#write-the-likelihood-function-in-jax","level":3,"title":"Write the likelihood function in JAX","text":"<p>If your likelihood is fully defined in JAX, there are a couple of benefits that compound with each other:</p> <ol> <li>JAX allows you to access the gradient of the likelihood function through automatic differentiation. This enables gradient-based local samplers such as MALA and HMC, which handle high-dimensional problems more efficiently than gradient-free alternatives like Metropolis-Hastings.</li> <li>JAX uses XLA to compile and optimise code for GPUs and TPUs. Multiple MCMC chains help speed up normalising flow training, and accelerators scale this further.</li> </ol> <p>Since version 0.4.0, flowMC requires likelihood functions to be compatible with JAX transformations. flowMC is designed to leverage GPU acceleration and machine learning methods, and rewriting a likelihood in JAX is often worthwhile on its own for the speedup alone.</p>","path":["Guides","Architecture"],"tags":[]},{"location":"guides/architecture/#parallelise-whenever-you-can","level":3,"title":"Parallelise whenever you can","text":"<p>Resource and strategy design should centre on leveraging parallelisation. This is reflected by <code>n_chains</code> being a required parameter for the <code>Sampler</code> class — flowMC is designed to solve problems with complex geometry using adaptive sampling methods that benefit tremendously from multiple chains running in parallel.</p>","path":["Guides","Architecture"],"tags":[]},{"location":"guides/bundles/","level":1,"title":"Resource-Strategy Bundles","text":"<p>A resource-strategy bundle packages a local MCMC sampler, a normalizing flow global proposal, and a training schedule into a single object. You pass it to <code>Sampler</code> and it configures everything for you.</p> <pre><code>from flowMC.Sampler import Sampler\n\nsampler = Sampler(\n    n_dim=n_dims,\n    n_chains=n_chains,\n    rng_key=rng_key,\n    resource_strategy_bundles=bundle,\n)\nsampler.sample(initial_positions, data)\n</code></pre>","path":["Guides","Resource-Strategy Bundles"],"tags":[]},{"location":"guides/bundles/#available-bundles","level":2,"title":"Available bundles","text":"Bundle Local sampler Parallel tempering <code>RQSpline_MALA_Bundle</code> MALA No <code>RQSpline_MALA_PT_Bundle</code> MALA Yes <code>RQSpline_HMC_Bundle</code> HMC No <code>RQSpline_HMC_PT_Bundle</code> HMC Yes <code>RQSpline_GRW_Bundle</code> Gaussian random walk No <code>RQSpline_GRW_PT_Bundle</code> Gaussian random walk Yes <p>All bundles are importable from <code>flowMC.resource_strategy_bundle</code>.</p>","path":["Guides","Resource-Strategy Bundles"],"tags":[]},{"location":"guides/bundles/#rqspline_mala_bundle","level":2,"title":"RQSpline_MALA_Bundle","text":"<p>Uses the Metropolis-Adjusted Langevin Algorithm (MALA) as the local sampler. MALA uses the gradient of the log-density to bias proposals toward regions of higher probability, giving better performance than a simple random walk at the cost of requiring a differentiable <code>logpdf</code>.</p> <pre><code>from flowMC.resource_strategy_bundle.RQSpline_MALA import RQSpline_MALA_Bundle\n\nbundle = RQSpline_MALA_Bundle(\n    rng_key=rng_key,\n    n_chains=500,\n    n_dims=n_dims,\n    logpdf=logpdf,\n    n_local_steps=25,\n    n_global_steps=25,\n    n_training_loops=20,\n    n_production_loops=10,\n    n_epochs=30,\n)\n</code></pre> <p>The local step size targets an acceptance rate of ~57% and is adapted automatically during the training phase when <code>adapt_step_size=True</code> (the default).</p>","path":["Guides","Resource-Strategy Bundles"],"tags":[]},{"location":"guides/bundles/#rqspline_mala_pt_bundle","level":2,"title":"RQSpline_MALA_PT_Bundle","text":"<p>Extends <code>RQSpline_MALA_Bundle</code> with parallel tempering. Additional replicas of the chains are run at elevated temperatures, exploring the prior more freely. Periodic swap proposals between adjacent temperature levels allow the target chains to escape local modes.</p> <pre><code>from flowMC.resource_strategy_bundle.RQSpline_MALA_PT import RQSpline_MALA_PT_Bundle\n\nbundle = RQSpline_MALA_PT_Bundle(\n    rng_key=rng_key,\n    n_chains=500,\n    n_dims=n_dims,\n    logpdf=logpdf,\n    n_local_steps=25,\n    n_global_steps=25,\n    n_training_loops=20,\n    n_production_loops=10,\n    n_epochs=30,\n    # Parallel tempering\n    logprior=logprior,\n    n_temperatures=5,\n    max_temperature=5.0,\n)\n</code></pre> <p>The tempered log-density at temperature \\(T\\) is</p> \\[\\log \\tilde{p}_T(x) = \\frac{1}{T} \\log \\mathcal{L}(x) + \\log \\pi(x)\\] <p>so the prior is preserved at all temperatures.</p>","path":["Guides","Resource-Strategy Bundles"],"tags":[]},{"location":"guides/bundles/#rqspline_hmc_bundle","level":2,"title":"RQSpline_HMC_Bundle","text":"<p>Uses Hamiltonian Monte Carlo (HMC) as the local sampler. HMC integrates the Hamiltonian equations of motion using a leapfrog integrator, producing proposals that travel far in parameter space with high acceptance. It requires a differentiable <code>logpdf</code> and benefits from a good <code>condition_matrix</code> (diagonal inverse mass matrix).</p> <pre><code>from flowMC.resource_strategy_bundle.RQSpline_HMC import RQSpline_HMC_Bundle\n\nbundle = RQSpline_HMC_Bundle(\n    rng_key=rng_key,\n    n_chains=500,\n    n_dims=n_dims,\n    logpdf=logpdf,\n    n_local_steps=25,\n    n_global_steps=25,\n    n_training_loops=20,\n    n_production_loops=10,\n    n_epochs=30,\n    hmc_step_size=0.1,\n    hmc_n_leapfrog=10,\n    condition_matrix=1,   # or a 1-D array of per-dimension scales\n)\n</code></pre> <p>The local step size targets an acceptance rate of ~65%.</p>","path":["Guides","Resource-Strategy Bundles"],"tags":[]},{"location":"guides/bundles/#rqspline_hmc_pt_bundle","level":2,"title":"RQSpline_HMC_PT_Bundle","text":"<p>Extends <code>RQSpline_HMC_Bundle</code> with parallel tempering. The constructor accepts all <code>RQSpline_HMC_Bundle</code> parameters plus the parallel tempering parameters (<code>logprior</code>, <code>n_temperatures</code>, <code>max_temperature</code>, <code>n_tempered_steps</code>).</p> <pre><code>from flowMC.resource_strategy_bundle.RQSpline_HMC_PT import RQSpline_HMC_PT_Bundle\n</code></pre>","path":["Guides","Resource-Strategy Bundles"],"tags":[]},{"location":"guides/bundles/#rqspline_grw_bundle","level":2,"title":"RQSpline_GRW_Bundle","text":"<p>Uses a Gaussian random walk (Metropolis–Hastings) as the local sampler. No gradient is required, making this the right choice when <code>logpdf</code> is not differentiable. The optimal acceptance rate for a Gaussian random walk is ~23%.</p> <pre><code>from flowMC.resource_strategy_bundle.RQSpline_GRW import RQSpline_GRW_Bundle\n\nbundle = RQSpline_GRW_Bundle(\n    rng_key=rng_key,\n    n_chains=500,\n    n_dims=n_dims,\n    logpdf=logpdf,\n    n_local_steps=25,\n    n_global_steps=25,\n    n_training_loops=20,\n    n_production_loops=10,\n    n_epochs=30,\n    grw_step_size=0.1,\n)\n</code></pre>","path":["Guides","Resource-Strategy Bundles"],"tags":[]},{"location":"guides/bundles/#rqspline_grw_pt_bundle","level":2,"title":"RQSpline_GRW_PT_Bundle","text":"<p>Extends <code>RQSpline_GRW_Bundle</code> with parallel tempering.</p> <pre><code>from flowMC.resource_strategy_bundle.RQSpline_GRW_PT import RQSpline_GRW_PT_Bundle\n</code></pre>","path":["Guides","Resource-Strategy Bundles"],"tags":[]},{"location":"guides/bundles/#sampling-loop-structure","level":2,"title":"Sampling loop structure","text":"<p>Every bundle organises sampling into a training phase followed by a production phase.</p> <p>Training phase (<code>n_training_loops</code> iterations):</p> <ol> <li>Run <code>n_local_steps</code> local MCMC steps per chain</li> <li>(Optional) Adapt the local step size</li> <li>Run <code>n_global_steps</code> NF proposal steps per chain</li> <li>Train the normalizing flow for <code>n_epochs</code> on accumulated samples</li> <li>(PT bundles) Attempt parallel-tempering swaps at the start of each loop</li> </ol> <p>Production phase (<code>n_production_loops</code> iterations):</p> <ol> <li>Run <code>n_local_steps</code> local MCMC steps per chain</li> <li>Run <code>n_global_steps</code> NF proposal steps per chain</li> <li>(PT bundles) Attempt parallel-tempering swaps at the start of each loop</li> </ol> <p>The flow is not updated during the production phase, so detailed balance is restored and standard MCMC convergence diagnostics can be applied safely.</p> <p>Early stopping can terminate the training phase early once the global acceptance rate has stabilised; see <code>early_stopping</code>.</p>","path":["Guides","Resource-Strategy Bundles"],"tags":[]},{"location":"guides/hyperparameters/","level":1,"title":"Hyperparameter Reference","text":"<p>Quick-reference index by category. Click any parameter name to jump to its description.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#required","level":2,"title":"Required","text":"Parameter Description <code>rng_key</code> JAX PRNG key <code>n_chains</code> Number of parallel chains <code>n_dims</code> Dimensionality of the parameter space <code>logpdf</code> Log-density function <code>n_local_steps</code> Local MCMC steps per loop <code>n_global_steps</code> NF proposal steps per loop <code>n_training_loops</code> Number of training phase loops <code>n_production_loops</code> Number of production phase loops <code>n_epochs</code> Training epochs per loop","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#local-sampler-mala","level":2,"title":"Local sampler — MALA","text":"Parameter Default Description <code>mala_step_size</code> <code>1e-1</code> Initial MALA step size <code>adapt_step_size</code> <code>True</code> Auto-adapt global step size <code>adapt_step_size_per_dim</code> <code>True</code> Auto-adapt per-dimension step sizes <code>periodic</code> <code>None</code> Periodic dimensions","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#local-sampler-hmc","level":2,"title":"Local sampler — HMC","text":"Parameter Default Description <code>hmc_step_size</code> <code>0.1</code> Initial leapfrog step size <code>hmc_n_leapfrog</code> <code>10</code> Leapfrog steps per proposal <code>condition_matrix</code> <code>1</code> Diagonal inverse mass matrix <code>adapt_step_size</code> <code>True</code> Auto-adapt global step size <code>adapt_step_size_per_dim</code> <code>True</code> Auto-adapt per-dimension step sizes <code>periodic</code> <code>None</code> Periodic dimensions","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#local-sampler-gaussian-random-walk","level":2,"title":"Local sampler — Gaussian random walk","text":"Parameter Default Description <code>grw_step_size</code> <code>1e-1</code> Initial random walk step size <code>adapt_step_size</code> <code>True</code> Auto-adapt global step size <code>adapt_step_size_per_dim</code> <code>True</code> Auto-adapt per-dimension step sizes <code>periodic</code> <code>None</code> Periodic dimensions","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#normalizing-flow","level":2,"title":"Normalizing flow","text":"Parameter Default Description <code>rq_spline_n_layers</code> <code>4</code> Number of coupling layers <code>rq_spline_hidden_units</code> <code>[32, 32]</code> Hidden layer widths <code>rq_spline_n_bins</code> <code>8</code> Spline bins per layer <code>n_NFproposal_batch_size</code> <code>10000</code> NF sampling batch size","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#training","level":2,"title":"Training","text":"Parameter Default Description <code>learning_rate</code> <code>1e-3</code> AdamW learning rate <code>batch_size</code> <code>10000</code> Training mini-batch size <code>n_max_examples</code> <code>10000</code> Max samples in training buffer <code>history_window</code> <code>100</code> Most-recent steps per chain used as NF training data","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#execution","level":2,"title":"Execution","text":"Parameter Default Description <code>chain_batch_size</code> <code>0</code> Split vmap over chains <code>local_thinning</code> <code>1</code> Thinning for local samples <code>global_thinning</code> <code>1</code> Thinning for global samples <code>verbose</code> <code>False</code> Print progress","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#early-stopping","level":2,"title":"Early stopping","text":"Parameter Default Description <code>early_stopping</code> <code>False</code> Enable early stopping <code>early_stopping_tolerance</code> <code>0.05</code> Relative acceptance-rate change threshold <code>early_stopping_patience</code> <code>3</code> Consecutive loops to confirm stability <code>early_stopping_min_acceptance</code> <code>0.1</code> Acceptance rate that triggers early stopping","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#parallel-tempering-pt-bundles-only","level":2,"title":"Parallel tempering (PT bundles only)","text":"Parameter Default Description <code>logprior</code> flat Log-prior function <code>n_temperatures</code> <code>5</code> Number of temperature levels <code>max_temperature</code> <code>5.0</code> Highest temperature <code>n_tempered_steps</code> <code>-1</code> Swap attempts per loop","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#required-parameters","level":2,"title":"Required parameters","text":"","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#rng_key","level":3,"title":"rng_key","text":"<p>A JAX PRNG key used to initialise all random state inside the bundle (chain positions, model weights, optimizer state).</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#n_chains","level":3,"title":"n_chains","text":"<p>Number of parallel chains. The algorithm parallelises all chains simultaneously via <code>vmap</code>, so the performance benefit saturates once the chains fill the available hardware threads. More chains give a better picture of the global landscape, which helps train the normalizing flow faster and reduces the chance of mode collapse.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#n_dims","level":3,"title":"n_dims","text":"<p>Dimensionality of the parameter space. Must match the input dimension of <code>logpdf</code>.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#logpdf","level":3,"title":"logpdf","text":"<p>A callable with signature <code>logpdf(x, data) -&gt; float</code> where <code>x</code> is a 1-D array of length <code>n_dims</code> and <code>data</code> is the auxiliary data passed to <code>Sampler.sample</code>. The function must be JAX-differentiable if you use MALA or HMC.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#n_local_steps","level":3,"title":"n_local_steps","text":"<p>Number of local MCMC steps executed per chain in each training and production loop. Together with <code>n_chains</code> this controls how many samples are added to the training buffer per loop.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#n_global_steps","level":3,"title":"n_global_steps","text":"<p>Number of normalizing-flow proposal steps executed per chain in each loop. These are independent-MH moves drawn from the current flow, so they are most effective once the flow has been trained for a few loops.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#n_training_loops","level":3,"title":"n_training_loops","text":"<p>Number of local–global–train cycles to run in the training phase. During each cycle the local sampler runs, the flow is trained on the accumulated samples, and the global proposer runs. Increasing this gives the flow more opportunities to improve, at the cost of a longer warmup.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#n_production_loops","level":3,"title":"n_production_loops","text":"<p>Number of local–global cycles to run in the production phase. The flow is not updated during production, so detailed balance is restored and standard MCMC convergence diagnostics apply. All production samples are stored.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#n_epochs","level":3,"title":"n_epochs","text":"<p>Number of gradient-descent epochs per training step. Higher values improve the flow fit but increase training time per loop.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#local-sampler-parameters","level":2,"title":"Local sampler parameters","text":"","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#mala_step_size","level":3,"title":"mala_step_size","text":"<p>MALA bundles only. Initial step size for MALA. Default <code>1e-1</code>. Accepts either a scalar (broadcast to all dimensions) or a 1-D array of length <code>n_dims</code> to set a different initial step size per dimension. The optimal MALA acceptance rate is ~57%, and <code>adapt_step_size</code> will drive the step size toward that target automatically.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#hmc_step_size","level":3,"title":"hmc_step_size","text":"<p>HMC bundles only. Initial leapfrog step size. Default <code>0.1</code>. The optimal HMC acceptance rate is ~65%.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#hmc_n_leapfrog","level":3,"title":"hmc_n_leapfrog","text":"<p>HMC bundles only. Number of leapfrog steps per HMC proposal. Default <code>10</code>. More steps produce proposals further along the trajectory but increase cost per step.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#condition_matrix","level":3,"title":"condition_matrix","text":"<p>HMC bundles only. Diagonal of the inverse mass matrix (preconditioning matrix). Default <code>1</code> (scalar, broadcast to all dimensions). Pass a 1-D array of length <code>n_dims</code> to set per-dimension scales. Useful when parameters have very different characteristic scales.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#grw_step_size","level":3,"title":"grw_step_size","text":"<p>GRW bundles only. Initial step size for the Gaussian random walk proposal. Default <code>1e-1</code>. Accepts either a scalar (broadcast to all dimensions) or a 1-D array of length <code>n_dims</code> to set a different initial step size per dimension. The optimal random-walk acceptance rate is ~23%.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#adapt_step_size","level":3,"title":"adapt_step_size","text":"<p>Whether to automatically adapt the local step size during the training phase. Default <code>True</code>. The step size is updated after each training loop using a simple multiplicative rule targeting the algorithm-specific optimal acceptance rate. Adaptation is disabled during the production phase.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#adapt_step_size_per_dim","level":3,"title":"adapt_step_size_per_dim","text":"<p>Whether to additionally tune the per-dimension step size profile using the empirical posterior scale. Default <code>True</code>. After the global step size is adjusted by <code>adapt_step_size</code>, this strategy rescales each dimension's step size so that parameters with a larger posterior scale get proportionally larger steps. The geometric mean of the step sizes is preserved, so <code>adapt_step_size</code> retains full control of the overall magnitude, while this strategy only reshapes the per-dimension profile. For MALA and GRW, the per-dimension <code>step_size</code> array is scaled directly; for HMC the <code>condition_matrix</code> (diagonal mass matrix) is adjusted instead. Adaptation is disabled during the production phase.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#periodic","level":3,"title":"periodic","text":"<p>A <code>dict[int, tuple[float, float]]</code> mapping dimension indices to <code>(lower, upper)</code> bounds for periodic dimensions. Default <code>None</code> (no periodic dimensions). Proposals in periodic dimensions are automatically wrapped into <code>[lower, upper]</code>.</p> <pre><code># e.g. dim 0 is periodic on [0, 2π] and dim 2 on [-1, 1]\nperiodic = {0: (0.0, 6.283), 2: (-1.0, 1.0)}\n</code></pre>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#normalizing-flow-parameters","level":2,"title":"Normalizing flow parameters","text":"","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#rq_spline_n_layers","level":3,"title":"rq_spline_n_layers","text":"<p>Number of masked coupling layers in the RQ-spline normalizing flow. Default <code>4</code>. More layers increase expressive power at the cost of memory and training time.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#rq_spline_hidden_units","level":3,"title":"rq_spline_hidden_units","text":"<p>List of hidden layer widths for the conditioner MLPs inside each coupling layer. Default <code>[32, 32]</code>. Increasing the width or depth improves the flow's capacity to represent complex distributions.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#rq_spline_n_bins","level":3,"title":"rq_spline_n_bins","text":"<p>Number of bins for the rational-quadratic spline bijection. Default <code>8</code>. More bins give a finer piecewise approximation.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#n_nfproposal_batch_size","level":3,"title":"n_NFproposal_batch_size","text":"<p>Batch size used when running the NF proposal over many steps. Default <code>10000</code>. When <code>n_global_steps</code> exceeds this value, the flow samples are computed in chunks of this size using <code>jax.lax.map</code> rather than a single <code>vmap</code>, reducing peak memory.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#training-parameters","level":2,"title":"Training parameters","text":"","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#learning_rate","level":3,"title":"learning_rate","text":"<p>Learning rate for the AdamW optimizer used to train the normalizing flow. Default <code>1e-3</code>.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#batch_size","level":3,"title":"batch_size","text":"<p>Mini-batch size for each gradient step during flow training. Default <code>10000</code>. The training loss is computed on random mini-batches of the accumulated samples. Within the memory budget, larger batches tend to be better because the training dataset is continuously evolving and overfitting is not a concern.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#n_max_examples","level":3,"title":"n_max_examples","text":"<p>Maximum number of samples retained in the training buffer. Default <code>10000</code>. When the buffer exceeds this size, only the most recent <code>n_max_examples</code> samples are kept for training. Choose this based on your device's memory capacity. Setting it too low risks mode collapse, as the flow may forget regions of the posterior not recently visited by the chains.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#history_window","level":3,"title":"history_window","text":"<p>Number of most-recent position steps per chain used when selecting training data for the normalizing flow. Default <code>100</code>. At each training step, only the last <code>history_window</code> positions from each chain's buffer are considered; <code>n_max_examples</code> samples are then drawn from that pool. Reducing this value focuses the flow on the current mode but risks forgetting regions not recently visited.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#execution-parameters","level":2,"title":"Execution parameters","text":"","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#chain_batch_size","level":3,"title":"chain_batch_size","text":"<p>If non-zero, splits the <code>vmap</code> over chains into sequential batches of this size. Default <code>0</code> (no splitting; all chains are vmapped at once). Use this when vmapping over all chains simultaneously exceeds device memory.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#local_thinning","level":3,"title":"local_thinning","text":"<p>Store every <code>local_thinning</code>-th position from the local sampler. Default <code>1</code> (store all). All <code>n_local_steps</code> are always executed — thinning only controls which steps are written to the buffer. Increasing it reduces memory and the number of training samples available to the normalizing flow, but also reduces autocorrelation in the stored samples. The acceptance rate stored per slot is the mean over the corresponding thinning window. Note that <code>local_thinning</code> must not exceed <code>n_local_steps</code>.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#global_thinning","level":3,"title":"global_thinning","text":"<p>Store every <code>global_thinning</code>-th position from the NF global proposal. Default <code>1</code>. Same semantics as <code>local_thinning</code>: all <code>n_global_steps</code> are always executed, but only every <code>global_thinning</code>-th result is written to the buffer. This affects memory, the acceptance-rate values seen by early stopping, and the number of samples available in the training buffer. <code>global_thinning</code> must not exceed <code>n_global_steps</code>.</p> <p>A key practical motivation for increasing <code>global_thinning</code> is that the normalizing flow can never perfectly represent the posterior, so the global acceptance rate is typically low. When many consecutive proposals are rejected, the chain stays at the same position, and storing every step fills the buffer with duplicate points that contribute nothing to the posterior estimate or NF training. Thinning skips over these repeated positions while still executing all <code>n_global_steps</code>, preserving the full opportunity to make large jumps whenever a proposal is accepted.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#verbose","level":3,"title":"verbose","text":"<p>Print training loss and acceptance rates during sampling. Default <code>False</code>.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#early-stopping-parameters","level":2,"title":"Early stopping parameters","text":"","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#early_stopping","level":3,"title":"early_stopping","text":"<p>Whether to enable early stopping of the training phase. Default <code>False</code>. When enabled, the training phase ends early and the sampler transitions to the production phase once either of the two stopping conditions is met (see <code>early_stopping_tolerance</code> and <code>early_stopping_min_acceptance</code> below). The first 3 training loops are always completed regardless.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#early_stopping_tolerance","level":3,"title":"early_stopping_tolerance","text":"<p>Relative change threshold for the global acceptance rate stability check. Early stopping triggers when both the mean global acceptance rate and its coefficient of variation across chains change by less than this fraction for <code>early_stopping_patience</code> consecutive loops: <code>|current - prev| / prev &lt; tolerance</code>. Default <code>0.05</code>.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#early_stopping_patience","level":3,"title":"early_stopping_patience","text":"<p>Number of consecutive loops either stopping criterion must be satisfied before the training phase ends. Default <code>3</code>.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#early_stopping_min_acceptance","level":3,"title":"early_stopping_min_acceptance","text":"<p>Global acceptance rate threshold that, once reached, triggers early stopping regardless of stability (after <code>early_stopping_patience</code> consecutive loops). Default <code>0.1</code>. Set to <code>0</code> to disable this trigger and rely solely on the stability check.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#parallel-tempering-parameters","level":2,"title":"Parallel tempering parameters","text":"<p>These parameters are only available in the PT bundles: <code>RQSpline_MALA_PT_Bundle</code>, <code>RQSpline_HMC_PT_Bundle</code>, <code>RQSpline_GRW_PT_Bundle</code>.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#logprior","level":3,"title":"logprior","text":"<p>Log-prior function with signature <code>logprior(x, data) -&gt; float</code>. Default is a flat prior that always returns <code>0</code>. The tempered log-density is</p> \\[\\log \\tilde{p}_T(x) = \\frac{1}{T} \\log \\mathcal{L}(x) + \\log \\pi(x)\\] <p>so the prior is preserved at all temperatures.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#n_temperatures","level":3,"title":"n_temperatures","text":"<p>Number of temperature levels, including the target (temperature = 1). Default <code>5</code>. Chains at higher temperatures explore the prior more freely and occasionally swap positions with the target chains, helping escape local modes.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#max_temperature","level":3,"title":"max_temperature","text":"<p>Highest temperature in the ladder. Default <code>5.0</code>. The temperatures are spaced linearly between 1 and <code>max_temperature</code>. A larger value allows more prior-like exploration but reduces the swap acceptance rate between adjacent levels.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"guides/hyperparameters/#n_tempered_steps","level":3,"title":"n_tempered_steps","text":"<p>Number of parallel-tempering swap attempts per loop. Default <code>-1</code>, which is interpreted as equal to <code>n_local_steps</code>. Reduce this to save computation if swap acceptance is low.</p>","path":["Guides","Hyperparameter Reference"],"tags":[]},{"location":"tutorials/","level":1,"title":"Tutorials","text":"<p>These tutorials walk you through flowMC from first principles to advanced customization. They are ordered from simplest to most involved — if you are new to flowMC, start with the dual moon example.</p>","path":["Tutorials"],"tags":[]},{"location":"tutorials/#a-step-by-step-example","level":2,"title":"A Step-by-Step Example","text":"<p>The canonical flowMC example. Sample a 5-dimensional multi-modal \"dual moon\" distribution using the high-level <code>RQSpline_MALA_Bundle</code>, which pairs a MALA local kernel with a Masked Coupling RQSpline normalising flow as a learned global proposal. Covers the full workflow: defining a log-probability function, configuring the sampler, running it, and diagnosing convergence with loss curves, acceptance rates, and R-hat.</p>","path":["Tutorials"],"tags":[]},{"location":"tutorials/#parallel-tempering","level":2,"title":"Parallel Tempering","text":"<p>Demonstrates why standard local kernels struggle with well-separated modes and how parallel tempering (replica exchange MCMC) overcomes this. Builds a parallel tempering sampler from scratch using the low-level resource-strategy API, then contrasts it with the convenience <code>RQSpline_MALA_PT_Bundle</code>.</p>","path":["Tutorials"],"tags":[]},{"location":"tutorials/#custom-strategy-composition","level":2,"title":"Custom Strategy Composition","text":"<p>Shows the flexibility of the resource-strategy interface by composing a custom pipeline that prepends an Adam optimization phase to a random-walk MCMC kernel. A useful pattern when chains are initialized far from the high-probability region.</p>","path":["Tutorials"],"tags":[]},{"location":"tutorials/#training-normalising-flows","level":2,"title":"Training Normalising Flows","text":"<p>A standalone introduction to the two normalising flow architectures available in flowMC — RealNVP and Masked Coupling RQSpline — trained on a 2D toy dataset. Compares their expressiveness and computational cost, and shows how a trained flow plugs into the resource-strategy system via the <code>TrainModel</code> strategy.</p>","path":["Tutorials"],"tags":[]},{"location":"tutorials/#training-flow-matching-models","level":2,"title":"Training Flow Matching Models","text":"<p>Introduces flow matching as an alternative to traditional normalising flows. Trains a Conditional Optimal Transport model on a 2D distribution using an MLP-parameterised velocity field, then generates samples by integrating the learned ODE and evaluates the inferred density on a grid.</p>","path":["Tutorials"],"tags":[]},{"location":"tutorials/custom_strategy/","level":1,"title":"Custom Sampling Strategy","text":"<p>The default strategy of flowMC looks like the following:</p> <ol> <li>In training phase<ol> <li>Sample with local sampler</li> <li>Train normalising flow with the samples</li> <li>Sample with global sampler</li> </ol> </li> <li>Freeze the normalising flow</li> <li>In production phase<ol> <li>Sample with local sampler</li> <li>Sample with global sampler</li> </ol> </li> </ol> <p>One of the major changes introduced in <code>flowMC</code> 0.4.0 is the ability to customize the sampling strategy. In this notebook, we will demonstrate how to build your own sampling strategy to sample a Normal distribution when the initialisation is very off from the true distribution.</p> <pre><code>import jax\nimport jax.numpy as jnp\nfrom flowMC.resource.kernel.Gaussian_random_walk import GaussianRandomWalk\nfrom flowMC.resource.buffers import Buffer\nfrom flowMC.resource.states import State\nfrom flowMC.resource.logPDF import LogPDF\nfrom flowMC.strategy.optimization import AdamOptimization\nfrom flowMC.strategy.take_steps import TakeSerialSteps\nfrom flowMC.Sampler import Sampler\nimport numpy as np\n\n\ndef target_log_prob(x, data):\n    return -jnp.sum(x**2) / 2\n</code></pre>","path":["Tutorials","Custom Sampling Strategy"],"tags":[]},{"location":"tutorials/custom_strategy/#using-only-random-walk-gaussian-proposal","level":1,"title":"Using only random walk Gaussian proposal","text":"<p>To begin with, let's consider a simple case where we only use random walk Gaussian proposal to sample from the target distribution. We need to initialise the resources, which includes the random walk Gaussian proposal and the buffers we wish to store the relevant information, as well as the strategy which is taking serial steps using the proposal and updating the buffers.</p> <pre><code># Defining hyperparameters\n\nn_chains = 5\nn_dims = 2\nrng_key = jax.random.key(0)\nn_steps = 200\nstep_size = jnp.ones(n_dims)\n\n# Setting up resources\n\nRWMCMC_sampler = GaussianRandomWalk(step_size=step_size)\npositions = Buffer(\"positions\", (n_chains, n_steps, n_dims), 1)\nlog_prob = Buffer(\"log_prob\", (n_chains, n_steps), 1)\nacceptance = Buffer(\"acceptance\", (n_chains, n_steps), 1)\nsampler_state = State(\n    {\n        \"positions\": \"positions\",\n        \"log_prob\": \"log_prob\",\n        \"acceptance\": \"acceptance\",\n    },\n    name=\"sampler_state\",\n)\nresource = {\n    \"positions\": positions,\n    \"log_prob\": log_prob,\n    \"acceptance\": acceptance,\n    \"RWMCMC\": RWMCMC_sampler,\n    \"logpdf\": LogPDF(target_log_prob, n_dims=n_dims),\n    \"sampler_state\": sampler_state,\n}\n\n# Defining strategy\n\nstrategy = {\n    \"take_serial_step\": TakeSerialSteps(\n        logpdf_name=\"logpdf\",\n        kernel_name=\"RWMCMC\",\n        state_name=\"sampler_state\",\n        buffer_names=[\"positions\", \"log_prob\", \"acceptance\"],\n        n_steps=n_steps,\n    )\n}\n\n# Initializing sampler\n\nnf_sampler = Sampler(\n    n_dim=n_dims,\n    n_chains=n_chains,\n    rng_key=rng_key,\n    resources=resource,\n    strategies=strategy,\n    strategy_order=[\"take_serial_step\"],\n)\n</code></pre> <p>Now let's consider the scenario where the initialisation is very off from the true distribution, so the sampler may spend a lot of time travelling from the tail of the distribution to the most probable set. To do this, instead of initialising with a unit gaussian centred at 0, we initialise with a gaussian that is centred at 100</p> <pre><code>import matplotlib.pyplot as plt\n\nrng_key, subkey = jax.random.split(rng_key)\ninitial_position = jax.random.normal(subkey, shape=(n_chains, n_dims)) + 100\n\nnf_sampler.sample(initial_position, {})\n\noutput_chains = nf_sampler.resources[\"positions\"].data.reshape(-1, n_dims)\nplt.figure()\nplt.scatter(\n    output_chains[:, 0],\n    output_chains[:, 1],\n    c=np.repeat(np.arange(output_chains.shape[0] // 5)[None], 5, axis=0).flatten(),\n    s=3,\n)\nplt.colorbar()\nplt.show()\n</code></pre> <p></p> <p>We can see the plot shows the samples started from the initialisation of 100, and all of the steps are just random walk toward a Normal distribution centred at 1. This corresponds to a chains that have not \"burned in\" yet.</p> <p>Now instead of only running the proposal kernel, we add an optimisation phase before the sampling to move the initialisation closer to the true distribution.</p> <pre><code># Defining hyperparameters for optimization\n\nn_opt_steps = 50\nlearning_rate = 3\nnoise_level = 0.1\nadam_optimizer = AdamOptimization(\n    target_log_prob,\n    n_steps=n_opt_steps,\n    learning_rate=learning_rate,\n    noise_level=noise_level,\n)\n\n# Constructing the resources again.\n\npositions = Buffer(\"positions\", (n_chains, n_steps, n_dims), 1)\nlog_prob = Buffer(\"log_prob\", (n_chains, n_steps), 1)\nacceptance = Buffer(\"acceptance\", (n_chains, n_steps), 1)\nsampler_state = State(\n    {\n        \"positions\": \"positions\",\n        \"log_prob\": \"log_prob\",\n        \"acceptance\": \"acceptance\",\n    },\n    name=\"sampler_state\",\n)\nresource = {\n    \"positions\": positions,\n    \"log_prob\": log_prob,\n    \"acceptance\": acceptance,\n    \"RWMCMC\": RWMCMC_sampler,\n    \"logpdf\": LogPDF(target_log_prob, n_dims=n_dims),\n    \"sampler_state\": sampler_state,\n}\n\nstrategy = TakeSerialSteps(\n    logpdf_name=\"logpdf\",\n    kernel_name=\"RWMCMC\",\n    state_name=\"sampler_state\",\n    buffer_names=[\"positions\", \"log_prob\", \"acceptance\"],\n    n_steps=n_steps,\n)\n\n# Prepending the optimization strategy to the list of strategies\n\nnf_sampler = Sampler(\n    n_dim=n_dims,\n    n_chains=n_chains,\n    rng_key=rng_key,\n    resources=resource,\n    strategies={\n        \"take_serial_step\": strategy,\n        \"optimize\": adam_optimizer,\n    },\n    strategy_order=[\"optimize\", \"take_serial_step\"],\n)\n\nnf_sampler.sample(initial_position, {})\n\noutput_chains = nf_sampler.resources[\"positions\"].data.reshape(-1, n_dims)\nplt.figure()\nplt.scatter(\n    output_chains[:, 0],\n    output_chains[:, 1],\n    c=np.repeat(np.arange(output_chains.shape[0] // 5)[None], 5, axis=0).flatten(),\n    s=3,\n)\nplt.show()\n</code></pre> <p></p> <p>Now we can see the sampling result is much close to the target distribution. It overshoots a little bit since we chose a fairly large step size for the optimisation phase. One can tune the step size to get a better result.</p>","path":["Tutorials","Custom Sampling Strategy"],"tags":[]},{"location":"tutorials/dualmoon/","level":1,"title":"Step-by-Step Example","text":"<p>In this tutorial we will showcase the ability of <code>flowMC</code> to sample efficiently a 5-dimensional multi-modal distribution. We will go through the typical steps of verification one follows to confirm the quality of the samples at the end of the MCMC run.</p> <p>This is a simple example of a 5-dimensional multi-modal distribution:</p> <p></p>","path":["Tutorials","Step-by-Step Example"],"tags":[]},{"location":"tutorials/dualmoon/#1-the-target-distribution","level":2,"title":"1 - The target distribution","text":"<p>The first step is to define the target distribution using <code>jax</code>. The target distribution should be a function conforming to the following function signature (using <code>jaxtyping</code> convention for type hints):</p> <pre><code>def target_log_prob_fn(x: Float[Array, \"n_dims\"], data: dict[str, Any]) -&gt; Float:\n    ...\n    return log_prob\n</code></pre> <p><code>flowMC</code> will handle batching and compiling the function, so you simply have to provide the version of the function which is evaluated on a specific point in the sample space <code>X</code>. <code>data</code> represents any additional data that is needed to evaluate the target but not sampled. Note that <code>data</code> should remain fixed at run time.</p> <p>Here below we define our target function:</p> <pre><code>import jax\nimport jax.numpy as jnp\nfrom jax.scipy.special import logsumexp\nfrom jaxtyping import Float, Array\nfrom typing import Any\n\n\ndef target_dual_moon(x: Float[Array, \"n_dims\"], data: dict[str, Any]) -&gt; Float:\n    \"\"\"\n    Term 2 and 3 separate the distribution and smear it along the first and second dimension\n    \"\"\"\n    term1 = 0.5 * ((jnp.linalg.norm(x) - 2) / 0.1) ** 2\n    term2 = -0.5 * ((x[:1] + jnp.array([-3.0, 3.0])) / 0.8) ** 2\n    term3 = -0.5 * ((x[1:2] + jnp.array([-3.0, 3.0])) / 0.6) ** 2\n    return -(term1 - logsumexp(term2) - logsumexp(term3))\n\n\nn_dims = 5\ndata = {\"data\": jnp.arange(n_dims).astype(jnp.float32)}\n\n# Testing evaluation\nx = jnp.zeros(n_dims)\nprint(\"Testing the value of the target function at x=0: \", target_dual_moon(x, data))\n</code></pre> <pre><code>Testing the value of the target function at x=0:  -218.14496\n</code></pre>","path":["Tutorials","Step-by-Step Example"],"tags":[]},{"location":"tutorials/dualmoon/#2-choosing-the-sampling-resources-and-strategies","level":2,"title":"2 - Choosing the sampling resources and strategies","text":"<p>The next step is to initialize the resources and strategies for the MCMC run. The default <code>flowMC</code> algorithm that involves a local proposal with MALA and a global proposal with normalizing flows is conveniently packaged in a bundle which the users only have to set its hyper-parameters instead of making sure the internal components are compatible. Here we will import the bundle and initialize it. In the quicks start, there is a detailed explanation of the resource-startegy interface.</p> <pre><code>from flowMC.resource_strategy_bundle.RQSpline_MALA import RQSpline_MALA_Bundle\n\n# Defining hyperparameters\n\nn_local_steps = 100\nn_global_steps = 10\nn_training_loops = 30\nn_production_loops = 30\nn_epochs = 5\nn_chains = 100\nrq_spline_hidden_units = [32, 32]\nrq_spline_n_bins = 8\nrq_spline_n_layers = 4\nlearning_rate = 5e-3\nbatch_size = 5000\nn_max_examples = 5000\n\n# Initializing the default flowMC algorithm with RQSpline_MALA_Bundle\n\nrng_key = jax.random.key(42)\nrng_key, subkey = jax.random.split(rng_key)\nbundle = RQSpline_MALA_Bundle(\n    subkey,\n    n_chains,\n    n_dims,\n    target_dual_moon,\n    n_local_steps,\n    n_global_steps,\n    n_training_loops,\n    n_production_loops,\n    n_epochs,\n    rq_spline_hidden_units=rq_spline_hidden_units,\n    rq_spline_n_bins=rq_spline_n_bins,\n    rq_spline_n_layers=rq_spline_n_layers,\n    learning_rate=learning_rate,\n    batch_size=batch_size,\n    n_max_examples=n_max_examples,\n    verbose=False,\n)\n</code></pre>","path":["Tutorials","Step-by-Step Example"],"tags":[]},{"location":"tutorials/dualmoon/#3-defining-and-running-the-sampler","level":2,"title":"3 - Defining and Running the sampler","text":"<p>Now we have defined our resources and strategies, we can pass it to <code>flowMC</code> top-level manager, the <code>Sampler</code> class to run the MCMC.</p> <pre><code>from flowMC.Sampler import Sampler\n\nrng_key, subkey = jax.random.split(rng_key)\ninitial_position = jax.random.normal(subkey, shape=(n_chains, n_dims)) * 1\n\nsampler = Sampler(\n    n_dim=n_dims,\n    n_chains=n_chains,\n    rng_key=rng_key,\n    resource_strategy_bundles=bundle,\n)\n\nsampler.sample(initial_position, data)\n</code></pre>","path":["Tutorials","Step-by-Step Example"],"tags":[]},{"location":"tutorials/dualmoon/#4-inspecting-the-results","level":2,"title":"4 - Inspecting the results","text":"<p>Now the sampling is done, let's check some of the results. The first step is to check what type of buffers are available:</p> <pre><code>for key, resource in sampler.resources.items():\n    print(resource)\n</code></pre> <pre><code>LogPDF with 5 dimensions\nBuffer positions_training with shape (100, 3300, 5)\nBuffer log_prob_training with shape (100, 3300)\nBuffer local_accs_training with shape (100, 3300)\nBuffer global_accs_training with shape (100, 3300)\nBuffer loss_buffer with shape (150,)\nBuffer positions_production with shape (100, 3300, 5)\nBuffer log_prob_production with shape (100, 3300)\nBuffer local_accs_production with shape (100, 3300)\nBuffer global_accs_production with shape (100, 3300)\nMALA with step size [0.18221645 0.18221645 0.18221645 0.18221645 0.18221645]\nNF proposal with MaskedCouplingRQSpline with n_features=5, n_layers=2\nMaskedCouplingRQSpline with n_features=5, n_layers=2\nOptimizer\nState sampler_state with shape 6\n</code></pre> <p>We can see there are a number of buffers that store the information about the run, along with some resources like MALA and NF proposal. There are two sets of buffers, one for results during the training phase and the other for the production phase. For each of the phase, there are <code>position</code>, <code>log_prob</code>, <code>local_accs</code> and <code>global_accs</code>, which corresponds to the chains, the log-probabilities, whether the proposals were accepted by the local or global proposal. In the training phase, there is an additional buffer <code>loss_buffer</code> which stores the loss of the normalizing flow during training. Let's start with inspecting the loss during the training.</p> <pre><code>import matplotlib.pyplot as plt\n\nloss_data = sampler.resources[\"loss_buffer\"].data\nplt.figure(figsize=(5, 3))\nplt.plot(loss_data.reshape(-1, 1))\nplt.xlabel(\"Epochs\")\nplt.ylabel(\"Loss\")\nplt.show()\n</code></pre> <p></p> <p>Loss goes down, looking good! Now let's plot the chains and have a look of the samples. There are a number of visualization tools like <code>corner</code> and <code>arviz</code> that can be used to visualize the samples. Here we will use <code>corner</code> to plot the samples generated from <code>flowMC</code>, as well as samples from the trained normalizing flow.</p> Note: If you want to run the following cells with `corner` or `arviz`, you have to run `uv sync --extra visualize` to install the dependencies in your `uv` environment.  <pre><code>import corner\nimport numpy as np\n\nchains = sampler.resources[\"positions_production\"].data\nnf_samples = sampler.resources[\"model\"].sample(jax.random.PRNGKey(2046), 10000)\n\nlabels = [f\"x{i}\" for i in range(n_dims)]\n\n# Plotting chain from flowMC\nfig = plt.figure(figsize=(6, 6))\nfig = corner.corner(np.array(chains.reshape(-1, n_dims)), fig=fig, labels=labels)\n\n# Plotting samples from the normalizing flow\nfig = plt.figure(figsize=(6, 6))\nfig = corner.corner(np.array(nf_samples), fig=fig, labels=labels)\n</code></pre> <p></p> <p></p> <p>There is our posterior! Now let's also look at some diagnostics, such as the mean acceptance rate and the trace during the production phase.</p> <pre><code>import numpy as np\n\n# Get acceptance rate\nlocal_accs = sampler.resources[\"local_accs_production\"].data\nglobal_accs = sampler.resources[\"global_accs_production\"].data\nlog_prob = sampler.resources[\"log_prob_production\"].data\n\n# Mean over chains\nmean_local_accs = np.mean(local_accs, axis=0)\nmean_global_accs = np.mean(global_accs, axis=0)\n\nfig, ax = plt.subplots(1, 3, figsize=(15, 5))\nax[0].plot(mean_local_accs)\nax[0].set_title(\"Local acceptance rate\")\nax[0].set_xlabel(\"Local steps\")\nax[1].plot(mean_global_accs)\nax[1].set_title(\"Global acceptance rate\")\nax[1].set_xlabel(\"Global steps\")\n# We are downsampling both the chain and the step dimension for a better visualization\nax[2].plot(log_prob[::5, ::20].T, lw=1, alpha=0.5)\nax[2].set_title(\"Log probability\")\nplt.show()\n</code></pre> <p></p> <p>The gap you see in the global acceptance rate plot is because those are the period when the local sampler is proposing, so there is no global acceptance during those periods.</p> <p>Lastly, let's look at the effective sample size (ESS) and r-hat values for the samples in the production phase.</p> <pre><code>import arviz as az\n\nchains = np.array(sampler.resources[\"positions_production\"].data)\n## Load data as arviz InferenceData class\nn_steps = chains.shape[1]\nn_step_group = 7\nn_group_step = n_steps // n_step_group\n\n\nrhat_s = np.array(\n    [\n        [\n            az.rhat(chains[:, : (i + 1) * n_group_step, j], method=\"rank\")\n            for i in range(n_step_group)\n        ]\n        for j in range(n_dims)\n    ]\n)\niterations = np.linspace(0, n_steps, n_step_group)\n\nplt.figure(figsize=(5, 3))\nplt.plot(iterations, rhat_s.T, \"-o\", label=[f\"x{i}\" for i in range(n_dims)])\nplt.axhline(1, c=\"k\", ls=\"--\")\nplt.xlabel(\"Iteration\")\nplt.ylabel(r\"$\\hat{R}$\")\nplt.legend()\nplt.show()\n</code></pre> <p></p>","path":["Tutorials","Step-by-Step Example"],"tags":[]},{"location":"tutorials/parallel_tempering/","level":1,"title":"Parallel Tempering","text":"<pre><code>import jax\nimport jax.numpy as jnp\nfrom jax.scipy.special import logsumexp\nfrom jaxtyping import Float, Array\nfrom typing import Any\n\nfrom flowMC.resource.kernel.MALA import MALA\nfrom flowMC.resource.buffers import Buffer\nfrom flowMC.resource.states import State\nfrom flowMC.strategy.take_steps import TakeSerialSteps\nfrom flowMC.strategy.parallel_tempering import ParallelTempering\nfrom flowMC.Sampler import Sampler\nfrom flowMC.resource.logPDF import TemperedPDF, LogPDF\n\n\ndef target_dual_moon(x: Float[Array, \"n_dims\"], data: dict[str, Any]) -&gt; Float:\n    \"\"\"\n    Term 2 and 3 separate the distribution and smear it along the first and second dimension\n    \"\"\"\n    term1 = 0.5 * ((jnp.linalg.norm(x) - 2) / 0.1) ** 2\n    term2 = -0.5 * ((x[:1] + jnp.array([-3.0, 3.0])) / 0.8) ** 2\n    term3 = -0.5 * ((x[1:2] + jnp.array([-3.0, 3.0])) / 0.6) ** 2\n    return -(term1 - logsumexp(term2) - logsumexp(term3))\n</code></pre> <p>Let's setup a normal run with a MALA sampler</p> <pre><code># Defining hyperparameters\nn_chains = 5\nrng_key = jax.random.PRNGKey(0)\nn_steps = 400\nn_dims = 5\nstep_size = jnp.full(n_dims, 0.1)\ndata = {\"data\": jnp.arange(n_dims).astype(jnp.float32)}\n# Setting up resources\n\nMALA_sampler = MALA(step_size=step_size)\npositions = Buffer(\"positions\", (n_chains, n_steps, n_dims), 1)\nlog_prob = Buffer(\"log_prob\", (n_chains, n_steps), 1)\nacceptance = Buffer(\"acceptance\", (n_chains, n_steps), 1)\nsampler_state = State(\n    {\n        \"positions\": \"positions\",\n        \"log_prob\": \"log_prob\",\n        \"acceptance\": \"acceptance\",\n    },\n    name=\"sampler_state\",\n)\n\nresource = {\n    \"positions\": positions,\n    \"log_prob\": log_prob,\n    \"acceptance\": acceptance,\n    \"MALA\": MALA_sampler,\n    \"logpdf\": LogPDF(target_dual_moon, n_dims=n_dims),\n    \"state\": sampler_state,\n}\n\n# Defining strategy\n\nstrategy = TakeSerialSteps(\n    logpdf_name=\"logpdf\",\n    kernel_name=\"MALA\",\n    state_name=\"state\",\n    buffer_names=[\"positions\", \"log_prob\", \"acceptance\"],\n    n_steps=n_steps,\n)\n\n# Initializing sampler\n\nsampler = Sampler(\n    n_dim=n_dims,\n    n_chains=n_chains,\n    rng_key=rng_key,\n    resources=resource,\n    strategies={\"take_steps\": strategy},\n    strategy_order=[\"take_steps\"],\n)\n\nsampler.sample(\n    initial_position=jax.random.normal(rng_key, (n_chains, n_dims)),\n    data=data,\n)\n</code></pre> <pre><code>import corner\nimport numpy as np\nimport matplotlib.pyplot as plt\n\nchains = sampler.resources[\"positions\"].data\n\nlabels = [f\"x{i}\" for i in range(n_dims)]\n\n# Plotting chain from flowMC\nfig = plt.figure(figsize=(6, 6))\nfig = corner.corner(np.array(chains.reshape(-1, n_dims)), fig=fig, labels=labels)\nfig = plt.figure(figsize=(6, 6))\nplt.plot(chains[:, :, 0].T, chains[:, :, 1].T, alpha=0.5)\nplt.xlabel(\"x0\")\nplt.ylabel(\"x1\")\nplt.show()\n</code></pre> <p></p> <p></p>","path":["Tutorials","Parallel Tempering"],"tags":[]},{"location":"tutorials/parallel_tempering/#now-lets-add-parallel-tempering-to-the-mix","level":1,"title":"Now let's add parallel tempering to the mix","text":"<pre><code>import itertools\n\n# Defining hyperparameters\nn_chains = 5\nn_steps = 10\nn_dims = 5\nstep_size = jnp.full(n_dims, 0.1)\nn_temps = 5\nn_loops = 40\ndata = {\n    \"data\": jnp.arange(n_dims).astype(jnp.float32),\n}\n# Setting up resources\n\nlogpdf = LogPDF(target_dual_moon, n_dims=n_dims)\n\nkey = jax.random.PRNGKey(42)\nkey, subkey = jax.random.split(key)\ninitial_position = jax.random.normal(subkey, shape=(n_chains, n_dims))\n\nMALA_sampler = MALA(step_size=step_size)\npositions = Buffer(\"positions\", (n_chains, n_steps * n_loops, n_dims), 1)\nlog_prob = Buffer(\"log_prob\", (n_chains, n_steps * n_loops), 1)\nacceptance = Buffer(\"acceptance\", (n_chains, n_steps * n_loops), 1)\nsampler_state = State(\n    {\n        \"positions\": \"positions\",\n        \"log_prob\": \"log_prob\",\n        \"acceptance\": \"acceptance\",\n        \"training\": True,\n    },\n    name=\"sampler_state\",\n)\npositions.update_buffer(initial_position[:, None])\nlog_prob.update_buffer(\n    jax.vmap(logpdf, in_axes=(0, None))(initial_position, data)[:, None]\n)\n\n\ntake_steps = TakeSerialSteps(\n    logpdf_name=\"logpdf\",\n    kernel_name=\"MALA\",\n    state_name=\"state\",\n    buffer_names=[\"positions\", \"log_prob\", \"acceptance\"],\n    n_steps=n_steps,\n)\n</code></pre> <p>Here below are the tempering specific code to define the extra resources and strategies</p> <pre><code>key, subkey = jax.random.split(key)\ntempered_logpdf = TemperedPDF(\n    target_dual_moon,\n    lambda x, data: jnp.array(0.0),\n    n_dims=n_dims,\n    n_temps=n_temps,\n)\ntempered_initial_position = jax.random.normal(\n    subkey, shape=(n_chains, n_temps - 1, n_dims)\n)\ntempered_positions = Buffer(\"tempered_positions\", (n_chains, n_temps - 1, n_dims), 2)\ntempered_positions.update_buffer(tempered_initial_position)\n\ntemperatures = Buffer(\"temperature\", (n_temps,), 0)\ntemperatures.update_buffer(jnp.arange(n_temps) * 5 + 1.0)\n\nresources = {\n    \"logpdf\": logpdf,\n    \"MALA\": MALA_sampler,\n    \"positions\": positions,\n    \"log_prob\": log_prob,\n    \"acceptance\": acceptance,\n    \"tempered_logpdf\": tempered_logpdf,\n    \"tempered_positions\": tempered_positions,\n    \"temperatures\": temperatures,\n    \"state\": sampler_state,\n}\n\nparallel_tempering_strat = ParallelTempering(\n    n_steps=n_steps,\n    tempered_logpdf_name=\"tempered_logpdf\",\n    kernel_name=\"MALA\",\n    state_name=\"state\",\n    tempered_buffer_names=[\"tempered_positions\", \"temperatures\"],\n)\n\nstrategy_order = [[\"take_steps\", \"parallel_tempering\"] for _ in range(n_loops)]\n\nstrategy_order = list(itertools.chain.from_iterable(strategy_order))\n</code></pre> <pre><code>sampler = Sampler(\n    n_dim=n_dims,\n    n_chains=n_chains,\n    rng_key=rng_key,\n    resources=resources,\n    strategies={\n        \"take_steps\": take_steps,\n        \"parallel_tempering\": parallel_tempering_strat,\n    },\n    strategy_order=strategy_order,\n)\n\nsampler.sample(\n    initial_position=initial_position,\n    data=data,\n)\n</code></pre> <pre><code>chains = sampler.resources[\"positions\"].data\nprint(chains.shape)\n\nlabels = [f\"x{i}\" for i in range(n_dims)]\n\n# Plotting chain from flowMC\nfig = plt.figure(figsize=(6, 6))\nfig = corner.corner(np.array(chains.reshape(-1, n_dims)), fig=fig, labels=labels)\nfig = plt.figure(figsize=(6, 6))\nplt.plot(chains[:, :, 0].T, chains[:, :, 1].T, alpha=0.5)\nplt.xlabel(\"x0\")\nplt.ylabel(\"x1\")\nplt.show()\n</code></pre> <pre><code>(5, 400, 5)\n</code></pre> <p></p> <p></p> <p>Now instead of defining your sampler from scratch, you can also use the parallel tempering bundle, which is the original flowMC sampler with the parallel tempering strategy. The training of the normalizing flow is a computationally expensive task, so one can expect this strategy bundle to be slower than the lighter version as shown above.</p> <pre><code>from flowMC.resource_strategy_bundle.RQSpline_MALA_PT import RQSpline_MALA_PT_Bundle\n\nrng_key = jax.random.PRNGKey(0)\nrng_key, subkey = jax.random.split(rng_key)\n\n\nn_chains = 5\nn_dims = 5\nbundle = RQSpline_MALA_PT_Bundle(\n    subkey,\n    n_chains=n_chains,\n    n_dims=n_dims,\n    logpdf=target_dual_moon,\n    n_local_steps=2,\n    n_global_steps=2,\n    n_training_loops=100,\n    n_production_loops=100,\n    n_epochs=2,\n    rq_spline_hidden_units=[32, 32],\n    rq_spline_n_layers=2,\n    max_temperature=20.0,\n    n_temperatures=5,\n    n_tempered_steps=3,\n)\n\nrng_key, subkey = jax.random.split(rng_key)\ninitial_position = jax.random.normal(subkey, shape=(n_chains, n_dims))\nsampler = Sampler(\n    n_dim=n_dims,\n    n_chains=n_chains,\n    rng_key=rng_key,\n    resource_strategy_bundles=bundle,\n)\nsampler.sample(\n    initial_position=initial_position,\n    data=data,\n)\n</code></pre> <pre><code>chains = sampler.resources[\"positions_training\"].data\nprint(chains.shape)\n\nlabels = [f\"x{i}\" for i in range(n_dims)]\n\n# Plotting chain from flowMC\nfig = plt.figure(figsize=(6, 6))\nfig = corner.corner(np.array(chains.reshape(-1, n_dims)), fig=fig, labels=labels)\nfig = plt.figure(figsize=(6, 6))\nplt.plot(chains[:, :, 0].T, chains[:, :, 1].T, alpha=0.5)\nplt.xlabel(\"x0\")\nplt.ylabel(\"x1\")\nplt.show()\n</code></pre> <pre><code>(5, 400, 5)\n</code></pre> <p></p> <p></p>","path":["Tutorials","Parallel Tempering"],"tags":[]},{"location":"tutorials/train_flow_match/","level":1,"title":"Training Flow Matching Models","text":"<pre><code>import jax\nimport jax.numpy as jnp\nfrom sklearn.datasets import make_moons\nimport matplotlib.pyplot as plt\nfrom flowMC.resource.model.common import MLP\nfrom flowMC.resource.model.flowmatching.base import (\n    Solver,\n    Path,\n    CondOTScheduler,\n    FlowMatchingModel,\n)\n\n# Optimization parameters\nnum_epochs = 5000\nbatch_size = 30000\nlearning_rate = 1e-3\nmomentum = 0.9\nn_samples = 30000\nseed = 0\n\nsolver = Solver(\n    MLP([3, 128, 128, 128, 2], jax.random.PRNGKey(0), activation=jax.nn.swish)\n)\npath = Path(CondOTScheduler())\nmodel = FlowMatchingModel(solver, path)\n\ndata = jnp.array(make_moons(n_samples, noise=0.05)[0])\n\nplt.scatter(data[:, 0], data[:, 1], s=0.5, alpha=0.5, label=\"data\")\nplt.legend()\nplt.show()\n</code></pre> <pre><code>import optax  # Optimizers\nimport equinox as eqx  # Equinox\n\nkey = jax.random.PRNGKey(seed)\n\nkey, subkey = jax.random.split(key)\nx0 = jax.random.normal(subkey, (data.shape[0], 2))  # Initial points\nkey, subkey = jax.random.split(key)\nt = jax.random.uniform(\n    subkey, (data.shape[0], 1), minval=0.0, maxval=1.0\n)  # Random time points\n\noptim = optax.adam(learning_rate)\nstate = optim.init(eqx.filter(model, eqx.is_inexact_array))\nkey, subkey = jax.random.split(key)\nkey, trained_model, state, loss = model.train(\n    key, (x0, data, t), optim, state, num_epochs, batch_size, verbose=True\n)\n</code></pre> <pre><code>Training NF, current loss: 1.369: 100%|██████████| 5000/5000 [00:34&lt;00:00, 146.56it/s]\n</code></pre> <pre><code>sampled_data = trained_model.sample(key, 10000, dt=0.1)\nplt.scatter(\n    sampled_data[:, 0], sampled_data[:, 1], s=0.5, alpha=0.5, label=\"sampled data\"\n)\nplt.legend()\nplt.show()\n</code></pre> <pre><code>grid = jnp.mgrid[-2:2:100j, -2:2:100j]\ngrid = grid.reshape(2, -1).T  # Reshape to (10000, 2)\nlog_prob = eqx.filter_vmap(trained_model.log_prob, in_axes=(0,))(grid)\nplt.imshow(\n    log_prob.reshape(100, 100).T, extent=(-2, 2, -2, 2), origin=\"lower\", cmap=\"viridis\"\n)\nplt.colorbar(label=\"Log Probability\")\nplt.title(\"Log Probability Density\")\nplt.xlabel(\"x\")\nplt.ylabel(\"y\")\nplt.show()\n</code></pre>","path":["Tutorials","Training Flow Matching Models"],"tags":[]},{"location":"tutorials/train_normalizing_flow/","level":1,"title":"Training Normalizing Flows","text":"<p>In this notebook, we use the API in <code>flowMC</code> to train two different normalizing flow networks to approximate a simple test distribution. The API is built on top of the companion libraries of JAX for deep learning, <code>flax</code> and <code>optax</code>. </p> <p>In typical applications of <code>flowMC</code> to obtain samples from a given posterior distribution you will not need to interact with this level of the API, the training will be directly handled within the sampling. However you will need to choose the normalizing flow model and this tutorial exemplifies the abilities of the two models currently available in the package.</p> <p>We train both a RealNVP flow from [Dinh et al. 2016] and a more complex normalizing flow model, the rational quadratic spline model [Durkan et al. 2019].</p> <pre><code>import jax\nimport jax.numpy as jnp  # JAX NumPy\nimport optax  # Optimizers\nimport equinox as eqx  # Equinox\n\n\nfrom flowMC.resource.model.nf_model.realNVP import RealNVP\nfrom flowMC.resource.model.nf_model.rqSpline import MaskedCouplingRQSpline\n</code></pre> <p>We will use make_moons from scikit-learn to create a toy dataset in 2-dimensions.</p> <pre><code>from sklearn.datasets import make_moons\nimport matplotlib.pyplot as plt\n\ndata = jnp.array(make_moons(n_samples=100000, noise=0.05)[0])\n\nplt.scatter(data[:, 0], data[:, 1], s=0.5, alpha=0.5, label=\"data\")\nplt.legend()\nplt.show()\n</code></pre> <p></p>","path":["Tutorials","Training Normalizing Flows"],"tags":[]},{"location":"tutorials/train_normalizing_flow/#realnvps","level":2,"title":"RealNVPs","text":"<p>We first use the RealNVP model to fit the data. We need to specify:  - <code>n_layers</code>: the number of coupling layers.  - <code>n_hidden</code>: the width of the hidden layers in the 1-hidden layer MLPs for learning the scales and translations in the affine coupling layers. </p> <p>Inflating these numbers provides more flexibility to the normalizing flow, yet at the cost of increasing the computational budget. </p> <pre><code># Model parameters\nn_feature = 2\nn_layers = 10\nn_hidden = 100\n\nkey, subkey = jax.random.split(jax.random.key(0), 2)\n\nmodel = RealNVP(\n    n_feature,\n    n_layers,\n    n_hidden,\n    subkey,\n    data_mean=jnp.mean(data, axis=0),\n    data_cov=jnp.cov(data.T),\n)\n</code></pre> <pre><code>jax.vmap(model.__call__)(data)\n</code></pre> <pre><code>(Array([[-0.99613   , -0.10127474],\n        [-0.14790912,  1.6296724 ],\n        [ 3.0793257 , -1.157111  ],\n        ...,\n        [ 0.67037183,  0.05505186],\n        [ 2.5246344 , -1.467852  ],\n        [-1.0591872 ,  0.1318307 ]], dtype=float32),\n Array([1.3785942, 1.3574398, 1.3711026, ..., 1.3693422, 1.3767755,\n        1.3767034], dtype=float32))\n</code></pre> <p>Next, we initialize a <code>train_state</code> following <code>flax</code> logic and an <code>optax</code> optimizer beforw lanching the training. </p> <pre><code># Optimization parameters\nnum_epochs = 100\nbatch_size = 10000\nlearning_rate = 0.001\nmomentum = 0.9\n\noptim = optax.adam(learning_rate)\nstate = optim.init(eqx.filter(model, eqx.is_inexact_array))\nkey, subkey = jax.random.split(key)\nkey, model, state, loss = model.train(\n    key, data, optim, state, num_epochs, batch_size, verbose=True\n)\n</code></pre> <pre><code>Training NF, current loss: 1.364: 100%|██████████| 100/100 [00:08&lt;00:00, 12.35it/s]\n</code></pre> <pre><code>plt.plot(loss)\nplt.xlabel(\"Epoch\")\nplt.ylabel(\"Loss\")\nplt.show()\n</code></pre> <p></p> <p>Finally we can visualize what the flow has learned by comparing the data distribution to the distribution of samples from the flow.</p> <pre><code>key, subkey = jax.random.split(key, 2)\nnf_samples = model.sample(subkey, 10000)\nplt.figure()\nplt.scatter(data[:, 0], data[:, 1], s=0.5, alpha=0.5, label=\"data\")\nplt.scatter(\n    nf_samples[:, 0], nf_samples[:, 1], s=0.5, alpha=0.5, label=\"RealNVP samples\"\n)\nplt.legend()\nplt.show()\n</code></pre> <p></p>","path":["Tutorials","Training Normalizing Flows"],"tags":[]},{"location":"tutorials/train_normalizing_flow/#rqsplines","level":2,"title":"RQSplines","text":"<p>The second type of flows available are the RQSpline. These models are also based on coupling layers, however they allow for transformation more expressive than affine, namely splines of quotients of quadratic functions. Here the parameters are:  - <code>n_layers</code>: the number of coupling layers.  - <code>n_hidden</code>: the list of widths of the hidden layers MLPs for learning the polynomial coefficients. -  <code>n_bins</code>: the number of bins for the spline decompositions.</p> <p>As previsouly, the bigger these numbers the more flexibility to the normalizing flow and higher is the computational cost of one training iteration. While RQSplines are generally more computationally demanding per training step than RealNVPs, there can be a favorable trade-off in selecting this more sophisticated model as it may require less iterations to converge to a satisfactory solution. </p> <pre><code># Model parameters\nn_feature = 2\nn_layers = 8\nn_hiddens = [64, 64]\nn_bins = 8\n\nkey, subkey = jax.random.split(jax.random.key(1))\n\nmodel = MaskedCouplingRQSpline(\n    n_feature,\n    n_layers,\n    n_hiddens,\n    n_bins,\n    subkey,\n    data_cov=jnp.cov(data.T),\n    data_mean=jnp.mean(data, axis=0),\n)\n\nnum_epochs = 100\nbatch_size = 10000\nlearning_rate = 0.001\nmomentum = 0.9\n\noptim = optax.adam(learning_rate)\nstate = optim.init(eqx.filter(model, eqx.is_inexact_array))\nkey, subkey = jax.random.split(key)\nkey, model, state, loss = model.train(\n    key, data, optim, state, num_epochs, batch_size, verbose=True\n)\n</code></pre> <pre><code>Training NF, current loss: 1.196: 100%|██████████| 100/100 [00:17&lt;00:00,  5.60it/s]\n</code></pre> <pre><code>plt.plot(loss)\nplt.xlabel(\"Epoch\")\nplt.ylabel(\"Loss\")\nplt.show()\n</code></pre> <p></p> <pre><code>key, subkey = jax.random.split(key, 2)\nnf_samples = model.sample(subkey, 10000)\nplt.figure()\nplt.scatter(data[:, 0], data[:, 1], s=0.5, alpha=0.5, label=\"data\")\nplt.scatter(\n    nf_samples[:, 0], nf_samples[:, 1], s=0.5, alpha=0.5, label=\"RQSpline samples\"\n)\nplt.legend()\nplt.show()\n</code></pre> <p></p> <p>Here below is an example to show how the training model interact with the top level resource-strategy API</p> <pre><code>from flowMC.strategy.train_model import TrainModel\nfrom flowMC.resource.optimizer import Optimizer\nfrom flowMC.resource.buffers import Buffer\n\nrng_key, rng_subkey = jax.random.split(jax.random.key(0), 2)\nmodel = MaskedCouplingRQSpline(\n    n_feature,\n    n_layers,\n    n_hiddens,\n    n_bins,\n    subkey,\n    data_cov=jnp.cov(data.T),\n    data_mean=jnp.mean(data, axis=0),\n)\n\nn_chains = 10\nn_steps = 100\nn_dims = 2\n\nnum_epochs = 100\nbatch_size = 10000\nlearning_rate = 0.001\nmomentum = 0.9\n\ntest_data = Buffer(\"test_data\", (n_chains, data.shape[0] // n_chains, n_dims), 0)\ntest_data.update_buffer(\n    data.reshape(n_chains, data.shape[0] // n_chains, n_dims),\n    data.shape[0] // n_chains,\n)\noptimizer = Optimizer(model)\n\nresources = {\n    \"test_data\": test_data,\n    \"optimizer\": optimizer,\n    \"model\": model,\n}\n\nstrategy = TrainModel(\n    \"model\",\n    \"test_data\",\n    \"optimizer\",\n    n_epochs=100,\n    batch_size=batch_size,\n    n_max_examples=100000,\n    verbose=True,\n)\n\nkey = jax.random.key(42)\n\nprint(resources[\"model\"].data_mean, resources[\"model\"].data_cov)\nkey, resources, positions = strategy(\n    key,\n    resources,\n    jax.random.normal(key, shape=(n_chains, n_dims)),\n    {},\n)\n</code></pre> <pre><code>[0.50011855 0.24977292] [[ 0.752463   -0.19324097]\n [-0.19324097  0.24699372]]\n\n\nflowMC.strategy.train_model - INFO - Training model model\nflowMC.strategy.train_model - DEBUG -   Training data shape: (100000, 2)\nflowMC.strategy.train_model - DEBUG -   Number of epochs: 100\nflowMC.strategy.train_model - DEBUG -   Batch size: 10000\nTraining NF, current loss: 1.336: 100%|██████████| 100/100 [00:12&lt;00:00,  7.98it/s]\nflowMC.strategy.train_model - INFO - Completed training model - Final loss: 1.257569\nflowMC.strategy.train_model - DEBUG -   Loss values: min=1.223082, max=2.802106\n</code></pre> <pre><code>output_samples = resources[\"model\"].sample(jax.random.key(42), 10000)\nplt.scatter(output_samples[:, 0], output_samples[:, 1], s=0.5, alpha=0.5, label=\"data\")\nplt.show()\n</code></pre> <p></p>","path":["Tutorials","Training Normalizing Flows"],"tags":[]}]}